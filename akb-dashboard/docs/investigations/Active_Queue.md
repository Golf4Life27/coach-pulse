# Active Investigation Queue

Status: parallel-track to belt build
Discipline: each line = a discrete forensic investigation, briefed to Code separately. Findings discovered during one investigation that aren't in scope of THAT investigation are added to "Discovered during prior investigations" — they do NOT get worked silently.

## Build discipline (A-series)

The A-series is the build-discipline lane — work that defends the
system from session-drift / continuity-loss / re-derivation churn.
Distinct from the INV-series (forensic investigations) so the two
numbering schemes can't collide. Co-exists with the master checklist
Insertion log per Rule 10.

- **[A3] Pipeline_State source-of-truth re-points + dead-filter** — **LIVE on `main` 2026-06-03** (Spine `recUS0oHqXLtEM3lG`, governed by `recio537IOB835NCQ`). Track B closure for the Pipeline_Stage model: `lib/deal-stage.ts:inferDealStage` now consults `pipelineStage` FIRST (with timeline overlays still firing), `app/api/deal-context/[id]` threads it through, `/api/listings` excludes `pipelineStage==="dead"` by default (opt-in via `?include_dead=true` — cleans the dashboard's PipelineBoard + pipeline page in one filter), `/api/morning-briefing` filters dead at the top of its active-set filter. Engine stays sole writer — read-path + filter only, no schema change. Pure mapping module `lib/pipeline-state/pipeline-stage-to-deal-stage.ts` is exhaustiveness-locked via `ALL_PIPELINE_STAGES`. Tests +21 (mapping x9 + deal-stage re-point x12). Co-shipped with INV-023 V1 math gate (see checklist row 2026-06-03 + queue item 23).

- **[A2] Pipeline_State model (status source-of-truth)** — MODEL LOCKED 2026-05-31 (Spine `rec4E0eDrb1f0vTSN`) → **ENGINE + DRY-RUN REPORT LIVE on `main` 2026-05-31** (merge `e220d6d`, prod deploy `dpl_4366FfidE4LMSUrkqs3ubfXADGDc`; per ship decision `recGL50XAKdHAfQH5`, engine pairing `recBeV7cYpdVhKbDS`) → **GATED APPLY ROUTE LIVE on `main` 2026-05-31** (merge `fd9dc14`, prod deploy `dpl_BkyCxtUyu9if4S6uVUsfcmLJWQmM` at SHA `fd9dc14`; per ship decision `rechGJ32oW9Qmv8wp`, builds on verified dry-run `rec7cYhtOBMWRN1PZ`) → **POST-INCIDENT HARDENING + SERVER-SIDE SWEEP + STANDING RULE shipped 2026-06-01** on `claude/pipeline-state-backfill-finish` → **BACKFILL COMPLETE 2026-06-02** (Spine context `recQ2BgorvzaNfziv`): `Pipeline_Stage isEmpty = 0` verified at Airtable source-of-truth; final cumulative histogram `{dead: 2487, intake: 738, verified: 209, outreach_ready: 112, outreach_sent: 64, responded: 28, negotiating: 6}` across 3,644 records; 23 Fields Ave = `negotiating`; Canon §9 blacklist hard-guard verified (`2715 Monterey St` = `dead`); errors = 0 across all runs. **Cross-fix shipped same cycle (`99d40b9`):** the auth-waterfall's `x-vercel-cron:1` defense-in-depth check retired — Vercel stopped sending the header so every prod cron was silently 401'ing (the 2026-05-28 H2 cron 401 finding's root cause). Post-fix `scout:zip_approval_reply_scan` at 04:00:33 UTC = `confirmed_success` `auth_kind: cron`. All prod crons now authenticate. Apply route capped to `max_records=1000` (was 2000) after a `max_records=4000` POST returned HTTP 500 on lambda timeout (~3,358 writes still persisted, data safe but operator-facing 500). New CRON_SECRET-gated route `/api/cron/pipeline-state-backfill-sweep` + GitHub Actions workflow `pipeline-state-backfill-sweep.yml` (workflow_dispatch) so the final 86 + future sweeps run server-side, never from a browser console. SYSTEM_FACTS.md §8 amended with the standing rule: operational/execution/diagnostic work is Code's, not Alex's. Cumulative sweep summary lands in checklist row 2026-06-01 once the workflow fires. Apply route at `POST /api/admin/pipeline-state-backfill-apply` enforces every guardrail in code: dashboard-cookie-ONLY auth (OAuth/CRON_SECRET refused), today's date as a confirm token (replay protection), Canon §9 blocklist hard-guard via `isNeverResurfaceLoose` (any of the 12 addresses → forced to `dead`), idempotent (pre-filter + engine noop short-circuit, resumable), every write routed through `transitionStage` for audit, bounded-concurrency batched with per-record error isolation + wall-clock budget + summary response. **Mass write stays GATED for operator go** — the route is deployed and ready to fire on `confirm: "BACKFILL-PIPELINE-STATE-YYYY-MM-DD"`. Next: D3 + DealStage per-scenario migrations. Vercel-native transition engine (`lib/pipeline-state/{stages,transitions,derive,engine}.ts`) is now the **sole writer** of `Pipeline_Stage`; legal-edge guard (forward-one-step / kill / resurrection) enforced before every Airtable write; refusals audited as `confirmed_failure`. Existing orchestrator `advance-stage` route refactored to route through engine (override bypasses gate, NOT legal-edge). `isUnderContract` generalized per decision #4 (stage-based primary, envelope fallback during migration). `responded` stage added per decision #2 and flows through the orchestrator types union automatically. **D3 appliers + `DealStage` derivation NOT migrated this commit** — per-scenario retirement matches the Make-coexistence architecture default. **Mass backfill NOT performed** — `deriveStageFromLegacy` pure fn shipped, awaiting separate operator-reviewed dry-run report. **Tests: +60, full suite 1507/1507; tsc clean.** See Master Checklist Insertion log row 2026-05-31 for full ship detail. Next: dry-run backfill report (no writes) for operator review; per-scenario migrations of D3 + DealStage + Make consumers.

- **[A1] System Facts vault** — COMPLETED 2026-05-31 → **LIVE on `main` 2026-05-31 (merge `d36f26c`)**. Canonical record
  at `akb-dashboard/docs/system/SYSTEM_FACTS.md`. Wired into Maverick
  `load_state` as the FIRST section of every structured briefing
  (synthesizer Rule 0 + template lead-section). Closes the
  "no canonical System Facts" gap Maverick has flagged at session-open
  for multiple sessions; the Vercel-plan (Pro, not Hobby) question
  alone re-derived incorrectly twice before this. Authority rule:
  vault wins over code, AGENTS.md, Spine recall, and prior training
  when they contradict. Paired build_event written to Spine this
  commit cycle (Rule 9). See Master Checklist Insertion log row
  2026-05-31 for full ship detail. **Stale-info flagged for next
  edit:** `akb-dashboard/AGENTS.md` still says "Vercel Hobby plan";
  vault supersedes but file itself should be amended on next touch.

## Active

1. **[INV-001] Distress_Pass evaluation integrity** — COMPLETED 2026-05-20 → `docs/investigations/Distress_Pass_Audit_v1.md` → REMEDIATION SHIPPED (Option α, Spine `rece3J2f0TbHXOuE2`)

2. **[INV-002] Listing_Condition persistence** — COMPLETED 2026-05-20 → `docs/investigations/Listing_Condition_Audit_v1.md` → REMEDIATION SHIPPED (Option A, Spine `recOB75kGmHzkPgKr`) — end-to-end runtime tests deferred to next intake cycle (Scenario A is `isActive: false`)

## Queued

3. **[INV-003] Stage_Calc V1 vs V2 sibling formulas** — SHIPPED 2026-05-20 → `docs/investigations/Stage_Calc_V1_V2_Audit_v1.md` → Option A + Path 2: Stage_Calc_V2 formula updated (MLS-date gate at top, returns `"Data Issue: Missing MLS Date"` when Has_MLS_Date=false), Execution_Path_Calc formula updated (LEFT prefix match routes Data Issue → Manual Review), Stage_Calc V1 deletion PENDING OPERATOR UI ACTION (no MCP `delete_field` tool; brief acknowledged). Verified on 2 known SqFt-divergent records + 6 live missing-MLS-date records (no synthetic test needed). Spine `recPKEhirWUWthpXS`. ~10 records/full population now correctly flagged at intake instead of silently passing V1.

4. **[INV-004] Crier stale-contact false-positive on fresh contracts** — SHIPPED 2026-05-20 → `docs/investigations/Crier_Stale_Contract_Audit_v1.md` → Option D (Hybrid): Option A guard ships now + Option B phase-aware ladder documented for Phase 14. `isUnderContract(listing)` exported from `lib/maverick/deal-commentary.ts` gates the Crier silence branch on `Envelope_ID`. Pulse `stale-data-drift` detector inherits the guard + closes a parallel Phase 11.2 regression (4-field `mostRecentTouchMs` parity). 9 new unit tests, full suite 990/990 passing. Belt v1 Spec §6 documents the Phase 14 ladder + 4 prerequisites. Spine `rec0A9ZWSMMT5Nk9a` (related to recOry0VQVddhEWFb Phase 11.2). Zero records affected today (no Envelope_ID populated); preventive infrastructure activates the moment DocuSign provisioning lands.

5. **[INV-005] Phase 4B Rehab fallback when scrape + Street View both empty** — SHIPPED 2026-05-23 → `docs/investigations/INV-005_Audit_v1.md` (audit + Remediation outcome appendix) → Option D core + local-1 + local-4 in-scope. New Airtable field `Rehab_Source` (singleSelect: `vision` / `manual_operator` / `manual_partner`, id `fldhn2vxQipa3PVsX`) auxiliary to `Rehab_Confidence_Score`. New POST `/api/agents/appraiser/rehab/[recordId]/manual` operator fallback unlocked AFTER GET returns no_photos/photo_collection_failed/vision_call_failed. New POST `/drift-resolve` for vision-vs-manual drift reconciliation. New daily 15:00 UTC cron `/api/cron/rehab-vision-retry` re-runs vision for manual_operator records with 7-day cooldown; on drift > 25%, surfaces Type 2C banner in AppraiserRehabPanel via `[REHAB_DRIFT_DETECTED]` Notes marker — NEVER silently overwrites. Provenance badge plumbed through Listing type + BroCard PricingBlock. Constitution Rule 3 enforced: manual is fallback-only, vision always tried first, no preemptive bypass. 38 new tests, full suite 1038/1038 passing. local-2 + local-3 PROMOTED to INV-021. Spine `rec84OQ8UCqLKElCU`. 1,025 active records eligible for the original silent-block now have manual unlock; 90 Street-View-only records can be manually entered the moment vision returns no_photos.

6. **[INV-006] Outreach_Status / Stage transition logic on signed contracts** — SHIPPED 2026-05-20 → `docs/investigations/Outreach_Status_Transition_Audit_v1.md` → Option D (Hybrid): cron reconciler `/api/cron/outreach-status-reconcile` daily 14:00 UTC + Phase 12.7 in-process arm documented in Belt v1 Spec §6. Pure helper `lib/maverick/outreach-status-reconcile.ts` (Envelope_ID + eligible state + no Notes marker → transition). 10 new unit tests, full suite 1000/1000 passing. Option A (Airtable native automation) rejected by discipline. Spine `recmiFJLja4cHrZ3O` (related to `rec0A9ZWSMMT5Nk9a` INV-004 patch). Zero records affected today; activates when DocuSign provisioning lands.

7. **[INV-007] Multi-listing-agent message attribution — CRITICAL** — COMPLETED 2026-05-20 → `docs/investigations/Multi_Listing_Agent_Attribution_Audit_v1.md` → STEP 1 REMEDIATION SHIPPED (Spine `rec5UVLsXktD8M8Mi`) — `/api/conversations/[id]` now consumes `scorePropertyMatch` with the same 0.6 confidence floor the AMBIGUOUS banner uses; visible leak closed. **STEP 2 DESIGN SPEC COMPLETE** → `docs/specs/Attribution_Layer_v1_Spec.md` → pending operator decision on sprint authorization (4-sprint plan; §8 open questions include storage substrate choice). When built, resolves INV-007 Step 2, INV-014, INV-015, INV-016, and the `scorePropertyMatch` unit-test gap.

8. **[INV-008] DD Checklist auto-extraction from comms chain** — DD Checklist shows 0/12 on 23 Fields despite operator stating DDs have been answered in conversation history (Quo + Gmail). No auto-extraction pipeline from comms content → structured DD fields. Manual click-tracking defeats system intent: the answers are already in the chain, the system just doesn't parse them. **Brief: `docs/investigations/INV-008_Brief.md` (2026-05-21).** Pair-with INV-006 — same architectural pattern.

9. **[INV-009] Date formatting bug — "2001" instead of "2026"** — Conversation panel timestamps render as "5/6/2001" etc. when source dates are 2026-05-06. Likely `formatString` or `Date` constructor issue in deal-room conversation renderer. Probably YY parsing vs YYYY mismatch. **Brief: `docs/investigations/INV-009_Brief.md` (2026-05-21).** Severity unknown until reproduced — MEDIUM provisionally, HIGH if persisted dates are corrupted.

10. **[INV-010] RESPONSE DUE alert false-positive contradicting deal stage** — Deal at `STAGE: ACCEPTED PENDING PA` but RESPONSE DUE alert flags "agent waiting on you." Needs stage-aware suppression: certain alerts should not fire past specific stage thresholds. Related to INV-006 but a distinct surface (alert system vs badge rendering). Brief: TBD.

11. **[INV-011] Make A vs Vercel intake-path divergence** — Discovered during INV-002. The two intake paths handle missing-condition differently (Make: silent drop; Vercel: pass) and handle Reject differently (Make: no row; Vercel: persists row with `Execution_Path = "Reject"`). Produces structurally different state for identical inputs. Worth a separate audit when Belt v1 spec §3.5 "Path 1 vs Path 2" decision lands. Brief: TBD.

12. **[INV-012] Condition_Score is a dead field** — Discovered during INV-002. `Condition_Score` (`fldkE6xgHeCvmyKJy`) declared on Listings_V1 with description "AI assessment of property condition from listing data (1-10 scale)" but zero code paths write it (codebase grep confirms). 0/50 records in 30-day sample populated. Either repurpose (and update description) or remove from schema. Low urgency; cleanup task. Brief: TBD.

13. **[INV-013] Phase 4B Rehab has no vision-failure fallback** — Discovered during INV-002. `/api/agents/appraiser/rehab/[recordId]` returns HTTP 502 with `vision_call_failed` and halts when Anthropic vision call fails — no fallback to any other condition signal. With `Listing_Condition` now persisted (post INV-002 remediation), the fallback would be a one-line addition: `vision.condition_overall ?? listing.listingCondition`. Brief: TBD.

14. **[INV-014] L3 winner-takes-all attribution** — L3 Make Scenario 4812756 (Reply_Triage_V3) Module 3 (`ActionSearchRecords`) uses `maxRecords: 1` with no sort order. When multiple Listings_V1 records share an Agent_Phone (common for multi-listing agents), L3 updates whichever record Airtable returns first — non-deterministic. Inbound messages can be attributed to any of the agent's listings depending on Airtable query order. Foundational to L3 reply-triage correctness. Should be wired through the unified attribution layer that lands in INV-007 Step 2. Brief: TBD.

15. **[INV-015] scan-comms cron fan-out** — `app/api/cron/scan-comms/route.ts` lines 228-262 groups listings by phone and, for each inbound message, creates an Agent_Proposals row for EVERY listing in the group. One Candice reply → 4 pending proposals, all referencing identical inbound body. Produces N duplicate proposals per actual reply; affects Sentinel queue cleanliness and operator review surface noise. Same redesign-path as INV-014 (unified attribution layer, INV-007 Step 2). Brief: TBD.

16. **[INV-016] `scorePropertyMatch` price-match misaligned** — `lib/timeline-merge.ts:54-60` matches body `$N,NNN` patterns against `targetPrice` (= `List_Price`). But H2 outbound bodies (`outreach-fire/route.ts:31-38`) contain the OFFER (≈ 65% × List_Price), not the list price. The +0.3 price-match bonus never fires correctly on H2 outbound. Attribution still works via token-match + "listing at" bonus (which together reach 0.8 confidence on standard H2 sends), but a designed signal is dead weight. Cheap to fix once confirmed with live H2 message bodies (compare against both `List_Price` and `Outreach_Offer_Price`). Brief: TBD.

17. **[INV-017] Quo MCP read/write auth asymmetry** — Discovered during 23 Fields buyer outreach session 2026-05-21. `Quo:send-message` succeeded across 6 sequential outbound texts with no auth errors. `Quo:fetch-messages` returned `API key required or invalid` on first two consecutive calls, then succeeded on third call after ~15 minutes. Suggests intermittent read-scope token refresh or rate-limit asymmetry between send and read endpoints. Affects ability to monitor inbound replies without operator manual relay — direct blocker to closed-loop buyer-conversation automation. Brief: TBD.

18. **[INV-018] Decision Preconditions failure-mode audit** — Discovered during 23 Fields 5/12 underwriting (theoretical 65% rule produced a confident pencil/no-pencil verdict from incomplete inputs; empirical Buyer_Median was not loaded at decision time and the 5/12 verdict was later revised when full intel landed) AND 5/21 Ardeep buyer-outreach near-miss (almost pitched property violating two hard buy-box criteria) AND 5/22 INV-020/022/023 v1 drafts embedding operator-click-to-authorize patterns. Maverick generates confident outputs from incomplete data AND embeds operator-in-loop laziness into agent specs. Resolution drafted at `docs/specs/Decision_Preconditions_Amendment_v1.md` (Rule 1 Pricing Precondition + Rule 2 Buy-Box Precondition + Rule 3 Dashboard-First Autonomy). Pending operator ratification + Spine entries. Brief: PROPOSED RULES READY.

19. **[INV-019] Quo MCP silent delivery failure** — Discovered 2026-05-22 when buyer Jaylen Walker FB-DM'd asking for property info via email; Quo log showed his 5/21 outbound text as `outgoing` (API accepted) but **carrier did not deliver**. `Quo:send-message` returns success on API acceptance, NOT on carrier delivery confirmation. No failed-delivery signal surfaced anywhere. Adjacent to INV-017 (read/write auth asymmetry). Requires either: (a) Quo MCP exposes delivery-status field in fetch-messages payload we begin polling, (b) Quo provides webhook callback on delivery failure wired to Action Queue, or (c) per-send delivery-confirmation poll loop. Brief: TBD.

20. **[INV-020] Gmail inbound triage + Action Queue surfacing for active deals** — Discovered 2026-05-22 when operator missed a hot Texas seller-agent response (Les Mauck, 5435 Callaghan Rd, 5/20) for 48 hours, buried under ~20 newsletter/notification unread emails. Also surfaced: 1665 Ford St Mutual Release sent via Authentisign 5/11, 11 days unsigned. Architecture: Gmail watcher cron + severity classifier + attribution to active deal + auto-draft holding-reply (Type 2A queued for operator dashboard approval, SMS variant-picker for time-crunch, HOLDS on no-response per operator preference). Pair-with INV-006, INV-008, Reply_Triage L3 4812756. Adjacent: Quo MCP `fetch-missed-calls` capability (2026-05-22) folds into same architecture. **Brief: `docs/investigations/INV-020_Brief.md` v2 (2026-05-22 autonomous-by-default rewrite).**

21. **[INV-021] 4-consumer photo-collection divergence** — Promoted from INV-005 audit local findings local-2 + local-3. Four routes consume `collectPhotos` from `lib/photo-sources.ts`: rehab + photo-analysis HARD-BLOCK on empty result (422 / vision-not-run); pricing + highland-validation GRACEFULLY DEGRADE. `photo-sources.ts` documents a graceful-degrade contract the rehab route violates. Same shape as INV-011 (cross-consumer divergence). Resolution: unify the 4 routes on a single contract (likely the graceful-degrade pattern, with INV-005's manual-input affordance as operator-action surface). Brief: TBD.

22. **[INV-022] Data Source Federation Layer for DD-phase auto-hydration** — When `Outreach_Status` flips to `Negotiating` or `Offer Accepted`, system auto-triggers: PropStream (owner + liens + flood + crime grade), RentCast (`/v1/avm/value` + `/v1/avm/rent` + sold-comps), InvestorBase smart-match buyer pull, Firecrawl listing re-scrape, FEMA flood. Aggregates to `Property_Intel` Airtable record with provenance per field. All Type 1 autonomous per Constitution Rule 3 — no operator clicks for data pulls. Material discrepancies (lien presence, owner mismatch, flood zone) surface as Type 2C judgment items. Foundational dependency for INV-023 Underwriter Agent. Open question: PropStream + InvestorBase API access status — if APIs unavailable, fallback is browser automation (Claude in Chrome / Make.com browser modules), NOT operator CSV upload. **Brief: `docs/investigations/INV-022_Brief.md` (2026-05-22).**

23. **[INV-023] Underwriter Agent (Contract-Phase Quality Gate)** — **SOURCING-REALITY BATCH 2026-06-05** (Spine pending operator mirror — Maverick MCP down). Sweep selection re-pointed off lexicographic order to `Live_Status=Active AND Verification_URL non-empty` (`getRehabSweepCandidates`, wired into `appraiser-backfill?selection=rehab_ready`); base-verified candidate count **1,751** (URL-less actives **396**). `/api/admin/url-backfill` resolves URLs for the 396 via Firecrawl + STRICT address↔URL confirm (`strictAddressUrlMatch` — street number + distinguishing street-name token; never fabricates) + still-on-market. Intake now PERSISTS the Firecrawl-resolved URL on create (was discarded → root cause of URL-less new records). New Pulse `verification_url_coverage` detector + scan metric ("% active with a URL"). 23 Fields Ave (rec1HTUqK0YEVb7uA, contract terminated 6/4) disposed to `dead` through the stage engine kill edge (engine sole writer). Vision-on-924 diagnostic via temp cron (`REHAB … conf=N gate=…` log). Tests +17; suite 1632/1632. **OFF-MARKET / STREET-VIEW-ONLY PREFLIGHT GATE + TIGHT FIRECRAWL FILTER + SILENT-422 SWEEP 2026-06-04** (Spine `recd9RNKGWOWjjDzz`, operator-mirrored). Operator catch on 5705 Glen Forest Ln rehab estimate ($20,700 conf 52% on an OFF-MARKET property) traced to: (a) 3 of 4 prior rehab writes had NULL Verification_URL → vision got Street View only; (b) 4th was Live_Status=Off Market — Redfin URL still loaded but scrape mixed stale subject photos with comps + chrome. New preflight in `/api/agents/appraiser/rehab/[recordId]`: refuse 422 `listing_not_actively_listed` when Live_Status≠Active; refuse 422 `street_view_only_insufficient` when all collected photos came from Street View only. New `filterPropertyPhotos(urls, sourceUrl)` in `lib/photo-sources.ts`: chrome denylist + cluster by Redfin listing-id + variant dedup; preserves all genuine subject photos (interior + exterior). 14 new tests. Silent-422 sweep across 5 writers: `manual-rehab.ts`, `pricing/[recordId]`, `photo-analysis/[recordId]`, `pre-offer-screen/[recordId]`, `dd-volley-send/[recordId]` — fixed renames or removed no-analog writes. `LISTING_NAME_MAP` purged of 14 stale read mappings. Downstream consumer impact surfaced for operator schema decision (5 routes had been silently blocking / passing on always-null reads). 4 stale rehab writes cleared from Airtable. Suite 1610/1610. **PHOTO-SOURCE VERDICT + 4 REHAB WRITES (Firecrawl works, RentCast empty, ScraperAPI cleared but demoted, base64 vision verified) 2026-06-04** (Spine `reckfHQurMbSqJxeX`, operator-mirrored). Code fired the readiness probe itself via the same temp-cron pattern as the prior session cycle. **`P924 R0+-/0 F200+69 S200+79`** decoded: RentCast 924 Sunnyside returns NO photos (source=null, /properties=0, /listings/sale=null); Firecrawl `/v2/scrape` returns HTTP 200 + 69 CDN image URL matches; ScraperAPI's prior 500 cleared (200 + 79 matches) but stays demoted per directive. Rehab write count **4 records** through the Firecrawl + base64 path: 1138 Santa Anna $32,538 (conf 42); 427 Donaldson Ave $48,420 (conf 42); 1629 W Woodlawn Ave $47,040 (conf 38); 5705 Glen Forest Ln $20,700 (conf 52). All four confidence <60 → rehab gate continues to HOLD per directive (no exterior-only / low-confidence passes). Mid-cycle field-name fix `Est_Rehab_Low`/`Est_Rehab_High` → `Rehab_Est_Low`/`Rehab_Est_High` (Airtable schema names; 422 swallowed the first three fires). Same wrong names still in `pricing` / `photo-analysis` / `manual-rehab` / `lib/airtable.ts` Listing-type mapping — backlog cleanup. ScraperAPI NOT upgraded. Buyer_Median stays γ/α. Cron cleanup commit pending. **PHOTO-SOURCE PRIORITY REWORK + BASE64 VISION FIX 2026-06-04** (Spine `reckfHQurMbSqJxeX`, operator-mirrored; Maverick MCP connector down). Two systemic prod blockers diagnosed last cycle were the gate to rehab: ScraperAPI HTTP 500 on `render=true` + Anthropic vision 400 "URL disallowed by robots.txt." Both attacked here without an upgrade purchase: **(1) Photo-source priority order REORDERED to "least-scraping-first"** — `collectPhotos` now tries RentCast structured photos → Firecrawl `/v2/scrape` HTML → ScraperAPI raw → Street View, in that order. RentCast is already paid + already authenticates + would return no-bot-wall photo URLs if its payload carries them; Firecrawl is already wired for verify-listing (paid, render-handled). Both layered above ScraperAPI keeps ScraperAPI as a legacy fallback rather than a dependency. `lib/rentcast.ts` exports new `getListingPhotosFromRentCast` + pure `extractPhotoUrls` / `findPhotoFieldKeys` (probes BOTH `/listings/sale` and `/properties` endpoints, looks at `photos`/`images`/`media`/`photoUrls`/`imageUrls` keys including nested `{url}`/`{originalUrl}` shapes). `lib/photo-sources.ts` exports new `scrapeListingPhotosFirecrawl` + `probeFirecrawlPhotos` + `probeRentCastPhotos`. **(2) Base64 vision fix shipped** in `lib/rehab-calibration.ts` — `callRehabVision` now fetches each photo URL server-side, base64-encodes, sends as `{type:"image", source:{type:"base64", media_type, data}}` to Anthropic. Bypasses the robots.txt refusal entirely (Anthropic only fetches URLs itself when given URL-source blocks; it always accepts base64 bytes). Per-photo fetch failures degrade gracefully (logged into `methodology_notes`, throws only when ALL photos fail). **(3) `/api/admin/appraiser-readiness` extended** to probe RentCast + Firecrawl photo sources alongside the existing ScraperAPI probe — default probe target remains 924 Sunnyside (`https://www.redfin.com/TX/Dallas/924-Sunnyside-Ave-75211/home/32118136` + `924 Sunnyside Ave / Dallas / TX / 75211` for RentCast). Console-logs flat `RC.*` / `FC.*` / `PROBE.*` lines so cron-fire visibility is preserved. **Operator action requested: hit `/api/admin/appraiser-readiness` so `rentcast_photo_probes[0]` reports `source` (listings_sale | properties | null), `*_photo_count`, and `sample_photo` for 924 Sunnyside — that determines whether priority-1 RentCast carries photos for prod records or whether we proceed on Firecrawl alone.** Gate posture unchanged: rehab HOLDS on Street-View-only / low-confidence — exterior-only never passes. ScraperAPI NOT upgraded. Buyer_Median stays γ/α (β not built). 7 new tests; suite 1596/1596 green; tsc clean. **SCRAPER KEY LIVE, REHAB STILL BLOCKED (2 new causes) 2026-06-04.** Operator added SCRAPER_API_KEY + GOOGLE_MAPS_API_KEY; redeploy confirmed `scraper_key=true`. But rehab still 0 writes, now blocked on: (1) **ScraperAPI HTTP 500 on `render=true`** Redfin scrape (plan-tier/render-credits — operator verify ScraperAPI JS-rendering); (2) **Anthropic vision 400 "URL disallowed by robots.txt"** on the Street View fallback URL — vision pipeline passes image URLs to Anthropic which respects robots.txt; fix = fetch bytes server-side → base64 in `callRehabVision` (code fix, pending operator go). **ARV now writing** (sqft backfill unblocked confidence — 3 fresh ARV_Validated_At) + Rent writing; only Est_Rehab blocked. Buyer_Median unchanged (γ/α, no β). **PHOTO VERDICT + SQFT BACKFILL 2026-06-04** (Spine `recNMggeuLa7QBlIE`). **Photo collection is SYSTEMIC-broken: `SCRAPER_API_KEY` is unset in the production Vercel env** (probed via new `/api/admin/appraiser-readiness` → `PROBE.url.scraper_key=false`). Not a Redfin HTML change, not per-record. Operator must set `SCRAPER_API_KEY` in Vercel prod; until then every rehab vision call 422s `no_photos_available` and Est_Rehab can't hydrate. **Building_SqFt backfilled 5/5** (the only 5 active records missing it, all from the 05-27 SA-782xx intake) from RentCast `getSubjectFacts` — so sqft is no longer a rehab blocker; the SCRAPER_API_KEY gap is the sole remaining one. **Backfill skip/cursor shipped** (`?skip=` / `?after=` + `next_cursor`) so the sweep stops recycling structurally-blocked records. ARV mostly there (confidence-floor, not data gap); Buyer_Median unchanged (γ/α, no β). **CLUSTER SWEEP RUN 2026-06-04** (Spine `rec6e6hYLuOpaLANf` reconciliation cycle). 7 cron fires across the 34-record active cluster yielded: **13 `arv_computed` (1 written `Real_ARV_Median` + `ARV_Validated_At`, 3 dropped to `manual_review_low_arv`, 9 below write threshold), 13 `dual_track_computed` (13 records gained `Estimated_Monthly_Rent`), 0 rehab writes (all 422)**. Rehab failures sampled: 2 of 3 records missing Building_SqFt + Verification_URL (intake-time data gap), 1 with both populated but `no_photos_available` (Redfin scrape + Street View both empty — flag for ScraperAPI/key investigation). Coverage gaps surfaced for operator triage: (a) Building_SqFt audit on the 31 still-eligible records, (b) photo-collection investigation on 924 Sunnyside (a probably-environmental signal), (c) cron's lex-ordered processing wastes calls on structurally-blocked records — flagged as backlog (route should accept `skip_recordIds[]` / cursor, OR auto-skip records consistently 422-ing across N fires). Reconciled `mao-range.ts:DEFAULT_WHOLESALE_FEE` to import from `pre-contract-math.ts` so BroCard pricing + PC-27 share one fee constant (operational $5K). 1219 E Highland anchor in `mao-range.test.ts` moves from $90K to $100K floor at the same inputs — mechanically correct with the lower fee. Hydration verdict from prior cycle unchanged: PC-26 still holds (Buyer_Median zero writers); PC-27 still holds (Est_Rehab gated on the data-gap triage above). **HYDRATION SCOPE ADDED 2026-06-04** (Spine `recSM9Bm7vhY63Aee`). Hydration verdict for the three inputs PC-26/PC-27 hold on: **ARV** = quick automation (`/api/agents/appraiser/arv/[recordId]` + `/api/admin/appraiser-backfill` already exist, RentCast comps are the source, one cron-pass closes the active cluster). **Est_Rehab** = moderate (full vision pipeline at `/api/agents/appraiser/rehab/[recordId]` exists; bottleneck is photo collection success rate via ScraperAPI + Street View per INV-005's manual fallback). **Buyer_Median** = real lift — InvestorBase scraper does NOT exist (`Property_Intel.Buyer_Median_Value` field + provenance fields exist; `VendorSource = "investorbase"` is in the type union; no HTTP client anywhere in `lib/`). Three paths surfaced: α build InvestorBase scraper (2-4 day lift, operator credential install required), β RentCast sold-comp median proxy (~30min code, semantic-drift retail-leaning, Rule 1 violation in spirit), γ operator-manual per-deal population (supports the current 28-record active-cluster scope, zero new code). Recommendation: γ for immediate Pulse cluster, α for durable. V1.1 details preserved below. **V1.1 SHIPPED 2026-06-03** (Spine `rec937cFJthvCZzBM`, builds on V1 `recUS0oHqXLtEM3lG`, governed by `recio537IOB835NCQ`). V1.1 updates: `DEFAULT_WHOLESALE_FEE` 15000 → **5000**; 1219 E Highland Your_MAO assertion $75K → **$85K**; 23 Fields still blocks at $5K fee (Your_MAO=$20K vs $61,750 contract); Gate 4 already auto-blocks on advance-to-under_contract via existing `STAGE_GATE_MAP` (confirmed wired); **no-override path enforced** for PC-25/26/27 by extending `advance-stage` route's inviolable filter to catch `data_missing` for INV-023 items (existing PO-04/PO-05/PC-05/PC-16 cohort keeps fail-only semantics — no regression there). **Buyer_Median hydration CONFIRMED MISSING:** `Property_Intel.Buyer_Median_Value` has zero writers anywhere in the codebase; INV-022 brief lists InvestorBase but the scraper/API client doesn't exist. PE-02 (existing Pre-EMD gate) has been holding on the same gap since it shipped — principle-correct refusal per Decision Preconditions Rule 1. PC-26 will continue to hold every record until a real Buyer_Median source is wired; **surfaced for operator decision**, not silently proxied (RentCast sold-comp median would semantic-drift retail-leaning and systematically over-MAO). V1 details preserved: Pure math + three hard-block preconditions wired into Gate 4 (pre-contract) as PC-25/PC-26/PC-27. `lib/pre-contract-math.ts` exports `computeInvestorMao` / `computeYourMao` / `evaluatePreContractMath`; checks read `ctx.listing.contractOfferPrice` + `ctx.propertyIntel.buyerMedianValue` + `ctx.listing.estRehabMid ?? estRehab` + `arvValidatedAt`. **NO override path** on PC-25/26/27 (`failure_action: "block"`). Status routing: decisive math failure → block (Type 2C); missing inputs → data_missing HOLD (retry-able per Rule 3, no operator surface); block wins over hold when math is decisively negative. **Acceptance regressions in `pre-contract-math.test.ts` (+23 tests)** lock the operator-stated anchors: 23 Fields at $61,750 vs $45K Buyer_Median + $20K rehab → BLOCK (Investor_MAO=$25K, Your_MAO=$10K); 1219 E Highland Blvd 78210 at $120K Buyer_Median + $30K rehab → Investor_MAO=$90K, Your_MAO=$75K. **What V1 covers:** the structural math precondition. **NOT yet in V1:** the wider DD checklist from the brief (mortgage payoff viability, federation discrepancy gates, contract-clause auto-extraction, buyer pipeline, photos-vs-modeled variance, downstream cascade). Those land in V2 alongside INV-022 hydration. The full brief stays at `docs/investigations/INV-023_Brief.md` v2 as the spec. **Dependency reality:** PC-26 currently HOLDs on every record because INV-022's `Property_Intel.Buyer_Median_Value` isn't yet populated in prod — the gate is correctly refusing to advance until hydration lands, which is the principle-correct default.

24. **[INV-024] DocuSign + Authentisign webhook wiring to dashboard Action Queue** — Discovered during INV-020 v2 scoping. DocuSign and Authentisign have direct webhook surfaces for envelope state changes (sent → viewed → signed → completed). These should bypass Gmail-inbound classification entirely and land directly in operator Action Queue as Type 2B items (envelope ready to sign). Confirm: do these webhooks already wire to dashboard, or is this net-new integration work? If net-new, ship as part of INV-020 v2 or as standalone? Brief: TBD.

25. **[INV-025] Crawler Engine — Route Planning + Cadence + Geographic Logic** — Autonomous MLS-pull engine that snakes through all eligible US states (44 states = 50 − wholesale-restrictive IL, MO, SC, NC, OK, ND) on an organized route plan, replacing today's manual PropStream-CSV-drop workflow. Spec'd in `AKB_Crawler_Build_Roadmap.docx` (4/30/2026) and gated as Phase 5 per userMemories. Dependency: must ship AFTER INV-005 (rehab affordance), INV-022 (data federation), and INV-023 (Underwriter Agent) because (a) Scenario I and K depend on INV-005 + INV-022, and (b) cadence + per-ZIP eligibility decisions need INV-023's buyer-pool-per-ZIP density data. Four design decisions DEFERRED to full-brief authoring after dependencies land:

    - **D1 (cadence philosophy)**: round-robin / density-weighted / geographic clustering / hybrid
    - **D2 (granularity)**: state-level / metro-level / ZIP-level eligibility maintenance
    - **D3 (vendor access path)**: Redfin scrape only / PropStream API / direct MLS via licensed-agent IDX / hybrid cost-optimized
    - **D4 (explore vs exploit budget)**: % of daily crawl budget to probe new ZIPs vs deep-mine known-productive ZIPs

    Operator north star (2026-05-22): "open the crawler to any state that doesn't have restrictive wholesale laws ... it needs to have an organized route plan to move throughout the country." Wife-retirement bet per userMemories. Brief: TBD (after INV-022 + INV-023 ship and data exists to ground D1-D4).

26. **[INV-026] Wife-Retirement Progress Meter — System Self-Awareness of Mission Progress** — Formalizes the metrics that actually mean something for the wife-retirement mission, replacing fake "% complete" thinking. Three load-bearing numbers tracked continuously and surfaced on dashboard:

    - **Lost-Phone-Test failure count**: number of pipeline stages that stall if operator unavailable for 7 days. Current baseline 2026-05-23 (post-INV-005): 4 HIGH-risk stages (intake CSV manual, ARV/Comps Scenario K not built, Contract Underwriting no agent, Crawler engine 0%), 5 MEDIUM-risk stages (Phase 4B Rehab dropped to MEDIUM today post-INV-005, Pricing math V2.1 InvestorBase manual, Reply triage Gmail not wired, Buyer match InvestorBase manual, Dispo/closing no orchestration), 2 LOW, 1 EXPECTED manual. Target trajectory: <2 HIGH stages by end of Crawler 1.0 ship; <1 by Crawler 2.0 unlock criteria.
    - **Monthly deal velocity**: deals closed per month. Target: $40K/mo net for 3 consecutive months = Crawler 2.0 unlock per Bible §1.2 + Appendix A.1. Current trailing 3-month: $0/mo. Per-deal P&L bundled in Phase 15.5.
    - **Operator hours-per-week on the system**: target <15h/wk for Dream Phase unlock per Bible §1.2. Current rough estimate 33-58h/wk (120-287% over target). Top consumers: dashboard time, code-paste coordination, manual data hydration, DD work. Will collapse with INV-022 ship (data hydration), INV-020 ship (inbox triage), INV-023 ship (DD checklist), GITHUB_PAT provisioning (code-paste coordination).

    Aligns with Phase 15.4 (NOT STARTED) in master checklist. Phase 15.4 has the rough scope ("annual income replacement target"); INV-026 builds the autonomous measurement + surface. All Type 1 autonomous per Constitution Rule 3 — system computes, system surfaces, operator never types numbers into a spreadsheet. Material movement (positive or negative) on any metric becomes Type 2C card with diagnosis ("Lost-Phone risk rose 1 stage this week — Phase 4B Rehab silent-blocks returned; INV-005 remediation regressed?"). First baseline snapshot persisted to Spine as recPFp69aDXhs95V8 on 2026-05-23.

    Brief: TBD (after INV-022 + INV-023 ship; metric infrastructure depends on Property_Intel hydration + deal-state-machine completeness). Genesis: 2026-05-22 operator question "do you have a gauge as to what % we have completed in the build now" — surfaced that % complete is misleading and the real metric is operator-required → operator-optional progress.

<!-- INV-027 slot reserved — not filed on this branch (queue jumps 026 → 028 per operator's explicit numbering; 027 presumed filed in a parallel session, reconcile on merge). -->

28. **[INV-028] Firecrawl Production Wiring + Listing Content Classification** — **COMPLETED-BY-MERGER 2026-05-25** into the Crawler intake ship (commit pending this cycle). Firecrawl wired live in `lib/crawler/sources/firecrawl.ts` (`POST /v2/search` with inline `scrapeOptions` markdown — resolves listing URL by address + scrapes in one call, since RentCast carries no URL/free-text). Consumed by `/api/cron/listings-intake` to exclude renovated/turnkey listings (`RENOVATION_EXCLUSION_KEYWORDS` exported CONST, operator-tunable) + cross-check still-Active (catches RentCast staleness). New reject reasons: `firecrawl_renovated`, `firecrawl_inactive`, `firecrawl_url_unresolved`. Budget: `FIRECRAWL_MAX_SCRAPES_PER_RUN` (default 1000) + per-run Spine-write on budget hit + `creditsUsed` surfaced in audit. Requires `FIRECRAWL_API_KEY` in prod (operator action). The original broader scope (off-market body-text detection, flip keyword scoring, DOM discrepancy → the 7 reserved Property_Intel fields) was NOT all built — only the renovation-exclusion slice needed for intake quality. Remaining Property_Intel keyword-scoring fields stay reserved for a future enrichment pass if needed. ~~Original scope below.~~ **Status:** ~~Brief TBD~~. **Priority:** Medium-high. **Depends on:** INV-022 v1 (Property_Intel schema + cron architecture must land first). **Unblocks:** pipeline quality — closes the keyword-scoring gap left by Scenario B retirement; richer DD signal for INV-023 Underwriter Agent. Genesis: Firecrawl egress validated 5/20 (HTTP 200, 26,836 chars markdown against 4838 Wisteria, content markers present, zero anti-bot signals — checklist 22.1 DONE). Capability validated but **unwired** — no `lib/firecrawl.ts`, no production route calls `api.firecrawl.dev`. Intended to replace the unbuilt half of Scenario B (retired): off-market body-text detection (checklist 1.4), flip/renovation keyword scoring (checklist 1.5), DOM discrepancy check (checklist 1.6), Restriction_Risk_Level computation. RentCast `/v1/listings/sale` (live) confirms active status + price; Firecrawl provides the listing-content / keyword layer lost when Scenario B turned off. `AKB_Belt_v1_Spec.md` §4 has design for `/api/verify-listing v2 + Firecrawl + markdown parser` (~1-2 days work). Brief should cover: (1) `lib/firecrawl.ts` helper mirroring `lib/rentcast.ts` pattern; (2) `/api/verify-listing` v2 design with markdown parser; (3) keyword scoring rules per checklist 1.4-1.5; (4) DOM discrepancy detection per 1.6; (5) burn-rate guard mirroring `RENTCAST_MONTHLY_CAP`; (6) output fields landing on Property_Intel table (the 7 reserved fields in INV-022 Q2 schema: `Listing_Flip_Score`, `Listing_Flip_Bucket`, `Off_Market_Body_Text_Detected`, `Restriction_Risk_Level`, `DOM_Discrepancy_Days`, `Listing_Content_Source`, `Listing_Content_FetchedAt`); (7) consumer pattern for INV-023. Scoped to INV-022 v2.

29. **[INV-029] Pre-EMD DD Gate (Gate 5)** — SHIPPED 2026-05-25 (commit `bfe9995`, Spine `rec8xYpE1Mw46JvjP`). New diagnostic gate `pre_emd` in `lib/orchestrator` (registered in `/api/orchestrator/run-gate`): 7 blocking checks — CMA fresh (`arvValidatedAt` ≤7d), Buyer_Median present (Property_Intel), federation green (Property_Intel severity), Memphis assignment verified when TN, buyer-track MAO computed (`investorMao`), photos-vs-modeled Sturtevant variance ≤50%, operator EMD sign-off. Returns BLOCKED with itemized reasons; EMD button (Forge ship) reads it. New Listings_V1 checkboxes `Memphis_Assignment_Verified` (`fldZx12OwNJEXWbpQ`) + `EMD_Operator_Signoff` (`fldl8GIxjBS5KiMq4`). Backfilled to queue for numbering coherence.

30. **[INV-030] Price-reduction re-engagement detector ("Crier-2")** — Brief TBD. **Priority:** Medium. **Depends on:** RentCast listings adapter (shipped — `lib/crawler/sources/rentcast.ts` already exports the reusable `detectPriceReduction(history, currentPrice)` primitive). **Genesis (operator 2026-05-25):** distress-signal gate was deliberately removed from Crawler intake — first contact fires on every active band listing (the 65%-of-list script is the door-opener). Price drops are a SEPARATE downstream trigger: a cron that polls active pipeline records, compares current price + `listing.history` against last-seen, and fires a RE-ENGAGEMENT (not first contact) when a drop is detected — for records already scripted with no reply or an earlier "not interested." Re-engagement copy differs: "Saw the price drop — my cash offer still stands at $X, quick close. Worth a fresh look?" This is Crier round-2 behavior, NOT Crawler intake. Likely a separate `re-engagement-scan` / `crier-2` cron route consuming `detectPriceReduction`. Do NOT bake price-reduction logic into intake. Brief: TBD.

31. **[INV-031] Crawler Firecrawl-verify parallelization** — SHIPPED 2026-05-25 (commit `8beb057`, Spine `recQ0hA8Bs6R05kik`). Sequential Firecrawl verify (~7.5s/call) hit the 270s Vercel-Hobby wall-clock at ~56% ZIP coverage (Standard tier gives 50 concurrent browsers; ~2% utilization). `lib/crawler/async-pool.ts` (`runAsyncPool` bounded concurrency + `makeRateGate` global spacing) + cron restructured into collect → parallel-verify → classify → write phases. `FIRECRAWL_MAX_CONCURRENT` (default 20). Wall-clock guard retained as backstop. 8 pool tests.

## Discovered during prior investigations

- **[unnumbered] `scorePropertyMatch` has no unit-test coverage** — Discovered during INV-007 Step 1 remediation. `lib/timeline-merge.test.ts` does not exist. The scorer is now consumed by `mergeTimeline()` AND `/api/conversations/[id]` directly, plus three indirect surfaces (`/api/deal-context`, `/api/multi-listing-detect`, AMBIGUOUS banner). Coverage is overdue. Not a separate audit candidate — better tracked as a test-add ticket alongside whoever picks up INV-016 (since that's the one that requires re-running scorer scenarios).

## Discipline notes

- Investigations are DISCOVERY briefs by default — no fixes, no code changes, no field updates without operator authorization.
- Each investigation produces a written report at `docs/investigations/<topic>_Audit_v<N>.md`.
- Remediation, if authorized, is a separate work item logged in the originating audit report's "Remediation outcome" appendix.
- New findings during an investigation get appended to this queue's "Discovered during prior investigations" section before the investigation closes.

**Naming discipline:** When an investigation surfaces new findings during its work, Code adds them to "Discovered during prior investigations" using temporary local numbering. Operator promotes them to formal queue items with assigned INV-NNN numbers in the same commit as the next queue update. Prevents numbering collisions; queue file is single source of truth for INV identifiers.

**Backfill policy:** Pre-Inevitable Crawler 1.0 records (created by prior system iterations) are NOT backfilled by remediations. Forward-going behavior changes apply to records created after the remediation date. Historical records age out via normal deal lifecycle. Purging/archiving pre-Inevitable records is a separate operator decision flagged for future consideration.

**Crawler 1.0 dependency chain:** INV-005 (rehab affordance) → INV-022 (data federation) → INV-023 (Underwriter Agent) → INV-025 (Crawler Engine). INV-021 (4-consumer photo divergence) is parallel-track, can ship alongside any of these. INV-024 (DocuSign/Authentisign webhooks) is parallel-track. Decision Preconditions amendment ratification (Rule 1 + Rule 2 + Rule 3) should land before INV-023 to avoid building checklist refusal logic against unratified rules.
