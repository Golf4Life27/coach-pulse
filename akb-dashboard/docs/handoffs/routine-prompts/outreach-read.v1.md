# WS · MAVERICK 6 · Outreach volume read — prompt v1

Routine `trig_01Udd5hHsCeK34fKSUzVe1YD` ("WS · MAVERICK 6 · Outreach volume read · daily 8:35a CDT"), cron `35 13 * * *` (UTC), bound to session `WS · Routines · MAVERICK 6 · Ops check-ins` (session_01QXxez1aH8is8n5t6rwJot6). Committed 2026-09-25 as the first repo copy of the live prompt.

```text
Morning outreach volume read for AKB Solutions' CONVEYOR engine (repo Golf4Life27/coach-pulse, prod https://coach-pulse-ten.vercel.app). Goal: tell Alex in three lines what went out, what came back, and what cap or error is binding. Never change a cap, flag, env var, or routine.
STEP 0: mcp__Maverick__maverick_load_state. From its RECENT KEY DECISIONS take yesterday's "Engine drive ... 00Z" line (openers, bumps, replies for the prior day) and today's "Triage 13:00Z" line (new replies, Tier B sends, operator texts). Those spine titles are the source for yesterday's totals; the KV audit below only reaches back about 12 hours.
STEP 1: Audit reads. For each event name below, run the GitHub Actions workflow admin-get.yml on Golf4Life27/coach-pulse ref main with inputs path=/api/admin/audit-tail and query=event=<name>&limit=5000, wait for the run, read the job log (mcp__github__get_job_logs with return_content true; use tail_lines 400 and, only if the newest entry is cut off, a larger tail), then DELETE that run's logs with mcp__github__actions_run_trigger method delete_workflow_run_logs (the repo is public; logs carry agent phones and names). Events: h2_outreach_live (per slot: ts, eligible_count, first_touch_sent, errors, delivery_quarantined, send_cap.allowed, capped_by_reason.zip_not_covered, daily.used_at_start, quo_breaker.tripped), h2_bump_live (per slot bumps sent; slots 14:15/16:15/18:15/20:15Z), discovery_sweep_live (metro, zips_swept, examined, qualified, errors), h2_outreach_delivery_quarantine, h2_bump_delivery_quarantine, marked_off_market (counts only). Run the six dispatches together, then read them.
STEP 2: If quo_breaker is tripped or first_touch_sent is 0 on a slot that had eligible records, check Gmail for a Quo billing email (from:hello@quo.com after:<3 days ago>) and for system emails with subject containing "QUO" or "Send lane"; name the real cause (for example a declined card and zero prepaid credits) rather than a vendor wobble.
STEP 3: If something needs Alex (a declined card, a tripped breaker with a cause he must fix, a reply waiting that cannot be sent), check GET https://coach-pulse-ten.vercel.app/api/maverick/priorities for an existing card with a matching id; if none, post ONE card by running maverick-actions.yml (ref main) with input payload = {"upsert":[{"id":"<kebab-id>","title":"<plain sentence with the fix and the time it takes>","why":"<facts with times in Central>","instructions":"<exact clicks>","href":"<url or /path>","revenueUsd":null,"deadlineAt":"<RFC3339>","expiresAt":"<RFC3339, 3 days out>","postedAt":"<now RFC3339>","postedBy":"maverick-6"}]}, then delete that run's logs.
STEP 4: One spine write via mcp__Maverick__maverick_write_state (event_type build_event, title "Morning outreach read YYYY-MM-DD HH:MMZ: <openers> openers, <bumps> bumps, <sweeps> sweeps/<qualified> qualified, <errors>; binding: <cap or error>"), description with the per-slot numbers and the event names read.
REPORT: exactly three lines, plain language, no em-dashes: (1) openers plus bumps sent yesterday and this morning's first slot; (2) replies in and any reply waiting on us; (3) the cap or error binding right now, and what Alex must do if anything. Budget: 40 tool calls, no subagents, no code changes.
```
