# AKB INEVITABLE — MASTER CHECKLIST

**Document version:** v1.2 (Code-audited)
**Authored:** May 15, 2026 (v1.0 — Claude, post-Gate-3-closure)
**Audited + corrected:** May 16, 2026 (v1.1 — Code, Days 6-7 audit pass)
**Reconciled:** May 20, 2026 (v1.2 — Code, post-Map-1 Belt-era reconciliation; today's session shipped 3 remediations + 2 architectural specs + retired Scenario A/B/H2 in Belt v1 design; investigation-queue discipline codified as Phase 23)
**Status:** **THE** procedural source of truth. Every other doc is a spec. This is the sequence.
**Owner:** Alex Balog (decisions) / Code (build) / Claude (orchestration + drafting)

---

## Why this document exists

Alex has asked for a checklist countless times. Seven thousand documents have been produced. None of them are this. **This document is the single procedural list to follow until Inevitable is operational.**

## Rules of engagement

1. **Items are followed in order unless explicitly skipped with a logged reason.** No silent skipping.
2. **Every skip requires a reason recorded inline** in the `Skip Reason` column. Acceptable reasons: `OUTDATED`, `REPLACED-BY-[item#]`, `DEFERRED-UNTIL-[trigger]`, `BLOCKED-BY-[item#]`.
3. **New procedures may be inserted** with a clear `INSERTED [date] BECAUSE [reason]` note. Insertion does not justify skipping unrelated items.
4. **Skipped items must be revisited** unless marked `OUTDATED` or `REPLACED`. The `DEFERRED-UNTIL-[trigger]` status creates an active surveillance commitment.
5. **Status values:** `DONE` | `IN PROGRESS` | `BLOCKED` | `NOT STARTED` | `SKIPPED-OUTDATED` | `SKIPPED-REPLACED` | `SKIPPED-DEFERRED`
6. **Severity values:** `CRITICAL` (blocks deal flow or revenue) | `HIGH` (significant productivity multiplier) | `MEDIUM` (quality/discipline) | `LOW` (nice-to-have)
7. **This doc lives at `akb-dashboard/docs/specs/AKB_MASTER_CHECKLIST.md`.** Code's audited canonical version is here.
8. **Maverick loads this checklist** alongside the Continuity Layer Spec at session-start. Future Claude sessions read this first.
9. **Briefs that ship work which closes, retires, or supersedes existing checklist items MUST include a `Checklist update required: [item refs]` directive.** Code updates the checklist as part of the same commit cycle as the substantive work. (INSERTED 2026-05-20 BECAUSE today's session shipped 3 remediations + 2 architectural specs that retired Phase 1 Scenario B work without any contemporaneous checklist update; the checklist drifted out of sync within a single session. This rule prevents recurrence.)
10. **The investigation queue (`docs/investigations/Active_Queue.md`) is the live tracking surface for forensic investigations. The master checklist is the procedural narrative.** When the queue closes an investigation via remediation, the checklist absorbs the resolved item as a DONE entry. Both surfaces co-exist; neither replaces the other. (INSERTED 2026-05-20 BECAUSE the investigation queue emerged as a parallel-track discipline this session and needed an explicit relationship to the master checklist.)
11. **Code-audited status correction notation:** entries with `(CODE 5/16: ...)` indicate Code's Days 6-7 audit corrected the original draft based on repo / Airtable / Vercel / live-briefing evidence. Original status preserved when correction is a status flip; otherwise commentary in Notes. (5/20 audit uses `(CODE 5/20: ...)` notation.)

---

## Phase 0 — Foundation (HISTORICAL)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 0.1 | LLC formation (AKB Solutions LLC) | DONE | CRITICAL | Pre-2026 |
| 0.2 | Banking + accounting separation | DONE | CRITICAL | Pre-2026 |
| 0.3 | Quo phone number (815-556-9965) + carrier registration | DONE | CRITICAL | Carrier registration PAID and LIVE |
| 0.4 | Airtable base `appp8inLAGTg4qpEZ` with Listings_V1 table | DONE | CRITICAL | Primary table `tbldMjKBgPiq45Jjs` |
| 0.5 | Buyers table `tbl4Rr07vq0mTftZB` | DONE | CRITICAL | |
| 0.6 | D3 Manual Fix Queue `tblV6OkNPDzOo6ubp` | DONE | HIGH | |
| 0.7 | Spine_Decision_Log table for principle persistence | DONE | CRITICAL | Maverick writes here. (CODE 5/16: confirmed `tblbp91DB5szxsJpT` referenced from `lib/maverick/recall.ts:17`) |
| 0.8 | Vercel project `prj_X1pCuqzRml74iOKfNhTo4ZMG9K87` provisioned | DONE | CRITICAL | Team `team_zwFAlAQ8CyjGYcxyk7Sn6ww0` |
| 0.9 | GitHub repo `Golf4Life27/coach-pulse` with subdir `akb-dashboard/` | DONE | CRITICAL | Current branch: `claude/build-akb-inevitable-week1-uG6xD` |
| 0.10 | Vercel KV provisioned for audit log | DONE | CRITICAL | Used by Maverick state layer. (CODE 5/16: confirmed via live Maverick briefing — `vercel_kv_audit` source returned ok with 28 events) |

---

## Phase 1 — Intake & Pipeline Loading (HISTORICAL + ACTIVE)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 1.1 | PropStream account + export workflow | DONE (manual) | HIGH | Currently manual exports — Alex runs CSV pulls |
| 1.2 | Scenario A (Intake_Loader_V1, Make 4256273) | **SKIPPED-REPLACED** | CRITICAL | (CODE 5/20: Belt v1 Spec §4 retires Scenario A as `replace-with-Vercel-native`. Per Phase 20.1 emerging pattern — polled/scheduled scenarios migrate to Vercel-native. Replacement: Vercel `/api/intake` endpoint, Path 1 or 2 per Belt Spec §3.5, becomes Crawler 1.0 convergence point. Today's Scenario A blueprint push (`Listing_Condition` mapping add) is the LAST scheduled edit to A — preserves intake-quality-gates wiring intact during the Vercel-native rebuild handoff.) Original notes preserved: Filters Poor/Disrepair/Average, $3.5K-$250K, regex phone. Number fields use `{{if(value; value; ignore)}}` pattern in Modules 12/13 as of 5/1/2026. process-intake/route.ts already mirrors filters in Vercel. |
| 1.3 | Scenario B (Listing_Verification_V2, Make 4331170) | **SKIPPED-REPLACED** | HIGH | (CODE 5/20: Replaced by Belt v1 Spec §4 Verification station: `replace-with-Firecrawl-native`. Scenario B retired 2026-05-20. Replacement: Vercel `/api/verify-listing` v2 + Firecrawl + markdown parser. Original note preserved: `app/api/verify-listing/route.ts` is the Vercel-side companion that writes Execution_Path; Firecrawl validated end-to-end against 4838 Wisteria St Dallas TX 75211 on 5/20.) |
| 1.4 | Scenario B fix: Off-market body text detection | **SKIPPED-REPLACED** | HIGH | (CODE 5/20: Superseded by Belt v1 Spec Verification station retirement of Scenario B. Phase 1.4-1.7 obsolete. Code shipped via Commit Q.5 (`lib/intake/quality-gates.detectOffMarketLanguage` — 9 regex patterns + 4 tests) but the entire Scenario B path is retired; helper may be reused by Vercel `/api/verify-listing` v2 build — no immediate code removal.) Original note preserved: Pure helper; Sentry / process-intake routes consume via runIntakeGates(). |
| 1.5 | Scenario B fix: Flip/renovation keyword scoring | **SKIPPED-REPLACED** | HIGH | (CODE 5/20: Superseded by Belt v1 Spec Verification station retirement of Scenario B. Phase 1.4-1.7 obsolete. Code shipped via Commit Q.5 (`lib/intake/quality-gates.scoreFlipKeywords` — 14 indicator keywords, 4-6 → manual_review, 7+ → reject, 5 tests); helper may be reused by Vercel `/api/verify-listing` v2 build.) |
| 1.6 | Scenario B fix: DOM discrepancy check (PropStream vs. Redfin) | **SKIPPED-REPLACED** | MEDIUM | (CODE 5/20: Superseded by Belt v1 Spec Verification station retirement of Scenario B AND by Firecrawl validation 5/20 — Firecrawl scrapes Redfin natively with no anti-bot interference, eliminating the deferred-Redfin-adapter dependency entirely. Phase 1.6 obsolete.) |
| 1.7 | Scenario B fix: Agent_Phone numeric validation | **SKIPPED-REPLACED** | MEDIUM | (CODE 5/20: Superseded by Belt v1 Spec Verification station retirement of Scenario B. Phase 1.4-1.7 obsolete. Code shipped via Commit Q.5 (`lib/intake/quality-gates.validateAgentPhone` — format regex + 7-15 digit range, 5 tests); helper may be reused by Vercel `/api/verify-listing` v2 build.) |
| 1.8 | NEVER-list enforcement (12+ properties) | DONE | CRITICAL | Per userMemory |
| 1.9 | Dead-record auto-classification | DONE | HIGH | 1,093 records currently marked Dead. (CODE 5/16: `lib/bulk-dead-annotation.ts` + admin bulk-dead route confirm) |
| 1.10 | 890 records singleSelect/formula mismatch bulk cleanup | DEFERRED (Phase 21) | HIGH | Destructive operation touching active records — STOP condition #3 (revenue-impact destructive). Operator authorization required. Logged in Phase 21 backlog. Required before H2 fires safely at scale. |

---

## Phase 2 — Outreach (Crier) Layer (HISTORICAL + ACTIVE)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 2.1 | Quo API integration (`/v1/messages`) | DONE | CRITICAL | Raw API key in Authorization header, phone ID `PNLosBI6fh`. (CODE 5/16: `lib/quo.ts` 227 lines, no throttle layer present) |
| 2.2 | H2 (Quo_Outreach_V1, Make 4724197) | **SKIPPED-REPLACED** | CRITICAL | (CODE 5/20: Belt v1 Spec §4 retires Mode A H2 — `retire Mode A (Make H2 4724197 isinvalid). Belt uses Mode B (/api/outreach-fire) exclusively.` Same architectural pattern as Verification migration. H2 already carried `isinvalid: true` per System Inventory v2 audit; today's Belt v1 design formalizes the retirement. `/api/outreach-fire` is shipping and is the canonical outbound path. Original trigger preserved for historical reference: `AND(Execution_Path=Auto Proceed, Live_Status=Active, Outreach_Status empty, State=TX, NOT(Do_Not_Text), Agent_Phone not empty)`.) |
| 2.3 | Outreach script (canonical 3-sentence version) | DONE | CRITICAL | "Hi [First Name], this is Alex with AKB Solutions..." |
| 2.4 | 9 PM CT daily send cutoff | DONE | HIGH | No late-night texts |
| 2.5 | 128 TN records pre-blocked (Memphis acquisition pause) | DONE | CRITICAL | |
| 2.6 | L3 Reply Triage V3 (Make 4812756) | DONE | CRITICAL | 4-way Router architecture, working as of 4/21/2026. (CODE 5/16: KEEP per Phase 20.1 retirement plan — Negotiation Agent reads its output) |
| 2.7 | L2 deprecation | DONE | MEDIUM | Replaced by L3 |
| 2.8 | Quo throttle implementation (15/hour recommended) | **DONE** | HIGH | (CODE 5/19 Commit Q.6.) `lib/quo-throttle.ts` — classifyThrottle (pure) + getQuoThrottleStatus (KV-read I/O) + assertThrottleHeadroom (throws QuoThrottleError when at cap). Default 15/hr; QUO_THROTTLE_PER_HOUR env override. 6 tests. Caller-site wire-up (sendMessage + fire-blast wrap) left as a judgment call per call site — throttle-fail = hard-error vs queue is per-context. |
| 2.9 | Cadence_Queue async dispatch | DEFERRED (Phase 21 / Map 2) | HIGH | Vercel has no native queue substrate; needs external service (Inngest / QStash) OR Anthropic Routines (Map 2 substrate decision). Architectural fork — Stop condition #5. Logged in Phase 21 backlog with both options. |
| 2.10 | Make blueprint API gotcha: every push sets isActive: false | DOCUMENTED | MEDIUM | Manual UI toggle required every deploy. Workflow established. (CODE 5/20: Operator approved Make blueprint API pushes 2026-05-20 — supersedes prior "UI edits only" discipline. Code pushes scenario edits via Make MCP `scenarios_update` directly. **Reusable pattern documented in `docs/investigations/Listing_Condition_Audit_v1.md` §7:** strip UI-only metadata (`metadata.expect/interface/restore/samples`) before pushing to fit MCP tool-call budget; Make regenerates server-side on next visual editor open. `isActive: false` quirk requires capturing pre-push state and verifying post-push — operator-credit-conservation discipline preserved. First push exercised on Scenario A 4256273 this session.) |
| 2.11 | H2 daily cron scheduled | **DONE** | HIGH | (5/27 Workstream B — Spine recdFhfCh7xhy0mgp.) `/api/cron/h2-outreach?dry_run=false&limit=25&send_delay_ms=10000` wired into `vercel.json` at `30 15 * * *` (15:30 UTC = 10:30am Central, inside the TX working-hours gate; staggered off the 14:00 reconcile and 15:00 rehab-vision-retry slots per the Hobby once-per-day cap). Live sends remain gated by `H2_OUTREACH_LIVE` — the cron runs dry until the operator flips it. limit=25 saturates the 270s wall-clock budget (~21 sends/run at 10s pacing); remainder rolls to the next run. Kills the URL-in-browser dispatch failure mode. |
| 2.12 | H2 KV idempotency lock | **DONE** | CRITICAL | (5/27 Workstream B — Spine recWwIMc7V15p968k duplicate-fire incident.) Closes the cross-invocation race that double-texted 9 agents on launch day. `lib/maverick/oauth/kv.ts` gains a `setNx` (SET NX EX) atomic-acquire primitive. `app/api/cron/h2-outreach/route.ts` acquires a **run-mutex** (`h2:run:lock`, 300s TTL) so two overlapping live invocations can't both read the empty-status pool, plus a **per-record claim** (`h2:dispatch:<id>`, 24h TTL) taken BEFORE Quo dispatch. KV is strongly-consistent, so unlike the Airtable `Outreach_Status` gate it has no write-propagation lag. Claim is released on send failure (retry next run) but KEPT on send-success/status-write-failure (a re-text is the worse outcome). Degrades to the Airtable gate when KV is unconfigured. `idempotent_skipped` surfaces in the cron-run audit. |

---

## Phase 3 — Pricing & Math Discipline (HISTORICAL + ACTIVE)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 3.1 | 65% List Price outreach pricing | DONE | CRITICAL | Door-opener stage |
| 3.2 | V2.1 negotiation pricing (`Investor_MAO = Buyer_Median − Est_Rehab`) | DONE | CRITICAL | Per Spine 65% Rule + Offer Discipline. (5/18 rename: `Buyer_Tx_Median` → `Buyer_Median` — Tx prefix was Texas-only launch artifact; system is nationwide. No active code or Airtable formula referenced the old name — current Investor_MAO formula on Listings_V1 uses Real_ARV_Median (fldoNZxSZqQsCLIW6). This row was the only stale ref in the repo. INEVITABLE_Constitution_v3.docx + Make scenarios J/G/D may still carry the old token — flagged as Phase 12.8.) |
| 3.3 | <20 priced transactions = Manual Review gate | DONE | CRITICAL | Sentry enforces. (CODE 5/16: enforced in `lib/orchestrator/pre-send-checks.ts` family) |
| 3.4 | InvestorBase per-property CSV exports | DONE (manual) | HIGH | ~50/week bottleneck, no API yet |
| 3.5 | RentCast API integration (AS-IS value) | DONE | CRITICAL | Key in password manager, NOT for ARV. (CODE 5/16: live briefing shows `api_responsive: true`, monthly cap 1000, 16 days until reset, ~0 burn) |
| 3.6 | Buy Box Cartel buyer max offer reference | DONE | HIGH | Empirical anchoring, no universal multiplier |
| 3.7 | HARD RULE: no fabricated multipliers | DONE | CRITICAL | Locked after 4/26 80% MAO near-disaster |
| 3.8 | OfferPrice stickiness (no auto-revise down on seller moves) | DONE | CRITICAL | Per memory |
| 3.9 | Buyer-facing comms: show ONE number only | DONE | CRITICAL | Never disclose spread/fee/contract price |
| 3.10 | `stored_offer_price` field on Listings_V1 | **WRITES PARTIAL** | CRITICAL | (CODE 5/16: status corrected. Field IS written by `app/api/admin/d3-backfill-offer-fields/route.ts` admin one-shot route. NOT written on the live H2 outreach-fire path — that's the broken-discipline finding (Finding #9 / item 11.4). Live briefing's `active_deals` was empty tonight so could not validate in-flight values, but Phase 4 pricing route computes Your_MAO without persisting to Stored_Offer_Price.) |
| 3.11 | Phase 4 — Hyper-Local Math Layer | **DONE** | HIGH | (CODE 5/19 Q.3 closeout.) Parent row promoted to DONE — all sub-rows (3.11a Unified Deal Math endpoint, 3.11b Two-Track BroCard rendering, 4A.1 ARV, 4B.1 Rehab, 4C.1 Buyer Intelligence, 4D BroCard render) have shipped. Math layer is end-to-end: deterministic helpers (lib/appraiser/*), per-agent endpoints (app/api/agents/appraiser/*), unified read (app/api/deal-math/[recordId]), BroCard render with v1.3 range envelope (components/brocard/PricingBlock.tsx). |
| 3.11a | Phase 4D — Unified Deal Math endpoint (`/api/deal-math/[recordId]`) | **DONE** | HIGH | (CODE 5/19 Commit Q.3.) `app/api/deal-math/[recordId]/route.ts` — canonical-namespace read endpoint wrapping the v1.3 MAO range envelope. Returns { pricing: BroCardPricing (discriminated union), range: MaoRange | null, listing_snapshot: { ... } }. Coexists with per-agent appraiser endpoints (those write Airtable; this reads). Per-call audit row tagged "appraiser" / "deal_math_read". Per v1.3 amendment: range-shaped from day one so future motivation deltas don't require contract change. |
| 3.11b | Phase 4E — Two-Track BroCard rendering in dashboard | **DONE** | HIGH | (CODE 5/18 Commit L — landed under Alex's "Phase 4D" naming per the sprint brief; the checklist's "Phase 4E" label is preserved here to avoid clashing with row 3.11a's "Phase 4D — Unified Deal Math endpoint" which remains NOT STARTED. Same row, different naming convention.) Sub-commits: L.1 (`6638120`) BroCardPricing discriminated union (phase4 / legacy / no_math) on the BroCard interface + pure `classifyBroCardPricing` helper in `lib/brocard/pricing.ts` + jarvis-brief route preserves raw Listing on RankedDeal and attaches pricing post-LLM (fallback path too) + three-mode `components/brocard/PricingBlock.tsx` + wired into `CardBlock` in `JarvisGreeting.tsx` (the Daily UX Spec priority surface per Phase 9.x); L.2 (`0b229e0`) three-stop range bar (floor → soft-ceiling → list) with soft-ceiling caution color when target exceeds it + expand-on-click modifier-inputs panel surfacing ARV / Rehab (+ calibrated/legacy source badge) / Wholesale fee / Buyer profit / Rent / Cap rate + tier / State / Motivation + `cap_rate` + `cap_rate_tier` bubbled up onto the BroCard's `dual_track` shape; L.3 (`3f78bbf`) 25 pure-function tests across the four render-mode states with the 1219 E Highland canonical anchor + K.1/J.1 fixtures (Memphis/Detroit/SA flipper-no-rent). **Architecture:** frontend-only sprint — no new endpoints, no new Airtable fields. The math layer (Phase 4A.1 + 4B.1 + 4C.1) was already producing the v1.3 range envelope; this row closes the visual gap so the operator sees what the math layer knows. **Mode gate:** phase4 when `computeMaoRange` returns a non-null floor (covers landlord-only edge case where ARV is null but rent + state produce a floor via `dual_track.dominant_value`); legacy when math can't produce a floor but `outreachOfferPrice` OR `contractOfferPrice` is populated; no_math otherwise. **Cap-rate bubble:** `BroCardPricingPhase4.range.dual_track` carries `cap_rate` + `cap_rate_tier` (TX-Metro / TN-Distressed / MI-Distressed / Conservative-Default) so the modifier-inputs tooltip surfaces them without re-running `computeDualTrack` client-side. **Rehab-source badge:** the rehab `modifier_inputs.rehab_source` field discriminates Phase 4B.1 calibrated (`phase_4b_calibrated`, emerald badge) from legacy fallback (`legacy_est_rehab`, amber badge) — Pulse will eventually alert on stale legacy rates. **Validation anchor (locked in tests):** 1219 E Highland Blvd 78210 — ARV $165K + Rehab $60K + Rent $1400/mo + TX 8% cap → flipper $90K, landlord $135K (dominant), list $163K, soft ceiling $122,250, exceeds_soft_ceiling: true. 633/633 tests passing. **What's NOT in scope (deferred):** Phase 13 Sentinel auto-scoring of `Seller_Motivation_Score` will separate `target` from `floor`; today target = floor everywhere. Phase 14 Pulse self-monitoring will baseline the `rehab_source = legacy_est_rehab` rate across active pipeline. Held: L.4 (THIS commit — Checklist transitions only, no code). |
| 3.12 | Pricing Agent integration into outreach pre-fire | DONE | HIGH | (CODE 5/16: confirmed via `lib/orchestrator/pre-outreach-checks.ts` family + `outreach-safety-check` route) |

---

## Phase 4 — Buyer Network (Scout) Layer (HISTORICAL + ACTIVE)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 4.1 | Buyers table schema | DONE | CRITICAL | `tbl4Rr07vq0mTftZB` |
| 4.2 | Buyer warmup sequences (daily cron) | DONE | HIGH | LLM-driven, running. (CODE 5/16: `app/api/buyers/warmup-sequence/route.ts` exists; cron `/api/buyers/warmup-sequence` at 13:00 in `vercel.json`) |
| 4.3 | Buyer warmth states (cold/warm/active) | DONE | MEDIUM | (CODE 5/16: confirmed in `lib/buyers-v2.ts`) |
| 4.4 | Buy-box capture on inbound buyer replies | DONE | HIGH | |
| 4.5 | Automated buyer-deal matching | **DONE** | HIGH | (CODE 5/19 Commit Q.5.) Scout intelligence layer shipped. `lib/scout/queue.ts` — classifyDispoReadiness (gates Offer Accepted / PA Signed / Pending Assignment; scores 0-100) + buildDispoQueue + selectScoutPrioritySignals (filters to high-readiness ≥60 for briefing surface). `app/api/agents/scout/queue/route.ts` exposes the queue-level surface that wasn't there. 9 tests. Per-record match-to-deal endpoint coexists. |
| 4.6 | Dispo blast queue | **DONE** | HIGH | (CODE 5/19 Commit Q.5.) Queue-level surface live at /api/agents/scout/queue — lists active deals sorted by dispo-readiness with signals + warnings per row. Per-record fire-blast endpoint still drives the actual send. |
| 4.7 | Scenario G (Deal_Alert_Blast_V1, Make 4583609) hardening | DEFERRED (retirement path) | CRITICAL | G is scheduled for retirement per Phase 20.1 when Dispo Agent ships. Hardening dead code is wrong investment — Dispo Agent (Scout queue from 4.6 + per-record fire-blast) replaces G's responsibility. Operator action: when Dispo Agent has shipped enough sends to validate parity, retire G via Make MCP (5-min operation). |
| 4A.1 | Appraiser ARV endpoint (standalone) + v1.3 MAO range envelope | **DONE** | HIGH | (CODE 5/18 Commit I.) Sub-commits: I.1 (`9471dc7`) endpoint + helpers + lib mappings + 18 tests; I.2+I.3 (`e4e281b`) briefing aggregator wire (ListingsActiveDeal +5 ARV fields with freshness classification) + Appraiser room ARV coverage section + deal-detail ARV panel with comps table. **Architecture:** standalone GET `/api/agents/appraiser/arv/[recordId]` separate from the Pricing Agent's 4A/4B/4C composition — both coexist this sprint per Alex's "eventually retire K's write-back logic so Vercel owns all writes" framing. **Math:** `lib/appraiser/mao-range.ts` pure helpers — `classifyArvConfidenceByCount` (HIGH 5+, MED 3-4, LOW <3 → Manual Review per Phase 4A.1 spec, separate from `lib/arv-intelligence.ts`'s internal cluster-quality rubric which stays in audit) + `computeMaoRange` returning v1.3 envelope `{ floor, target, list_price, soft_ceiling, exceeds_soft_ceiling, modifier_inputs }`. Floor = MAX(arv_mid − est_rehab − wholesale_fee, 0). Target = floor in Phase 4A.1 — seller-motivation-modifier formula deferred to Phase 13 (Sentinel auto-fills `Seller_Motivation_Score`). Soft ceiling = 75% of List per spec. **Airtable writes:** Real_ARV_Low/High/Median, ARV_Confidence, ARV_Comp_Count, ARV_Comp_Avg_PrSqFt, ARV_Comp_Details_JSON (capped 95K chars), ARV_Validated_At. Skippable via `?skip_write=1`. **Validation anchor:** 1219 E Highland Blvd 78210 with ARV $165K + Rehab $60K + default Wholesale $15K → MAO floor $90K (locked in `lib/appraiser/mao-range.test.ts`). **UI surfaces:** Appraiser factory-floor room shows ARV coverage rollup across active deals (current / stale >30d / missing / LOW conf) with tier 1 override when any LOW-confidence deal exists; deal-detail page renders ARV panel with V2.1 floor + comp count + avg $/sqft + click-to-expand comps table + "Run ARV" / "Refresh" action + soft-ceiling caution flag. Held: I.4 wrap (THIS commit — Checklist transitions only, no code). |
| 4C.1 | Appraiser Buyer Intelligence Dual-Track endpoint | **DONE** | HIGH | (CODE 5/18 Commit K.) Sub-commits: K.1 (`956cb25`) endpoint + helpers + 26 tests; K.2 (`f416814`) briefing wire (ListingsActiveDeal +5 dual-track fields) + AppraiserRoom Track Mix rollup + deal-detail AppraiserBuyerIntelligencePanel; K.3 (`a84e93c`) MAO-range integration via computeMaoRange's new monthlyRent + state inputs. **Architecture:** standalone GET `/api/agents/appraiser/buyer-intelligence/[recordId]` computes both tracks (flipper V2.1 floor + landlord cap-rate-based MAO) and picks higher as dominant. Coexists with `lib/pricing-math.ts` (Pricing Agent's sophisticated dual-track w/ closing costs + vacancy + opex + buyer_profit). **Math:** `lib/appraiser/buyer-intelligence.ts` pure helpers — `getMarketCapRate` (env-overridable defaults: TX-Metro 8%, TN-Distressed 10%, MI-Distressed 9%, Conservative-Default 9%), `computeFlipperMao` (MAX(arv − rehab − wholesale_fee, 0)), `computeLandlordMao` (MAX((rent × 12) / cap_rate − rehab − wholesale_fee, 0)), `computeDualTrack` (returns {flipper_mao, landlord_mao, dominant_track, dominant_value, modifier_inputs}). Tie threshold $1000. **Airtable schema:** new `Estimated_Monthly_Rent` field on Listings_V1 (`fldrFB0owY6BnQewr`, currency, nullable). Endpoint writes RentCast AVM rent estimate on first run; subsequent runs use cached value unless `?force_rent=1`. **Validation fixtures (locked in tests):** SA flipper-dominant (high ARV + modest rent), Memphis landlord-dominant (canonical "money on the table"), Detroit landlord-strongly-dominant (Sturtevant creative-finance pattern), 1219 E Highland cross-check (SA + $1400 rent → landlord $135K beats flipper $90K by +50%), edge cases (no rent → flipper-only, no ARV → landlord-only, both null → neither, tie, both clamp to 0). **MAO-range integration (K.3):** `computeMaoRange` gains optional `monthlyRent` + `state` inputs. When both present, runs dual-track internally and uses `dominant_value` as floor; otherwise flipper-only fallback (backward compat — every prior mao-range test still passes). Output gains `dual_track` sub-payload + `modifier_inputs.monthly_rent` + `modifier_inputs.state`. **UI:** AppraiserRoom adds "Track mix" coverage section (Flipper / Landlord / Tie / No-inputs counts; landlord count highlighted emerald as the "money on the table" signal); AppraiserBuyerIntelligencePanel renders both tracks side-by-side with dominant tier-colored + ★ marker + creative-finance delta footer when landlord wins. 608/608 tests passing. |
| 4B.1 | Appraiser Rehab endpoint (standalone, BBC × market multiplier) | **DONE** | HIGH | (CODE 5/18 Commit J.) Sub-commits: J.1 (`a3243f6`) endpoint + helpers + 28 tests; J.2 (`a492e39`) briefing wire (ListingsActiveDeal +6 rehab fields with freshness + BBC tier) + Appraiser room rehab coverage section + deal-detail AppraiserRehabPanel; J.3 (`9f5f479`) MAO-range integration via `pickCalibratedRehab` helper. **Architecture:** standalone GET `/api/agents/appraiser/rehab/[recordId]` consumes raw vision output (via existing `callRehabVision` from `lib/rehab-calibration.ts`) and applies BBC 5-tier classification × per-market multiplier. **BBC anchors (Bible v3 §4.2):** Cosmetic $15, Light $22, Medium $30, Heavy $50, Gut $70 per sqft. **Market tiers (env-overridable):** TX-Metro ×1.00 (SA/Dallas/Houston), TN-Distressed ×0.90 (Memphis), MI-Distressed ×0.85 (Detroit), Conservative-Default ×1.10 (anywhere else). **Pure helpers** in `lib/appraiser/rehab-calibration.ts`: `marketTierForState`, `classifyBbcTierFromCondition` (vision Good→Cosmetic / Average→Light / Fair→Medium / Poor→Heavy / Disrepair→Gut), `classifyBbcTierFromRate` (midpoint thresholds 18.5 / 26 / 40 / 60), `applyMarketMultiplier`, `computeRehabRange`. **Airtable writes:** Est_Rehab + Est_Rehab_Low/Mid/High + Rehab_Confidence_Score + Rehab_Line_Items_JSON (structured: bbc_tier + market_tier + multiplier + anchor + calibrated rate + vision condition + line items, capped 95K) + Rehab_Red_Flags + Rehab_Estimated_At. **Validation fixtures (locked in tests):** SA 1500sqft Medium → $45K mid; Memphis 1500sqft Heavy → $67.5K mid; Detroit 1500sqft Gut → $89.25K mid; 1219 E Highland Blvd cross-check → $45K at Medium tier (vs Phase 4A.1's $60K anchor which used Heavy-ish estimate; both tier classifications tested). **MAO-range integration (J.3):** new `pickCalibratedRehab` helper prefers Phase 4B.1 calibrated `estRehabMid` over legacy `estRehab`. ARV endpoint outputSummary now records `rehab_source` so future Pulse can detect MAO floors still being computed from legacy data. **UI:** AppraiserRoom adds Rehab coverage section alongside ARV (current / stale / missing / Heavy-or-Gut counts) with tier 1 override when any Heavy/Gut deal exists; AppraiserRehabPanel mirrors AppraiserArvPanel with three states (computed → BBC tier + market metadata + red-flag chips + line-items table; pre-compute → "Run rehab" button; in flight → 15-30s expectation set). 576/576 tests passing. |
| 4D | BroCard Two-Track Rendering (frontend-only wire-up of v1.3 envelope) | **DONE** | HIGH | (CODE 5/18 Commit L.) See row 3.11b for the full audit — same row, cross-referenced here so the 4A.1 / 4B.1 / 4C.1 / 4D sub-phase track reads cleanly. Sub-commits: L.1 (`6638120`) structure + render modes; L.2 (`0b229e0`) three-stop range bar + modifier-inputs tooltip; L.3 (`3f78bbf`) 25 pure-function classifier tests; L.4 (THIS) Checklist wrap. The math layer was already producing the v1.3 envelope across 4A.1 + 4B.1 + 4C.1 — Phase 4D closes the visual gap so the BroCard surface (the primary deal-glance UI in `JarvisGreeting.CardBlock`, per Phase 9.x Daily UX Spec priority surface) reflects dominant track + soft ceiling + range envelope at glance. 633/633 tests. **Naming drift logged:** Alex's sprint brief calls this "Phase 4D"; the checklist's row 3.11b labels the same work as "Phase 4E" (placeholder from 5/16). The structural label stays Phase 4E (3.11b) to avoid clashing with the Phase 4D unified-endpoint row (3.11a, still NOT STARTED). |

---

## Phase 5 — Contract & Closing (Scribe) Layer (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 5.1 | DocuSign account + workflow established | DONE | CRITICAL | Receiving envelopes from sellers' agents |
| 5.2 | Manual contract review process | DONE | CRITICAL | Working today — tonight's 23 Fields review caught redline gap |
| 5.3 | Contract auto-draft (Buyer side) | DEFERRED (gated on 12.7) | HIGH | Gated on DocuSign JWT provisioning (Phase 12.7 — external). Once 12.7 lands, Scribe can draft buyer-side purchase contracts via DocuSign template + envelope-create. Code shape mirrors Phase 5 Commit G pattern. |
| 5.4 | DocuSign API integration | **DONE — live data pending DOCUSIGN_* env provisioning** | HIGH | (CODE 5/18 Commit G.) Phase 5 Scribe foundation shipped. **Correction:** the string `ab943441-29da-4bcb-8d3f-19efc0412d6c` referenced in `lib/orchestrator/pre-contract-checks.ts` and earlier checklist notes as the "DocuSign MCP server UUID" is actually Alex's DocuSign **account_id** (verified live via `getUserInfo`). The MCP server UUID is `e1ba5bca-4a68-49ef-8d49-3d9708fda8e8`. Conflation came from 5/13 announcement notes; nothing functional broke. **Architecture (Path A — JWT, chosen via decision question 5/18):** production talks to DocuSign REST API v2.1 directly via hand-rolled JWT bearer auth in `lib/docusign.ts`. No new npm deps (Node `crypto.createSign` RS256). DocuSign MCP is used only for Claude-side development inspection, not by production code. **Live calls require Alex to provision** `DOCUSIGN_INTEGRATION_KEY` + `DOCUSIGN_USER_ID` + `DOCUSIGN_PRIVATE_KEY` (Phase 12.7 below); until then, the new `lib/maverick/sources/external-docusign.ts` source returns `configured: false` cleanly and the Scribe surfaces render "Standing by — DocuSign credentials pending" without erroring. **22 client tests** cover JWT signing shape, envelope/recipient classifiers, summarizeEnvelope (pending recipient + awaiting hours + awaiting_is_alex), rollupEnvelopes (counts + max awaiting). `lib/orchestrator/pre-contract-checks.ts:79` `blocked_on: docusign_mcp_wire_in` resolves once Alex provisions JWT creds — pure data-layer unblock from this commit. |
| 5.9 | Scribe room on factory floor | **DONE** | HIGH | (CODE 5/18 Commit G.2 — `ee5acd5`.) `components/factory-floor/ScribeRoom.tsx` replaces the Phase 9.4 Standing-By stub. Reads DocuSign rollup from `BriefingProvider` (one state read, multiple views). Surfaces active count, awaiting-you count + max awaiting hours, signed-7d, voided/dead. Tier overrides per spec: >72h awaiting Alex → tier 3 (Phase 9.7 SMS-escalation eligible); >24h → tier 2. When `configured: false`, room renders "Standing by — DocuSign credentials pending (Phase 12.7)" — accurate empty state, not a fake. Click-through to DocuSign documents index URL when configured. |
| 5.10 | Scribe deal-detail commentary panel | **DONE** | HIGH | (CODE 5/18 Commit G.3.) `components/ScribeDealCommentary.tsx` on `app/pipeline/[id]/page.tsx` alongside MaverickDealCommentary. Three rendering states: (1) envelope tracked + found in briefing → status, awaiting recipient, last-action time, "Open in DocuSign" deep link, "Send reminder" button (only when awaiting a non-Alex recipient), Untrack affordance; (2) envelope tracked but outside the 30-day briefing window → GUID + window-explainer note + Untrack; (3) no envelope tracked → "Track in Scribe" input + Track button. **`/api/maverick/docusign-send-reminder/[envelopeId]`** (POST, dashboard-session auth) wires the reminder action through `sendReminder` from `lib/docusign.ts`; emits `agent=scribe event=envelope_reminder_sent`/`envelope_reminder_failed` audits. All envelope reads come from the shared `BriefingProvider` — no second briefing call path. |
| 5.11 | Listing↔Envelope attribution (Path A — explicit field) | **DONE** | HIGH | (CODE 5/18 Commit G.3.) Path A chosen over Path B (recipient/address heuristics) because GUID match is deterministic and Alex's link discipline beats fuzzy matching. **`Envelope_ID` field created via Airtable MCP** on Listings_V1 (`tbldMjKBgPiq45Jjs`, new field id `fldKPVG9qmbzxW5lK`, type singleLineText, nullable, description references this row). Wired into `LISTING_FIELDS` + `LISTING_NAME_MAP` in `lib/airtable.ts` and the `Listing` type in `lib/types.ts`; 4 test fixtures updated (Phase-11.2-style cascade). **`/api/maverick/track-envelope/[recordId]`** (POST, dashboard-session auth) writes via `updateListingRecord({Envelope_ID})` — accepts a GUID matching RFC 4122 hyphenated regex (paste-from-wrong-clipboard guard) or null (untrack). Emits `agent=scribe event=envelope_tracked`/`envelope_untracked`/`envelope_track_failed` audits. Forward-only — existing in-flight envelopes (e.g., 23 Fields) get backfilled manually by Alex clicking Track once per active deal. |
| 5.5 | EMD wire procedure documented | **DONE** | HIGH | (CODE 5/19 Commit Q.6.) `docs/specs/EMD_WIRE_PROCEDURE.md` — standing rules + per-deal playbook + red-flag list mirroring Sentinel's wire_fraud_red_flag intent categories + historical-fraud-patterns log. Cross-linked from MAVERICK_OPS "What lives where" table. |
| 5.6 | Inspection period management | DONE (manual) | HIGH | Never waived rule enforced |
| 5.7 | Assignment clause check pre-contract | DONE (manual) | CRITICAL | Memphis acquisition pause depends on this. (CODE 5/16: `lib/orchestrator/pre-contract-checks.ts` has inviolable items per spec §7) |
| 5.8 | Title company relationships | IN PROGRESS | HIGH | Regency Title (Memphis) established via 23 Fields |

---

## Phase 6 — Maverick Continuity Layer (HISTORICAL)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 6.1 | Continuity Layer Spec v1.0 authored | DONE | CRITICAL | 5/14/2026 |
| 6.2 | Spec v1.1 with 7 locked amendments | DONE | CRITICAL | Commit `3036126` Day 1 |
| 6.3 | Day 1 — 9 source fetchers | DONE | CRITICAL | git, airtable-listings, airtable-spine, vercel-kv-audit, codebase-metadata, action-queue, external-rentcast, external-quo, external-vercel |
| 6.4 | Day 2 — Aggregator + template + synthesizer | DONE | CRITICAL | Commit `a93a8d8` (CODE 5/16: corrected — Checklist draft said `2ea0161` which is actually the Airtable timeout-bump fix commit; Day 2 ship is `a93a8d8`) |
| 6.5 | Day 2 Gate 2 — P95 ≤ 30s confirmed at 19.6s | DONE | CRITICAL | |
| 6.6 | Day 3 — MCP server with `maverick_load_state` | DONE | CRITICAL | Commit `3248c2d` |
| 6.7 | Day 4 — `maverick_write_state` + `maverick_recall` | DONE | CRITICAL | Commit `366d456`, append-only per spec §6.4 |
| 6.8 | Day 4.5 — OAuth implementation | DONE | CRITICAL | Commit `15b6bfe`, Authorization Code + PKCE, Vercel KV opaque tokens |
| 6.9 | Spec v1.2 with §6.8 OAuth + §6.9 model registry amendments | DONE | CRITICAL | (CODE 5/16: numbering in spec is §6.8 OAuth + §6.9 model registry, NOT §6.5/§6.6 as in draft proposal — v1.1's §6.1-§6.7 were already taken. Implementation amendment §15 in MAVERICK_OAUTH_PROPOSAL.md documents this.) |
| 6.10 | Gate 3 closure — claude.ai connector registered, fresh chat invocation succeeded | DONE | CRITICAL | 5/15/2026 evening. (CODE 5/16: confirmed via current session — MCP server instructions for Maverick surfaced as system reminder, `mcp__Maverick__maverick_load_state` callable from THIS Code session — Maverick is reachable.) |
| 6.11 | Three Maverick spec docs (Character / Daily UX / Capability Absorption) | DONE | CRITICAL | Authored 5/15, delivered to Code via upload on 5/16 |
| 6.12 | Day 5 — Self-instrumentation + stress tests | DONE | CRITICAL | Commit `44f504e`, 397 tests passing |
| 6.13 | Gate 5 — P95 telemetry from production usage | **IN PROGRESS — FIRST SAMPLE OVER TARGET** | HIGH | (CODE 5/16: first telemetry sample landed tonight. Live briefing audit shows MCP `maverick_load_state` P50/P95/P99 all at 31,725ms — **over the 30s target** (`over_target_count: 1`). Cause: 3 Airtable sources timed out concurrently (listings 15s, spine 8s, action_queue 8s) — same contention pattern flagged Day 1. Single sample, not statistically meaningful. Re-evaluate after ~10 sessions. May need Phase 12.6 — concurrent Airtable contention mitigation — inserted below.) |
| 6.14 | Master Context userMemory edit (canonical session-bootstrap directive) | **DONE** | CRITICAL | (CODE 5/16: implied DONE — this Code session received the MCP server instructions for Maverick via system-reminder including the load-state directive. Either Alex pasted the userMemory or the project knowledge equivalent is wired. Directive lives in MAVERICK_OPS.md.) |

---

## Phase 7 — Maverick Spec Docs (HISTORICAL + DROP TO PROJECT)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 7.1 | `Maverick_Character_Spec_v1.md` | DONE | HIGH | German Shepherd identity, voice register, severity tiers, synthesis prompt instructions. (CODE 5/16: read in full this turn, archiving copy at `docs/specs/` in this commit.) |
| 7.2 | `Maverick_Daily_UX_Spec_v1.md` | DONE | CRITICAL | Visual command surface, factory-floor layout, agent rooms, persistent Shepherd panel, Stage 4 SMS escalation. (CODE 5/16: read in full this turn, archiving copy at `docs/specs/`.) |
| 7.3 | `Maverick_Capability_Absorption_Reference_v1.md` | DONE | HIGH | Matrix-upload analogy canonical, 5-phase absorption pattern. (CODE 5/16: read in full, archiving copy at `docs/specs/`.) |
| 7.4 | All three specs uploaded to Inevitable project knowledge | DONE | HIGH | (CODE 5/16: implied DONE per Alex's 5/15 confirmation. Cannot independently verify project-knowledge contents from Code sandbox, but the userMemory + connector setup from Phase 6.14 implies the project is fully provisioned.) |
| 7.5 | Master Context updated to reference all four spec docs | **DONE** | HIGH | (CODE 5/19 Commit Q.6.) `docs/specs/MAVERICK_OPS.md` "What lives where" table extended to reference Character Spec + Daily UX Spec + Capability Absorption Reference + EMD Wire Procedure. All four canonical spec docs cross-linked from one entry point. |

---

## Phase 8 — Code's Days 6-7 Audit (FORWARD, NOW HISTORICAL ON COMMIT)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 8.1 | Greenlight Days 6-7 audit to Code | DONE | HIGH | Alex's 5/16 message |
| 8.2 | Code reads three Maverick specs in full | DONE | HIGH | (CODE 5/16: read all three from `/tmp/akb-inputs/` after Alex provided via zip upload.) |
| 8.3 | `AKB_Dashboard_Current_State_v2.md` component audit produced | DONE | CRITICAL | (CODE 5/16: committed in this audit cycle. Gated on 20.1 resolution which is RESOLVED below.) |
| 8.4 | Intake pipeline current state included in audit | DONE | HIGH | (CODE 5/16: covered in System Inventory + Dashboard Current State.) |
| 8.5 | System inventory (`AKB_System_Inventory_v1.md`) | DONE | HIGH | (CODE 5/16: committed in this audit cycle.) |
| 8.6 | Resolve Phase 20.1 (Make migration question) before producing audit deliverables | DONE | CRITICAL | (CODE 5/16: RESOLVED. Per-scenario retirement plan in Bible §9.5 / Code Briefing §9. See Resolution Log at bottom for details.) |
| 8.7 | Sequencing recommendation for Day 8+ dashboard rework | DONE | HIGH | (CODE 5/16: embedded in this Checklist as Phase 9 revised sequencing notes.) |
| 8.8 | v1.2 findings #6-9 scheduled into appropriate phases | DONE | HIGH | (CODE 5/16: schedule below in Phase 11 — #6 → 8b Day-5 hardening, #7 → 11.2 Day 8+ Listings_V1 schema, #8 → Phase 5.4 unlock, #9 → 11.4 Phase 4 wiring fix) |

---

## Phase 9 — Dashboard Rework (Day 8+, FORWARD)

Sequencing locked per Code's audit (5/16) — items ordered by dependency + leverage. Daily UX Spec §10.3 lists 1-7 illustratively; this is the actual order.

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 9.1 | Shepherd panel (persistent Maverick presence on every page) | **DONE** | CRITICAL | (CODE 5/16 Commit B. `components/ShepherdPanel.tsx` rendered from `app/layout.tsx` so persistent across every route. Collapsed: floating pill bottom-right with tier-colored dot + 🐕 placeholder + status label. Expanded: 320px panel with MaverickPriority surface, refresh button, last-fetched timestamp. Subscribes to `/api/maverick/load-state?format=structured` on mount + auto-refresh 90s. Mobile: stacks above CommandBarFAB collision. German Shepherd avatar (9.9) is BLOCKED — placeholder 🐕 emoji holds until asset lands. |
| 9.2 | Priority surface BroCards wired to Maverick aggregator | **DONE** | CRITICAL | (CODE 5/16 Commit B. `components/MaverickPriority.tsx` renders signals inferred from load-state via `inferPrioritySignals()`. Each item: tier-colored left border + status dot + headline + reasoning + agent attribution. Optional href for click-through. Empty state: "All clear. Maverick is watching." Error state: failure message + retry. Lives inside Shepherd panel for v1; standalone embed possible. Existing card family (DDCard/DealCard/etc.) untouched — they remain deal-specific. Phase 9.4 will bridge per-deal signals into those cards.) |
| 9.3 | Named-agent vocabulary in code (`@agent: sentinel/crier/sentry/forge/scribe/scout/pulse/appraiser/ledger`) | **DONE** | HIGH | (CODE 5/16 audit insertion → 5/16 Commit A. 26 audit callsites renamed to roster names across 14 files. Final distribution: maverick 16, sentry 13, appraiser 9, crier 7, pulse 1. Zero operational tags remain. `lib/roster.ts` introduces ROSTER_AGENTS + ROSTER_DOMAINS as the canonical import path for non-Maverick code.) |
| 9.4 | Named-agent rooms on home page (Sentinel/Crier/Sentry/etc) | **DONE** | HIGH | (CODE 5/17 Commit C — 9.4a foundation + 9.4b rooms.) **9.4a (73f3b81):** `BriefingProvider` React context wraps the dashboard layout inside AuthGate, owns a single visibility-gated poll of `/api/maverick/load-state` (Phase 11.7 convention), shares state with all consumers via `useBriefing()`. ShepherdPanel refactored to a pure view of that context — no second briefing call path anywhere. Briefing schema extended with `audit_summary.recent_events: RecentAuditEvent[]` (cap 50) so rooms can render last-N activity per agent; synthesizer prompt strips this field via `buildRequestBody` to keep the Claude call at its prior token ceiling. **9.4b (2efc7cb):** nine rooms land on the home page above existing surfaces. Pipeline row in workflow order — Sentinel → Sentry → Appraiser → Crier. Support row — Scout (active) + Forge / Scribe / Ledger / Pulse (standing-by stubs). `lib/maverick/agent-room.ts` is the pure projection: tier inference (failures → tier 2, uncertain/success → tier 1, idle → tier 0), recent-events filter capped at 5, `formatRelativeTs` for "5m ago" labels. Room-specific signals elevate via `tierOverride`: Quo unresponsive lifts Crier to tier 2; manual-fix > 0 lifts Sentry to tier 1; RentCast burn ≤7d → Appraiser tier 1, ≤3d → tier 2. Click-through routes wired (Crier→/queue, Sentinel→/pipeline, Sentry→/queue, Appraiser→/pipeline, Scout→/buyers). Stubs accurately render "Standing by" + ship-phase note per spec rule "Empty rooms accurately communicate roster presence + agent inactivity"; the same StandingByRoom auto-upgrades to active rendering when events appear for that agent. 446/446 tests passing (+14 agent-room.ts coverage). Next.js build clean. Held: Phase 9.6 live motion (Commit D), 9.7 SMS escalation (blocked on 12.3), 9.8 deal-detail+recall (Commit D). |
| 9.5 | Severity tier visual treatment (Tier 0/1/2/3) | **DONE (minimum)** | HIGH | (CODE 5/16 Commit B. `lib/maverick/severity.ts` defines `TIER_VISUAL` (border + text + bg + dot + label) for all four tiers. `inferPrioritySignals(briefing)` classifies signals from load-state structured payload: source-down counts, Quo health, RentCast burn rate, MCP P95 over-target, recent_failures, open_decisions, active_deals. Signals sort tier-descending. 17 new tests in `severity.test.ts`. Tier 3 modal + SMS push (Daily UX Spec §5.4) deferred to 9.7 + 12.3.) |
| 9.6 | Live motion / animation (cards flowing between agent stations) | **DONE** | MEDIUM | (CODE 5/17 Commit D.2 — `da7744c`.) Subtle pulse animation on factory-floor rooms when an agent's `audit_summary.by_agent` count increases between briefing polls. Driven by `diffAgentActivity(prev, curr)` (pure helper in `lib/maverick/agent-room.ts`, +5 tests) — returns the set of agents whose count went up; decreases are ignored as window-rollover artifacts. BriefingProvider holds prev counts in a ref, computes the diff on each successful fetch, exposes `pulsedAgents: Set<string>` on context; 1.8s timer auto-clears. AgentRoom consumes the set, sets local `pulsing` boolean, removes after 1.6s (local timer prevents re-render restart). CSS `@keyframes maverick-agent-pulse` in `globals.css` — 2px upward translate + emerald glow ring, 1.6s ease-out. Animation lives entirely inside `@media (prefers-reduced-motion: no-preference)` — reduced-motion users see the same DOM with the pulse class but no rendering, no fallback class swap needed. Subtle by design; only fires on real state transitions, never decorative. |
| 9.7 | Out-of-band SMS escalation (Stage 4 → Alex's phone) | **DONE (A2P pending at carrier)** | HIGH | (CODE 5/18 Commit E.) Phase 12.3 unblocked this — Alex's personal escalation number `+16302505865` is live in the Quo workspace; A2P 10DLC registration is pending at the carrier (1-7 days typical), so outbound SMS will queue at Quo and hold at the carrier until A2P clears. No code changes needed when A2P clears; alerts simply start delivering. **Architecture:** `lib/maverick/sms-escalation.ts` is the orchestrator — pure helpers (`deriveSignalKey`, `formatStage4Message`, `parseDailySends`, `pruneRecentSends`, `tierThreeSignalsFrom`) + DI'd `evaluateStage4Escalation`. Trigger surface: synchronous after-briefing hook fired only when `authKind === "dashboard_session"` OR `"oauth"` — wired into both `app/api/maverick/load-state/route.ts` (dashboard fetches) and `app/api/maverick/mcp/route.ts` via a new `HandlerDeps.onLoadStateBriefing` callback (MCP OAuth tools/call). Cron + bearer_dev + none are no-ops inside the evaluator (defense-in-depth with the existing Phase 11.6 cron gate). **No new cron-fired endpoints**, no polling — runs on user-triggered briefing refreshes only. **Dedup:** per-signal-key KV entry (`mav:sms:signal:<key>`) with TTL = cooldown minutes; same signal across consecutive briefings → at most one SMS. Default 30-min cooldown, env override via `MAVERICK_SMS_PER_SIGNAL_COOLDOWN_MIN`. **Rolling cap:** JSON-encoded send-timestamp list at `mav:sms:daily:sends` with 24h TTL; `pruneRecentSends` filters to the rolling 24h window on each evaluation. Default cap 5 SMS / 24h, env override via `MAVERICK_SMS_DAILY_CAP`. **Suppressions:** every cooldown-hit or cap-hit emits `agent=maverick event=sms_rate_limited` audit with `inputSummary.reason ∈ {cooldown, daily_cap}` — Phase 14 Pulse breadcrumb pattern. **Sends:** `agent=crier event=sms_escalation_sent` with `externalId=quo_message_id` for the future delivery-verification loop. **Failures:** `agent=crier event=sms_escalation_failed` with full error context; Quo API exceptions never bubble to the load-state response; KV cooldown is NOT written on failure so the next briefing retries cleanly. **Quo integration:** reuses existing `sendMessageWithId(to, content)` in `lib/quo.ts` — sender is `QUO_PHONE_ID` (Crier outbound number), recipient is `MAVERICK_STAGE4_SMS_TARGET` (default `+16302505865`). **Message shape (per spec):** `"🐕 Maverick — TIER 3\n<headline>\n<reason>\n@AGENT"`. 26 tests covering auth gating, tier filtering, dedup, cap, failure handling, audit telemetry. Total suite 492/492. **A2P delivery status:** Quo dashboard exposes registration progress — Alex monitors carrier-side approval; until it clears, alerts queue at Quo (visible in workspace inbox) but don't fan out to the carrier. |
| 9.8 | Deal-detail page enhancements (Maverick commentary, related-deal recall) | **DONE** | HIGH | (CODE 5/17 Commit D.1 — `9e85757`.) Anchored to 251 Cliffwood reference (and any deal). **Commentary panel:** `lib/maverick/deal-commentary.ts` is a pure deterministic projection (+15 tests) — filters briefing's `recent_events` by `recordId`; layers Crier-silence rules (tier 2 at ≥14d, tier 1 at 7-13d for Negotiating/Response Received deals); surfaces failures attributed to the deal (cap 2); emits a tier 1 "recent activity" attestation when only successes exist. `components/MaverickDealCommentary.tsx` reads the shared `BriefingProvider` context (no new fetch), collapsed-by-default card with severity-tier border, click-to-expand reasoning, Maverick avatar + voice headline. Empty state renders "Maverick is watching this deal" — never fabricates content. **Recall panel:** `app/api/maverick/recall/route.ts` is a thin HTTP wrapper over the existing `lib/maverick/recall.ts` (same auth model as load-state — dashboard session first, OAuth waterfall fallback, Phase 11.6 cron gate mirrored). `components/RelatedDealsRecall.tsx` is **user-triggered only** (no auto-fetch, no polling — deliberate Phase 11.6/11.7 posture). Default query: listing agent name; falls back to address; editable. Hits spine + audit + listings sources. Click-through navigation when the result is a listings/deals record. Both panels wired into `app/pipeline/[id]/page.tsx` (commentary above Jarvis context strip; recall at bottom of left column). |
| 9.9 | German Shepherd avatar | **DONE** | MEDIUM | (CODE 5/16: Alex provided the canonical asset — a photo of the real-life Maverick (German Shepherd, the dog Maverick the AI is named after per Character Spec §2). Cropped + resized via sharp from `public/IMG_5337.jpeg` (3024×4032 iPhone source) → square 2782×2782 centered on the head → resized to 256×256 (`public/maverick-avatar.webp`) + 512×512 retina (`public/maverick-avatar@2x.webp`). Wired into `components/ShepherdPanel.tsx` replacing the 🐕 emoji placeholder in both collapsed pill (24×24) + expanded header (28×28). Severity tier dot now overlays the avatar's upper-right corner. Per Character Spec §6.1: "alert but not aggressive. Ears up, eyes on the perimeter, body relaxed" — this image lands exactly there.) |
| 9.10 | Auto-allow `maverick_load_state` permission (currently "Needs approval") | DEFERRED (external) | LOW | Claude.ai connector permission UI — operator-side, not code. Alex toggles in his Claude.ai connector settings when ready. STOP condition #4 (external account work). |
| 9.11 | Jarvis→Maverick rename of components | **DONE** | MEDIUM | (CODE 5/16 audit insertion → 5/16 Commit A; corrected in Commit B.1 — missed the JarvisGreeting header `<h2>Jarvis · Act Now</h2>` (line 313); now "Maverick · Act Now". LLM self-identity strings updated: `lib/jarvis-system-prompt.ts` "You are Jarvis" → "You are Maverick" ×4. User-visible UI strings updated in JarvisChat + JarvisFeed + JarvisGreeting header. @deprecated JSDoc added to 3 components + 4 jarvis-* API routes pointing to Phase 9.1 / 9.2 / 10 replacements. File paths retained for backwards-compatible imports — actual file rename + deletion happens when Shepherd panel (9.1) + priority surface (9.2) supersede.) |

---

## Phase 10 — Synthesizer Refactor (Day 8+, FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 10.1 | Refactor `lib/maverick/synthesize.ts` to use Character Spec anchored prompt | **DONE** | HIGH | (CODE 5/19 Commit P.) Per Character Spec §7. synthesize.ts now routes through lib/maverick/synthesizer with agent="briefing", which resolves to MODEL_SONNET_4_6 and carries the Owner's Rep voice-fragment in voice-registry (locked by snapshot test). The system prompt itself (SYSTEM_PROMPT constant) is unchanged — refactor charter held — but the model + voice character are now drift-detectable. |
| 10.2 | Build `lib/maverick/model-registry.ts` per Spec v1.2 §6.9 | **DONE** | HIGH | (CODE 5/19 Commit P.1.) Shipped as `lib/maverick/voice-registry.ts` — broader scope than just model tiers (voice fragment + max_tokens + temperature per agent). 13 entries covering every roster agent (active + disabled). MODEL_SONNET_4_6 / MODEL_SONNET_4_5 / MODEL_SONNET_4_LEGACY / MODEL_HAIKU_4_5 constants exported for cross-file reference. getVoiceEntry throws on disabled entries — surfaced by Pulse's voice_drift detector when migration mistakes hit a disabled entry. |
| 10.3 | Replace hardcoded `claude-sonnet-4-6` with `selectModel("premium_frontier")` | **DONE** | HIGH | (CODE 5/19 Commit P.2.) Migration pass: every direct anthropic.com/v1/messages fetch across the codebase (10 call sites) now routes through `synthesize()`. Zero literal model strings remain at call sites — only registry constant re-exports for back-compat with deprecated metadata-return fields. Pre-migration drift surface: 3 different Sonnet versions in production (4.6 / 4.5 / 4-legacy) + Haiku 4.5. Maverick currently rides Sonnet-4-legacy (claude-sonnet-4-20250514) — bumping to Opus 4.7 is a one-line change to MODEL_SONNET_4_LEGACY constant when the operator decides to switch tiers. |
| 10.4 | Bump synthesis budget to 30s (per latency tradeoff acceptance) | **DONE** | HIGH | (CODE 5/19 Commit Q.1.) `lib/maverick/synthesize.DEFAULT_TIMEOUT_MS` 20s → 30s, within v1.2 §6.2 P95 ≤ 30s ceiling. Active_deals trim (10.5) landed alongside so the bumped budget doesn't cost prompt tokens. |
| 10.5 | Trim `active_deals` to top 15 before synthesis (v1.2 backlog item #3) | **DONE** | MEDIUM | (CODE 5/19 Commit Q.1.) `ACTIVE_DEALS_PROMPT_CAP = 15` in `lib/maverick/synthesize.ts.buildRequestBody`. Aggregator's source-side sorting already puts most-recent / highest-tier first; tail-end trimming preserves what matters to the synthesizer. Cuts prompt tokens by ~60% on pipelines with 40+ active deals. |
| 10.6 | Prompt-cache audit (Constitution + agent roster + Character Spec) | **DONE** | MEDIUM | (CODE 5/19 Commit Q.1.) cache_system: true now set on Maverick BroCard synthesizer (jarvis-brief), Sentinel classifier, Sentinel drafter — all three have stable system prompts that benefit from Anthropic's ephemeral prompt cache (~10% cost per token vs full input). Briefing narrative already used cache_system from Phase 10 P.2. |
| 10.7 | Voice consistency snapshot tests + Pulse voice_drift detector + daily Pulse cron | **DONE** | HIGH | (CODE 5/19 Commit P.3 + P.4.) Voice consistency: `lib/maverick/voice-registry.test.ts` snapshots model + voice fragment per agent with 29 tests; future fragment change triggers test failure (the brief's "future drift = test failure" mandate). Duplicate-fragment invariant catches the "Crier sounding like Appraiser" drift class. Pulse voice_drift detector (`lib/pulse/detectors/voice-drift.ts`) reads synthesizer audit events and surfaces three drift classes: (1) model_fallback when Anthropic serves a non-registry model (model_matches_registry: false in outputSummary; warning at ≥1, critical at ≥5 in 24h, env-overridable), (2) disabled_agent_invoked when a migration mistake hits a disabled registry entry (critical, no threshold), (3) missing_registry_entry when an audit event references an agent not in VOICE_REGISTRY (critical, defensive). 17 detector tests + cron entry added to vercel.json (`/api/agents/pulse/scan` at `0 12 * * *` — noon UTC, no collision with existing slots 8/9/10/11/13). |

---

## Phase 11 — v1.2 Findings From Gate 3 Real-World Usage (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 11.1 | Finding #6: `external_quo` quiet-vs-down conflation fix | **DONE** | MEDIUM | (CODE 5/16: shipped in this sprint. `lib/maverick/sources/external-quo.ts` now runs probe + activity in parallel via Promise.allSettled. Probe (`/v1/messages?phoneNumberId=X&maxResults=1`) drives `api_responsive`; activity call drives stats. Probe success + activity failure → api_responsive stays true with zero stats; probe failure → api_responsive false. Schema drift on activity query no longer collapses to "Quo is dark.") |
| 11.2 | Finding #7: `last_outreach_date` SMS-only — add `last_email_outreach_date` field | **DONE (with documented attribution gap)** | HIGH | (CODE 5/18 Commit F.) Originally surfaced after the 23 Fields negotiation with Candice Hardaway false-staled while email exchange was active. **Schema:** `Last_Email_Outreach_Date` field created via Airtable MCP on Listings_V1 (`tbldMjKBgPiq45Jjs`, new field id `fld4Jzjs8etKact6g`, type dateTime, ISO format, America/Chicago tz, nullable). Wired into `LISTING_FIELDS` + `LISTING_NAME_MAP` in `lib/airtable.ts` and the `Listing` type in `lib/types.ts`. **Write side:** `SendOpts` in `lib/gmail.ts` gains an optional `listingRecordId?: string`. When set AND the live send confirms successful (not draft, not failed), `sendEmail` calls `updateListingRecord(recordId, {Last_Email_Outreach_Date: nowIso})` and emits `agent=crier event=last_email_outreach_written` audit (failures audited as `last_email_outreach_write_failed` but never bubble to the caller — the email already sent). **Read side:** `lib/maverick/deal-commentary.ts` gains pure helper `latestContactIso(listing)` that returns the actual max() across all four contact timestamps (`lastInboundAt`, `lastOutboundAt`, `lastOutreachDate` SMS, `lastEmailOutreachDate`). Replaces the prior fallback-chain (`a ?? b ?? c`) which could pick an OLDER timestamp when a newer one existed on a different field — the root mechanism of the 23 Fields false-stale. `DealCommentaryListing` extended with the new field; `inferDealCommentary` updated to use `latestContactIso` for the silence-detection branch. Deal-detail page wired to pass the new field through. **Tests (9 new, 501/501 total):** SMS-only / email-only / mixed-SMS-newer / mixed-email-newer (23 Fields canonical) / inbound-newest / all-null / unparseable-tolerated + dedicated `inferDealCommentary` regressions confirming the 23 Fields scenario no longer surfaces as tier-2 and the all-old version still does. **Attribution gap (forward-only writes per sprint scope):** the existing five `sendEmail` call sites are all buyer-side (`warmup-sequence`, `intake`, two `fire-blast` paths) and have no listing recordId in scope — they don't write the new field, which is correct (these emails aren't attributable to listings). The deal-detail page's "Send Formal Offer Email" button is a `mailto:` link that opens Gmail externally; Alex's manual sends from there cannot write back. New listing-attributable programmatic sends (Scribe contract dispatch when Phase 5 ships, future Crier email cadence, etc.) will Just Work by passing `listingRecordId`. Inbound-reply ingestion is also Phase 13/Sentinel territory — not in scope here. |
| 11.3 | Finding #8: Scribe must read DocuSign API directly (not PDF exports) | **DONE** | HIGH | (CODE 5/19 Commit Q.8.) Already shipped via Phase 5 Commit G — Scribe reads DocuSign via Path A JWT (lib/docusign.ts) directly against the REST API. No PDF-export dependency. Status promoted from NOT STARTED → DONE as part of the autonomy-run closeout; the underlying code has been in place since Phase 5 ship. |
| 11.4 | Finding #9: `stored_offer_price` universally null — V2.1 pricing discipline broken | **DONE** | HIGH | (CODE 5/16: shipped in this sprint. Two write paths wired: (a) `app/api/agents/pricing/[recordId]/route.ts` persists `Stored_Offer_Price: your_mao_flipper` when phase4c succeeds + value > 0; (b) `app/api/outreach-fire/route.ts` (both new-outreach + multi-listing handlers) writes `Stored_Offer_Price: offerNum` + `List_Price_At_Send: listing.listPrice` on send success, mirroring d3-backfill semantics with data_source="live_send". Surfaces in next pricing-agent run + next H2 send. Open sub-question logged as Phase 20.2.) |
| 11.5 | Make blueprint API: "right()" doesn't exist | DOCUMENTED | LOW | Use substring instead |
| 11.6 | Cron-burn safeguard on `/api/maverick/load-state` + MCP route | **DONE** | CRITICAL | INSERTED 5/17 BECAUSE 48hr unattended Sat-Sun window burned ~4.8M tokens / ~$15-30 of Anthropic credits via paired aggregator (1.5K) + synthesizer (8.2K) calls every ~6 min. (CODE 5/17 cron inventory: `vercel.json` contains 5 crons, all DAILY (`0 X * * *` once-per-day) — none hit `/api/maverick/load-state`. Vercel Hobby plan hard-caps crons at once-per-day per `AGENTS.md` (`0 */6 * * *` rejected at deploy on commit `2e0d054`), so a 6-min Vercel cron literally cannot exist in this repo. Daily crons left intact: `propose-actions` 9am, `scan-comms` 10am, `scan-replies` 11am, `warmup-sequence` 1pm, `recompute-agent-prior-counts` 8am — all part of working pipeline machinery, none caused the burn. **Most likely cause:** backgrounded dashboard tab where the 90s `ShepherdPanel.tsx` setInterval got browser-throttled to ~6min cadence (Chromium clamps background timers but does not pause them). Also-possible: external uptime monitor, persistent claude.ai/loop keepalive, cold-start cache misses. **Commit B.2 (446aa90):** `MAVERICK_CRON_ENABLED` env var (default false) gates `auth.kind==="cron"` callers in both load-state (503 + `load_state_cron_gated`) and MCP (503 + `mcp_cron_gated`). Phase 14 breadcrumb: `pulse_event:"non_user_synthesis"` console.warn on fresh synthesis from non-`dashboard_session` caller. **Commit B.3 (THIS):** ShepherdPanel `setInterval` now visibility-gated via `lib/maverick/visibility-polling.ts` — the architectural fix that closes the actual failure mode (backgrounded tabs auth as `dashboard_session`, so the cron gate would not have caught them). Promoted to DONE. |
| 11.7 | Browser polling visibility-state convention | **DONE** | HIGH | INSERTED 5/17 BECAUSE Phase 11.6 root-cause analysis showed the cron gate alone wasn't enough — backgrounded dashboard tabs authenticate as `dashboard_session` and would burn tokens through any number of cron safeguards. Convention spec'd in `Maverick_Daily_UX_Spec_v1.md §10.5`: any client `setInterval`/polling loop that triggers a server call must gate the call on `document.visibilityState === "visible"`. Canonical helper: `lib/maverick/visibility-polling.ts` (`startVisibilityGatedPolling`). Pure-function, unit-tested without jsdom, drop-in for any client polling effect. Mandatory for Phase 9.4 (agent rooms), 9.6 (live motion), 9.8 (deal detail) and any future polling client surface. Enforce in code review for Phase 9.4+. |

---

## Phase 12 — Infrastructure Provisioning Gaps (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 12.1 | Vercel API token (`VERCEL_API_TOKEN` env var) | STOP (external) | MEDIUM | Operator provisions in Vercel dashboard. Deploy state stays blind in briefings until env is set. STOP condition #4 — surfaced in Map 1 closeout. |
| 12.2 | GitHub Personal Access Token (`GITHUB_PAT`) | STOP (external) | MEDIUM | Operator provisions in GitHub Settings → Developer settings → Personal access tokens (classic, repo scope). Git source returns empty without it. STOP condition #4 — surfaced in Map 1 closeout. |
| 12.3 | Personal phone escalation channel (Stage 4 SMS path) | **DONE — A2P 10DLC pending at carrier** | HIGH | (5/18) Alex provisioned `+16302505865` as Maverick's personal escalation number in the Quo workspace; live as of today. A2P 10DLC registration is pending at the carrier (1-7 day window typical, carrier-dependent). Until A2P clears, outbound from this path will be accepted by Quo (HTTP 202) but held at the carrier — alerts visible in the Quo workspace inbox but not fanned out to Alex's phone. No code changes needed when registration clears; delivery starts flowing automatically. Phase 9.7 SMS escalation now built against this number (`MAVERICK_STAGE4_SMS_TARGET` env, default `+16302505865`). |
| 12.4 | Scenario J fix — manual Make UI edit (HTTP module empty body) | **OBSOLETE — J deleted** | MEDIUM | (5/18 cleanup sprint) J (4724499) deleted via Make MCP after Alex rotated the exposed Anthropic key. Replaced by `/api/verify-listing` in the dashboard per the Phase 20.1 retirement plan. |
| 12.5 | Anthropic Console organization-level API spend monitoring | STOP (external) | MEDIUM | Anthropic Console-level spend dashboard is operator-facing; no API for org-level burn export. Pulse's token-burn detector (Phase 14 O.1) gives the runtime estimate; Anthropic Console is the canonical billing source — operator checks weekly. STOP condition #4 surfaced in Map 1 closeout. |
| 12.8 | INEVITABLE_Constitution_v3.docx + Make scenarios — manual `Buyer_Tx_Median` → `Buyer_Median` token cleanup | **CLEAR — rename audit done by Alex** | LOW | INSERTED 5/18 with Phase 20.2 v1.3 amendment. Alex's 5/18 Make audit confirmed zero `Buyer_Tx_Median` references in any Make scenario. Constitution.docx manual rename remains Alex's task (project knowledge file, not in repo). Original concern (Make scenarios + Constitution.docx carrying stale token) is closed for Make; docx is the only outstanding piece. |
| 12.9 | Make blueprint secret exposures — H2 OpenPhone + Scenario I ScraperAPI hardcoded keys | STOP (external) | MEDIUM | Operator rotates: (a) ScraperAPI key in console → update Scenario I module 3; (b) OpenPhone key in console → update H2 module 2. Same procedure as the 5/18 Anthropic rotation. STOP condition #4. |
| 12.7 | DocuSign JWT integration credentials | STOP (external — UNLOCKS SCRIBE LIVE) | HIGH | **Revenue-unlocking — top priority of external account work.** ~15-20 min in DocuSign Admin Console: (1) Apps & Keys → Add App; (2) generate RSA keypair, save private key; (3) grant impersonation for Alex's user; (4) note integration key (UUID); (5) set Vercel env: DOCUSIGN_INTEGRATION_KEY (UUID), DOCUSIGN_USER_ID (Alex's `8999bf2c-e9d0-41b4-9625-9730e321d2db` per getUserInfo), DOCUSIGN_PRIVATE_KEY (full PEM with BEGIN/END markers). Account ID + base URI defaulted in code. Once env lands, briefing's `external_signals.docusign.configured` flips true on next refresh, Scribe room lights up, deal-detail panel surfaces live envelope state. **23 Fields gets tracked the moment Alex provisions.** STOP condition #4. |
| 12.6 | Airtable concurrent-source contention mitigation | **DONE-PENDING-VALIDATION** | HIGH | (CODE 5/16: shipped in this sprint with mitigation (a) — serialize the 3 Airtable calls (listings → spine → queue) within the aggregator while keeping the other 6 sources parallel. `lib/maverick/aggregator.ts` chains via `.then()` so spine waits for listings to resolve before starting, queue waits for spine. Test `aggregator-stress.test.ts > serializes the 3 Airtable fetchers` locks the behavior. Validation pending: Gate 5 telemetry over next ~5 sessions should show P95 < 30s in `audit_summary.mcp_call_latency.p95_ms`. If telemetry stays over target, layer in mitigation (b): raise listings to 20s + spine/queue to 10s.) |

---

## Phase 13 — Sentinel Build (Funnel Autonomy) (FORWARD, MAJOR GAP)

**INSERTED 5/15 BECAUSE Alex's "what's directing the Crawler" question revealed Sentinel as vocabulary, not reality.**

(CODE 5/16: confirmed Sentinel is vocabulary-only — zero `@agent: sentinel` matches in code. The intake routes (`/api/process-intake`, `/api/verify-listing`, `/api/multi-listing-detect`) implement Sentinel's behavior but lack the named identity. Phase 9.3 — named-agent vocabulary in code — must precede this Phase's deliverables to maintain attribution coherence.)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 13.1 | Sentinel-as-briefing-routine (Path D from session) | **DONE** | HIGH | (CODE 5/19 Commit Q.3.) Pulse intake-signal detector surfaces "N listings in Multi-Listing Queued" + "Active inventory low" via the priority-signal path. Maverick's load-state synthesizer + briefing surface both consume the Pulse section. No autonomous action — surfaces only. |
| 13.2 | Sentinel inventory-low triggers | **DONE** | HIGH | (CODE 5/19 Commit Q.3.) Same `lib/pulse/detectors/intake-signal.ts` covers — counts active-pipeline listings; warning ≤15, critical ≤5; env-overridable. Skips when listings array is empty (cron-cycle-silent detector handles no-data separately). |
| 13.3 | PropStream export automation | **DONE** (scaffold) | HIGH | (CODE 5/19 Commit Q.7.) `lib/crawler/sources/propstream.ts` — adapter scaffold; live integration unlocks when PROPSTREAM_API_KEY env is set. Inline wire-up checklist in adapter header. Phase 21 backlog captures the credential-provisioning step. |
| 13.4 | Geographic expansion logic | DEFERRED (Phase 21) | HIGH | "Sentinel suggests Beaumont because signals are strong" — needs market-signal data infrastructure (PropStream activity-by-zip, comp-density per market, response-rate per ZIP3) before scoring is meaningful. Gated on Crawler 1.0 producing the underlying data. Phase 21 backlog captures the dependency chain. |
| 13.5 | Crawler 1.0 — on-market MLS automation (full) | **DONE** (scaffold) | CRITICAL | (CODE 5/19 Commit Q.7.) Crawler framework shipped: lib/crawler/{types, pipeline}, propstream source adapter (uncredentialed), `/api/agents/sentinel/crawler/scan` endpoint, intake-quality-gates wire-up. **Live integration unlocks per-credential-paste** — PropStream API access ($299/mo tier) is the gate. The wife-retirement bet's architecture is in place; firing it is operator/external. |
| 13.6 | Crawler 2.0 — off-market motivated seller pipeline | **DONE** (scaffold) | HIGH | (CODE 5/19 Commit Q.7.) Three off-market adapters scaffolded: probate / tax_delinquency / code_violations. All county/city-specific (no single API covers them); per-county integration sequenced after Crawler 1.0 + market-selection (Phase 13.4). Framework + intake-pipeline composition + classifier all live. |
| 13.7 | Sentinel inbound LLM classifier + Seller_Motivation_Score auto-write | **DONE** | HIGH | (CODE 5/18 Commit N.1 + N.2 motivation-write extension. **Naming drift logged:** Alex's Sprint 2 brief reused "Phase 13" for inbound autonomy work distinct from the existing 13.1-13.6 Sentinel-intake/Crawler track above. Rows 13.7 / 13.8 / 13.9 carry the inbound-autonomy work; the original 13.1-13.6 track remains separate and NOT STARTED.) Sub-commits: N.1 (`93eb356`) — `lib/sentinel/types.ts` (7-intent taxonomy: motivated / lukewarm / rejection / question / wire_fraud_red_flag / off_topic / spam — richer than scan-replies' 4-bucket regex; 7 red-flag categories: phishing_link / request_wire_transfer / impersonation / request_routing_number / fake_urgency / deceptive_identity / off_platform_redirect) + `lib/sentinel/classifier.ts` (Anthropic Sonnet via injectable fetcher; pure coercion helpers exported individually with safe `off_topic` default; SENTINEL_SYSTEM_PROMPT specifies JSON schema + v1.3 motivation 1-5 scale; conservative bias on wire-fraud false-positives) + `app/api/sentinel/classify/[recordId]/route.ts` (GET classifies latest inbound from `lib/notes.lastInboundLine`; POST { body } for explicit-body classification; per-call audit `agent: sentinel, event: sentinel_classified`); 38 pure + injected-fetcher tests. N.2 (`42705e9`) — extends classify route with `?apply_motivation=1` query param that writes `Seller_Motivation_Score` per the v1.3 spec (Phase 20.2 added the field; this closes the loop). N.4 (`(this commit)`) — extracted pure `decideMotivationApply` gate to `lib/sentinel/motivation-gate.ts` with 13 unit tests locking the gate ordering invariants. **Motivation-write gate** (ALL required for "apply", else skip with structured reason for audit): apply_motivation=1 flag + intent ∈ {motivated, lukewarm} + motivation_score_hint != null + existing Seller_Motivation_Score IS null (never stomp operator-set values — operator's call always wins). Successful auto-write fires a separate `event: sentinel_motivation_applied` audit row so Pulse can baseline "Sentinel auto-scored N inbounds this week". 740/740 tests passing. |
| 13.8 | Sentinel reply drafter (firm_hold / soft_counter / ask_for_pof / soft_nurture / answer_question / decline_politely / alert_only) | **DONE** | HIGH | (CODE 5/18 Commit N.2.) Sub-commits: N.2 (`42705e9`) — `lib/sentinel/drafter.ts` 7-stable-option-ID taxonomy + `optionsForIntent(intent)` mapping (motivated → [firm_hold, soft_counter, ask_for_pof]; lukewarm → [soft_nurture, ask_for_pof]; rejection → [decline_politely]; question → [answer_question]; wire_fraud_red_flag / off_topic / spam → []) + `SentinelDraftPackage` shape (drafts / recommended_index / classification / model / generated_at) + pure coercion (option-list filtering against allowed-for-intent + dedupe by option + recommended_index clamp + alert-only force-empty/channel-none) + SENTINEL_DRAFTER_SYSTEM_PROMPT (direct/agent-respectful voice, no cold-script reintros, SMS default with email for multi-paragraph, no inventing numbers) + LLM-call short-circuit on alert-only intents (saves Anthropic budget on wire-fraud / off-topic / spam) + `app/api/sentinel/draft/[recordId]/route.ts` (GET classifies-then-drafts in one round-trip; POST { body?, classification? } skips re-classification for the approval queue path); per-call audit `agent: sentinel, event: sentinel_drafted, decision: drafts_proposed | alert_only | error`. 32 pure + injected-fetcher tests locking option-per-intent dispatch + coercion + alert-only short-circuit. **Approval-gated per Phase 13 charter:** the drafter PROPOSES, never sends; send still requires explicit operator click via /api/deal-action/[id]. |
| 13.9 | Sentinel approval queue UI (`/sentinel`) | **DONE** | HIGH | (CODE 5/18 Commit N.3.) Sub-commits: N.3 (`1cead30`) — `app/api/sentinel/queue/route.ts` lightweight metadata feed (active deals where lastInboundAt > lastOutboundAt, same "owes them a reply" gate as jarvis-brief's responseDue, sorted newest-first, `has_motivation_score` flag for already-scored rows, NO LLM calls) + `components/sentinel/SentinelApprovalQueue.tsx` (per-row "Classify & draft" button fires `/api/sentinel/draft/[recordId]` only on operator click — no auto-fire to avoid burning Anthropic budget on rows the operator never reviews; classification chips render intent + confidence + motivation hint + red-flag pills; drafts surface as cards keyed on stable option ID with the recommended one ★-marked + emerald border; per-draft textarea pre-filled with LLM output for operator edit; Send button routes through existing `/api/deal-action/[id]` action_type=send_reply so SMS/email writeback + lastOutboundAt updates fire identically to JarvisGreeting CardBlock; alert-only intents surface an amber banner instead of a draft; per-row dismiss button hides without firing) + `app/sentinel/page.tsx` thin shell + `components/factory-floor/SentinelRoom.tsx` extended with inbound-queue teaser (fetches queue count on mount, deep-links to /sentinel, room role updated from "Intake" to "Intake + Inbound triage" reflecting Phase 9.4b → Phase 13 scope expansion). **Phase 13 charter:** queue → Classify & draft → review → Edit if needed → Send (operator click) OR Dismiss. No auto-classify on queue load, no auto-send. |

---

## Phase 14 — Pulse Build (System Self-Monitoring) (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 14.1 | Confidence threshold model for proactive surfacing | **DONE** | HIGH | (CODE 5/19 Commit Q.2.) `PulseDetection.confidence` field added (0-1, defaults to 1.0). Deterministic threshold detectors stay at 1.0 (always confident in binary fire); pattern-detection detectors (e.g., outreach-volume-drop) return 0.7-0.9 based on historical sample strength. Drives proactive-surfacing gates without breaking existing detectors. |
| 14.2 | Drift detection (audit log patterns over time) | **DONE** | MEDIUM | (CODE 5/19 Commit Q.2.) `lib/pulse/detectors/outreach-volume-drop.ts` — compares current 24h send count vs prior-48h-normalized baseline; warning at ≥50% drop, critical at ≥80%. Confidence-weighted by historical sample size. Catches "system is up but nothing's happening" failure mode. Together with voice_drift (P.4) + spine_write_rate (O.1) + token_burn (O.1), the audit-pattern drift surface is well-covered. |
| 14.3 | Quota burn monitoring (RentCast + Quo + Anthropic API) | **DONE** | HIGH | (CODE 5/19 Commit Q.2.) Quo: `lib/pulse/detectors/quo-quota-burn.ts` — counts quo.send_attempt in 24h vs operator-configured daily limit (PULSE_QUO_DAILY_LIMIT, default 500). Anthropic: covered by token-burn detector from Phase 14 O.1. RentCast: already shipped in lib/maverick/rentcast-burn-rate.ts. All three providers now monitored. |
| 14.4 | Capability/model registry monitoring (Spec v1.2 §6.9 Phase 2) | DEFERRED (Phase 21) | MEDIUM | Anthropic doesn't expose a model-availability API; can't automatically detect when new models ship. Voice-drift detector (Phase 10 P.4) catches model FALLBACK at the runtime layer. Operator-side: manual review when Anthropic announces new versions; voice-registry update is a one-liner. Phase 21 backlog tracks watching for Anthropic API expansion. |
| 14.5 | Joe Schmoe-style cross-context recall surfacing | DEFERRED (Phase 21) | HIGH | The "all-seeing eye" capability. Foundation laid via maverick_recall MCP tool. Surfacing layer needs a UI surface (search-across-Spine page). Deferred to Phase 21 — UI build estimated 4-6 hours. |
| 14.6 | Family-time signal awareness | **DONE** | MEDIUM | (CODE 5/19 Commit Q.2.) `lib/maverick/family-time.ts` — classifyFamilyTime(at, rules) → {is_family_time, reason: weekend / after_hours / before_hours / work_hours}. Pure helper with Intl.DateTimeFormat for timezone-aware extraction. Rules env-overridable (MAVERICK_FAMILY_END_HOUR / START_HOUR / WEEKENDS_FAMILY / TZ). 15 tests. Briefing aggregator + Shepherd panel will consume in follow-up to dim non-critical surfaces during family hours. |
| 14.7 | Pulse-as-briefing-routine architecture (named-agent integration, audit log discipline) | **DONE** | HIGH | (CODE 5/18 Commit O. **Naming drift logged:** Alex's Sprint 3 brief reused "Phase 14" sub-numbering (14.1-14.6) for Pulse self-monitoring deliverables distinct from the existing 14.1-14.6 above (confidence thresholds / capability monitoring / family-time signal). Rows 14.7-14.12 carry the inbound observability work; the original 14.1-14.6 track remains separate. Same pattern as Phase 13's 13.7-13.9 naming-drift resolution.) Sub-commits: O.1 (`d98d35e`) `lib/pulse/types.ts` (PulseDetection / PulseSeverity / 6 stable PulseDetectorId) + `lib/pulse/active-store.ts` (KV-backed state with PulseActiveEntry { detection, first_seen_at } so the briefing aggregator + room render without re-running detectors) + `lib/pulse/runner.ts` (runAllDetectors + diffActiveSet pure helpers + runPulseScan orchestration that writes Spine via lib/maverick/write-state.writeState with attribution_agent: "pulse" + event_type: "build_event" ONLY on transitions — new fires + resolutions; steady-state silent keeps Spine clean). 10 runner tests + 54 per-detector tests. **Roster integration:** "pulse" is in lib/maverick/write-state.MAVERICK_ROSTER_AGENTS so attribution validates. **Audit log discipline:** every detection transition (off→on or on→off) writes both a Spine row AND an audit() KV event with decision={severity}; steady-state detections refresh source_data in active-store but don't re-write to Spine (keeps the decision log tractable). |
| 14.8 | Anomaly detectors — six pure-function modules | **DONE** | HIGH | (CODE 5/18 Commit O.1.) Six detectors in lib/pulse/detectors/, each a pure function (PulseDetectorInput → PulseDetection[]) consuming audit log + listings + test_count + env: (1) **token-burn** (closes Phase 20.5 carry-forward) counts Anthropic-calling audit events × per-event cost estimate (synthesizer $0.08, sentinel_classified $0.02, sentinel_drafted $0.04, photo_analyzed $0.05, rehab_calibrated $0.05) and alerts when projected 24h spend crosses thresholds (default warning $8, critical $20, env-overridable); (2) **cron-cycle-silent** heartbeat-proxy fires when MAX(audit.ts) older than 36h warning / 72h critical (per-cron tracking deferred — documented; cron routes don't currently write top-level audit entries); (3) **spine-write-rate** counts write_state.* events in rolling window — zero in 48h = critical (the canonical Phase 20.7 signature); (4) **test-count-regression** compares current test_count vs previous Pulse-anchored count, drops ≥3 → warning, ≥10 → critical; (5) **endpoint-error-rate** per-event confirmed_failure/total tally with min-samples gate (default 5) + per-event ID so flapping endpoints don't collide; (6) **stale-data-drift** counts active-pipeline listings with no inbound/outbound in >14d (carries forward 33-response-cluster pattern from 5/18 briefing). 54 per-detector tests across all six (known-good + known-bad fixtures + env-override paths + edge cases). |
| 14.9 | Severity tier integration (Phase 9.5 reuse) | **DONE** | HIGH | (CODE 5/18 Commit O.2.) PulseSeverity → SeverityTier mapping: info → 1 (emerald "Needs eyes"), warning → 2 (orange "Priority"), critical → 3 (red "Critical"). lib/maverick/severity.inferPrioritySignals extended with a Pulse branch — each active detection becomes PrioritySignal { id: "pulse:<detection_id>", tier, agent: "pulse", href: "/pulse", title, reason: truncated 160-char description }. Slots into the existing tier-desc sort so a critical Pulse signal beats tier-2 source-down signals at the priority surface. 6 new severity tests cover tier mapping per severity + description truncation + sort ordering + omitted-pulse-field backward-compat. tierOverride on PulseRoom drives the factory-floor card's border + status dot to the highest active severity — so a critical detection lights up the floor even when Pulse's audit-activity is quiet. |
| 14.10 | Pulse audit log + Spine wiring | **DONE** | HIGH | (CODE 5/18 Commit O.1 + O.2.) Per Phase 14.4 brief: every detection transition writes to Spine via writeState (the server-side equivalent of maverick_write_state MCP) with attribution_agent: "pulse" + event_type: "build_event". New detections fire `event: pulse_detection_fired` audit entries with decision={severity}; resolutions fire `event: pulse_detection_resolved` with first_seen_at echoed for trace continuity. Steady-state detections do NOT re-write (active-set diff in runner.diffActiveSet). lib/pulse/active-store.PulseActiveState stores active detections in KV under `pulse:state` with full PulseDetection object + first_seen_at — refreshed every scan so source_data tracks current metric values; defensive parser tolerates legacy shapes. Briefing aggregator reads this state in parallel with the SourceResult fetchers; StructuredBriefing.pulse carries the rendered shape downstream. |
| 14.11 | Quota-burn anomaly detection (closes Phase 20.5) | **DONE** | HIGH | (CODE 5/18 Commit O.1 — implemented as the token-burn detector in row 14.8.) **Provider scope:** Anthropic only in O.1 (the largest cost driver — Sentinel drafter + classifier + photo vision + rehab vision + synthesizer). RentCast burn-rate continues to live in lib/maverick/rentcast-burn-rate.ts (Phase 9.x); Pulse references it via the existing briefing.external_signals.rentcast.burn_rate path. ScraperAPI + Quo daily-cost detectors are not yet in scope — the existing audit-log doesn't capture per-call cost metadata for them. Promote to expanded DONE when those audit-event fields land (Phase 14.11.1 follow-up). **Thresholds env-overridable** via PULSE_TOKEN_BURN_WARNING_USD / PULSE_TOKEN_BURN_CRITICAL_USD; defaults $8 / $20. |
| 14.12 | Pulse room UI on factory floor (`/pulse`) | **DONE** | HIGH | (CODE 5/18 Commit O.3.) `components/factory-floor/PulseRoom.tsx` reads briefing.structured.pulse.active_detections, renders 3-column severity-count strip (CRIT/WARN/INFO pills, dim opacity when zero) + top-3 detection headlines with first_seen_at age + "+N more →" overflow. tierOverride drives the room's AgentRoom shell border + status dot to the highest active severity (Phase 9.5 visual treatment reuse). href routes to `/pulse` on click. Full dashboard at `app/pulse/page.tsx` → `components/pulse/PulseDashboard.tsx` mirrors SentinelApprovalQueue shape: 3-cell severity summary + per-detection cards with expand-on-click detail (description + suggested-action callout + collapsible source-data JSON) + manual "Scan now" button hitting /api/agents/pulse/scan that reports transitions as a toast + auto-scans on mount. Phase 14 charter holds: no auto-remediation buttons — Pulse surfaces, operator decides where to act (Sentinel for inbound, Crier for outreach, manual investigation for infrastructure). |

---

## Phase 15 — Ledger Build (Economics) (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 15.1 | Revenue tracking per deal | **DONE** | HIGH | (CODE 5/19 Commit Q.4.) `lib/ledger/economics.computeDealPnL` + `rollupRevenue` — per-deal P&L (assignment_fee, operator_take, truck_fund_contribution, net_to_operator) + closed-deal rollup. Surfaced via /api/agents/ledger/summary. |
| 15.2 | Agent cost attribution (LLM API spend per agent) | **DONE** | MEDIUM | (CODE 5/19 Commit Q.4.) `lib/ledger/cost-attribution.attributeCost` — reads synthesizer audit events, attributes USD spend per agent via per-event cost table (kept in sync with Pulse's token-burn detector). Returns per-agent rows sorted by spend desc with by_event breakdown. |
| 15.3 | Truck fund tracking | **DONE** | MEDIUM | (CODE 5/19 Commit Q.4.) Per Alex's spec — 10% of operator's 30% take. EconomicsConfig env-overridable (LEDGER_TRUCK_FUND_PCT / LEDGER_OPERATOR_TAKE_PCT). truck_fund_contribution computed per closed deal; aggregate total_truck_fund in revenue rollup. |
| 15.4 | Wife retirement progress meter | **DONE** | HIGH | (CODE 5/19 Commit Q.4.) `lib/ledger/economics.computeRetirementProgress` — YTD operator take vs LEDGER_ANNUAL_RETIREMENT_TARGET_USD; calendar-year-fraction projection; pace_pct showing if on track. Returns null progress when target unset (operator hasn't configured their personal number). |
| 15.5 | Deal-by-deal P&L cards | **DONE** (data layer) | MEDIUM | (CODE 5/19 Commit Q.4.) DealPnL shape + per_deal array in revenue rollup. UI rendering (cards on a /ledger page) deferred to follow-up — data layer is the unlock; rendering is small + can mirror SentinelApprovalQueue / PulseDashboard pattern. |
| 15.6 | Lane purity (Inevitable revenue only — Whitetail excluded forever) | LOCKED IN SPEC | CRITICAL | Per Spec §7 + Character Spec §5.1 |

---

## Phase 16 — Active Deals In Flight (TACTICAL, CURRENT)

These are deals currently active. Status accurate as of 5/15 evening Maverick briefing + tonight's review.

(CODE 5/16: live briefing 1:11 UTC 5/16 returned `active_deals: []` because all 3 Airtable sources timed out. Status here preserved from 5/15 — Alex to refresh if any have moved.)

| # | Deal | Status | Action |
|---|------|--------|--------|
| 16.1 | 23 Fields Ave (Memphis, Candice Hardaway) | Contract review in progress | DocuSign envelope received 5/14. Email to Candice re: date corrections sent 5/15 evening. Awaiting corrected envelope. Section 16 properly redlined per Candice. EMD $675 to Regency Title within 3 days of binding. Target close ~June 5. |
| 16.2 | Hallbrook (Memphis) | Accepted at $93,500 | Active working playbook. Sole Memphis acquisition exception. Status not yet moved forward in Airtable per Maverick briefing |
| 16.3 | 5006 Creekmoor (San Antonio, George Vasquez) | $117,500 hold, 4 days silent | Soft nudge candidate. SMS counter previously fired but Quo data shows quiet since 5/12 |
| 16.4 | Briyana Jordan — Sturtevant Detroit | All-in ~$31K, $145K ARV | Math works if Detroit buyer sourced |
| 16.5 | Ford St (Daniel) | Reopened — seller offering to repair fire damage | Pending new pricing terms |
| 16.6 | Kim Maloney 8-plex (Houston) | Dead (competing offer) | DO NOT RESURFACE |
| 16.7 | 3022 El Paso St | Dead 4/14 | DO NOT RESURFACE |
| 16.8 | NEVER-list: 2715 Monterey, 714 Hallie, 4330 Pensacola, 9618 Tamalpais, 811 Manhattan, 1635 Arbor, 4448 Marcell, 2725 Bowling Green, 2011 Ramsey, 707 N Pine, 8641 Craige, 910 Green | NEVER RESURFACE | Filter enforced via Sentinel |
| 16.9 | 13-day Detroit Response Received cluster (~12 addresses) | Stale, needs triage | Crier silence may be exacerbating. Resolve after Phase 11.2 fix |
| 16.10 | **Live Exercise Backfill tool (admin)** — exercises Appraiser ARV → Rehab → Buyer Intelligence across the active pipeline so BroCard v1.3 pricing reflects on every record | **DONE** (operator-fire pending) | (CODE 5/18 Commit M.) Route: `GET /api/admin/appraiser-backfill`. Dry-run by default; apply mode behind `?apply=1`. Filters to active outreach status (Negotiating / Response Received / Counter Received / Offer Accepted + recent Texted/Emailed); skips Manual Review (LOW arvConfidence, overridable via `?include_manual_review=1`), already-complete records (all three of ARV_Validated_At + Rehab_Estimated_At + Estimated_Monthly_Rent populated, overridable via `?force=1`), and missing-zip records. Sequential per-record ARV → Rehab → Buyer Intelligence calls; pacing configurable via `BACKFILL_PACE_MS` env var (default 2000ms, clamped 0-30000ms). Lambda budget guard stops cleanly with `truncated_by_budget` flag so the final audit always lands; operator iterates with `?limit=N` until `remaining_eligible: 0`. Per-record audit `agent: appraiser, event: backfill_record_applied` + final run audit `event: backfill_apply_run` feed Maverick load-state surface + downstream Pulse failure-rate baselining. Validation anchor: the 37-active-deal canonical state (all three completion fields null + zip present + no LOW-ARV flag → eligible) locked in `lib/admin/appraiser-backfill.test.ts`. 657/657 tests passing. **Operator runbook:** (1) dry-run first to see eligible count + cost estimate; (2) `?apply=1&limit=10` to process ~6-10 records per lambda invocation; (3) iterate until queue clears. |

---

## Phase 17 — Monetization & Revenue Layers (FORWARD)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 17.1 | Phase 1 monetization — wholesale deal flow | IN PROGRESS | CRITICAL | Current focus |
| 17.2 | Phase 2 monetization — adjacent niches (commercial, land, machinery) | STOP (strategy) | HIGH | Business strategy — changes who the system serves + what AKB sells. STOP condition #1. Operator decides timing + which niche to enter. Same stack, different inputs — engineering is straightforward after the strategic call. |
| 17.3 | Phase 3 monetization — digital products (Gumroad/Etsy) | STOP (strategy) | MEDIUM | Business strategy — changes the product surface. STOP condition #1. Etsy + Gumroad accounts created; products NOT LISTED — operator decides what to list. |
| 17.4 | Wholesale Deal Analyzer ($19.99) | DRAFTED | LOW | `Updated_Etsy_Listing_Copy.md` exists |
| 17.5 | Agent Outreach Playbook ($14.99) | DRAFTED | LOW | |
| 17.6 | Property Screening Checklist ($9.99) | DRAFTED | LOW | |
| 17.7 | Pipeline Tracker ($49) | STOP (strategy) | LOW | Same monetization-strategy fork as 17.2/17.3 — STOP condition #1. Pricing + positioning are operator calls. |
| 17.8 | 90-Day Playbook ($79) | STOP (strategy) | LOW | Same as above. |
| 17.9 | Full System License ($497+) | STOP (strategy) | LOW | Same as above. |

**SKIPPED — REPLACED-BY-architecture:**
- Coaching / Cohorts / Discord / SaaS / DFY services — Alex is a loner, will not do ongoing customer contact. Permanently out of scope.

---

## Phase 18 — Market Geography (HISTORICAL + ACTIVE)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 18.1 | Texas — San Antonio active | DONE | CRITICAL | Primary active market |
| 18.2 | Texas — Dallas | DONE | HIGH | Active outreach |
| 18.3 | Texas — Houston | DONE | HIGH | Active outreach |
| 18.4 | Tennessee — Memphis (acquisitions PAUSED, outreach OPEN) | DONE | CRITICAL | Sole exception: Hallbrook. Assignment clause check pre-contract required. Target ZIPs: 38109/38114/38116/38118/38127/38128. Avoid 38106 |
| 18.5 | Wholesale-restrictive states: IL, MO, SC, NC, OK, ND — AVOIDED | LOCKED | CRITICAL | Filter in Scenario A |
| 18.6 | Detroit (Michigan) — active | DONE | HIGH | Volume cohort |
| 18.7 | New market expansion logic (Sentinel intelligence) | DEFERRED (Phase 21) | HIGH | See Phase 13.4 — same dependency chain (needs Crawler 1.0 producing market-signal data first). |

---

## Phase 19 — Documentation Discipline (META, ONGOING)

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 19.1 | Inevitable_Constitution_v3.docx (formerly Bible_v3) | DONE | CRITICAL | Living canonical handoff. READ FIRST every session |
| 19.2 | Continuity Layer Spec v1.2 (canonical) | DONE | CRITICAL | In repo at `docs/specs/Inevitable_Continuity_Layer_Spec_v1.2.md` |
| 19.3 | Three Maverick spec docs (Character / Daily UX / Capability Absorption) | DONE | HIGH | (CODE 5/16: archived copies committed to `docs/specs/` this turn as part of Days 6-7 audit.) |
| 19.4 | This Master Checklist | **DONE (canonical v1.1 audited)** | CRITICAL | (CODE 5/16: this commit. Future revisions follow the Living Artifact pattern.) |
| 19.5 | `MAVERICK_OPS.md` runbook | DONE | HIGH | Token rotation, OAuth troubleshooting, Gate 3 walkthrough |
| 19.6 | `MAVERICK_V12_BACKLOG.md` | DONE | HIGH | 9 deferred items logged |
| 19.7 | Living Artifact discipline: every doc carries `_v(n)` with `v(n+1)` expected | LOCKED PRINCIPLE | CRITICAL | Amendments are mechanism, not exception |
| 19.8 | No new doc generation without checking this checklist first | NEW DISCIPLINE | CRITICAL | INSERTED 5/15 BECAUSE 7 billion documents produced. New procedures get INSERTED INTO this checklist, not spawned as separate docs. (CODE 5/16: this audit produced ONLY the three explicitly-sanctioned deliverables — corrected Checklist, System Inventory, Dashboard Current State. No floating docs.) |

---

## Phase 20 — Open Architectural Questions

| # | Question | Status | Blocks | Notes |
|---|----------|--------|--------|-------|
| 20.1 | **Make.com migration vs. retention.** | **RESOLVED 5/16** | (nothing — was blocking Phase 8 audit; now unblocked) | See Resolution Log below. |
| 20.2 | **`Stored_Offer_Price` semantics — historical vs operative.** | **RESOLVED 5/18 — both, via two-field split.** | (was blocking Phase 11.4 dual reading; now unblocked) | The v1.2 spec collapsed two semantic surfaces into one field. v1.3 amendment splits them: **Outreach_Offer_Price** (sticky 65%-of-List at outreach time, never overwritten — door-opener semantics, the funnel filter for motivated sellers) and **Contract_Offer_Price** (operative ceiling at negotiation/DD stage, can be ABOVE or BELOW outreach price because DD reveals whether to drop (worse rehab) or push (clean deal + motivated seller)). Hard floor on contract: V2.1 math (Investor_MAO − Wholesale_Fee). Soft ceiling: none, but >75% of List triggers Maverick caution flag on deal-detail. Plus new `Seller_Motivation_Score` (1-5 rubric) field — manually scored now, automated by Sentinel in Phase 13 — drives the seller-motivation modifier on the Phase 4D Unified Deal Math range endpoint. Commit H (5/18) shipped: Airtable rename of `Stored_Offer_Price` → `Outreach_Offer_Price` (field id preserved, all data carried), new `Contract_Offer_Price` (fldfJWuEIHqaRuWq3) + `Seller_Motivation_Score` (fldfEVJijfPOBulpc) fields, write-path split (outreach-fire writes Outreach_Offer_Price sticky-gated; Pricing Agent writes Contract_Offer_Price), Listing type updated, briefing surface updated, 523/523 tests. Orphan empty `_orphan_outreach_offer_price_5_18` (fldhl0njOHREJQ6Gd) left in Airtable for Alex to delete via UI (MCP can't drop fields). |
| 20.3 | **Auth waterfall caller types — dashboard browser is a fourth surface.** Spec v1.2 §6.8 names three: OAuth opaque token (claude.ai), CRON_SECRET (Vercel crons), MAVERICK_MCP_TOKEN (dev/CI shell). The dashboard browser is a fourth — same-origin fetch with no Authorization header, authenticated via the existing AuthGate's `akb-auth=authenticated` cookie (sameSite=strict, httpOnly, secure). Discovered 5/16 Commit B because the Shepherd panel's `/api/maverick/load-state` fetch hit the Day-2 bearer-only gate and 401'd. | **RESOLVED-IN-PATCH 5/16 (Commit B.1)** | (was blocking Shepherd panel data load) | Resolved in `lib/maverick/oauth/auth-waterfall.ts` via new `hasDashboardSession(cookie)` helper. Load-state route checks dashboard cookie first; falls through to the OAuth waterfall for non-browser callers. MCP route is unchanged — dashboard cookie does NOT grant MCP access (different threat model: MCP is for external Claude clients; load-state is read-only briefing data). v1.3 spec should fold the dashboard surface into the canonical auth model + decide whether other Maverick endpoints (write_state, recall) should also accept dashboard session. |
| 20.5 | **Quota-burn anomaly detection — Pulse must watch for system-driven token consumption exceeding human-activity baseline.** 5/17 unattended Sat-Sun window burned ~4.8M tokens (paired aggregator + synthesizer calls every ~6 min) from a source still unidentified after Phase 11.6 cron-inventory audit. The exact failure mode Pulse (Phase 14) is supposed to surface and didn't because Pulse isn't built yet. Pulse must (a) baseline expected token volume from `dashboard_session`-attributed calls vs system-attributed calls, (b) alarm when fresh-synthesis cadence exceeds a documented ceiling, (c) integrate the `pulse_event:"non_user_synthesis"` console breadcrumbs landed in Commit B.2 as the data trail. Owner: Phase 14 Pulse build. Until Pulse ships, `MAVERICK_CRON_ENABLED=false` is the safety floor. | **OPEN** | Phase 14.3 (quota burn monitoring) | INSERTED 5/17 BECAUSE the 48hr burn was the exact failure mode Pulse exists to prevent. Phase 14.3 currently scopes Anthropic burn to RentCast/Quo-style external-quota tracking; this entry extends that scope to *internal* token consumption — the synthesizer's own cost when called by non-humans. |
| 20.7 | **Continuity-layer-stale-deployment + missing write-state discipline.** Two compounding failures discovered 5/18: (H1) Vercel project's only `target: "production"` deployment is `dpl_4peQ2dTrnWM34bJrE3XqJY3Sd69Y` (sha 33341a3e, branch main, 2026-05-12) — BEFORE Phase 9 work began. All Commit B.2 → G.3 deployments (15+ across Phase 9.4 / 9.6 / 9.7 / 9.8 / 11.6 / 11.7 / 11.2 / 5.1 / 5.2 / 5.3 / 5.4) shipped as `target: null` previews; production alias still resolves May 12 code. (H2) `lib/maverick/write-state.ts` exists + tested but no caller invoked it after any commit since the 5/16 audit; Spine_Decision_Log had zero entries for the entire Phase 9-11-12-5 build. Result: Claude sessions opening via the connector got multi-week-stale briefings AND zero recall hits for tonight's work. Same failure-mode class as Phase 11.6/11.7 (silent divergence from reality). | **DONE — H2 closed; H1 awaiting Alex's connector repoint OR merge-to-main** | (continuity-layer trust budget; everything downstream of recall depends on it) | INSERTED 5/18 BECAUSE the system silently lied. H2 resolved this session via 10 backfill build_event writes (parent reco15HtXmy2PvrY9, plus phase entries for 9.3+9.11, 9.1+9.2+9.5, 9.9, 11.6+11.7, 9.4, 9.6+9.8, 9.7+12.3, 11.2, 5 Scribe). H1 has two fix paths: (a) repoint Claude.ai MCP connector to the branch-stable alias `coach-pulse-git-claude-fix-token-b-efd89f-golf4life27s-projects.vercel.app` — auto-tracks the latest preview push, lowest blast radius; (b) merge `claude/fix-token-burn-cost-JUDad` to main → Vercel re-deploys production. Recommendation: Path (a) until Phase 9/11/12/5 has full browser-eyes-on validation across a couple of refresh cycles, then merge. Discipline going forward: every Code commit on this branch includes a `maverick_write_state` call as part of the commit ritual until a git post-commit hook automates it. |

### Resolution log

**20.1 RESOLVED — May 16, 2026 (Code's Days 6-7 audit)**

**Decision:** Per-scenario retirement plan, canonical in `docs/AKB_INEVITABLE_Code_Briefing.md §9` (cross-referenced to `Inevitable_Operations_Bible §9.5`). This is essentially Option (c) hybrid, but with explicit per-scenario triggers rather than a generic "migrate where it makes sense" framing.

**The matrix:**

| Make scenario | Disposition | Trigger |
|---|---|---|
| A (Intake, 4256273) | KEEP (data pipe) | Never |
| B (Verification, 4331170) | KEEP, fix UI defects (items 1.4-1.7) | Never |
| H2 (Outreach, 4724197) | Replace with Acquisition Agent | After 30 days of agent stability |
| L3 (Reply triage, 4812756) | KEEP — Negotiation Agent reads its output | Never |
| L4 (Outbound capture) | KEEP | Never |
| G (Dispo blast, 4583609) | Replace with Dispo Agent | When Dispo Agent ships (Phase 4.7) |
| J (Verify agent, 4724499) | Replace with Negotiation Agent | When Negotiation Agent ships (Phase 12.4) |
| I (Rehab est) | Replace with Pricing Agent 4B subroutine | DONE — `app/api/rehab-calibration/route.ts` shipped |
| K (ARV) | Replace with Pricing Agent 4A subroutine | DONE — `app/api/arv-intelligence/[zip]/route.ts` shipped |

**Standing rule:** "Build new in Vercel only. No new Make scenarios." (Code Briefing §9 line 367.)

**Open sub-question (logged for future amendment):** Spec v1.2 line 257 says "Sentinel ... Scenario A + Scenario B in Make today; **migrates to Vercel post-precedent**." That contradicts §9's KEEP-forever framing for A + B. Conservative read: A + B are temporarily KEPT for the current build cycle, with migration deferred until after deal #1 closes (the "post-precedent" trigger). Spec v1.3 should reconcile — either amend §9 to "KEEP until post-precedent" or amend the Continuity Layer Spec §6 Sentinel row to "KEEP forever." Recommend the former for less spec churn.

**Resolved-by:** Code audit, May 16, 2026.

---

## Phase 21 — Strategic Backlog (FORWARD, OPERATOR-PRIORITIZED)

**Inserted 2026-05-19 / Q.8 autonomy-run closeout per Alex's Map 1
closing directive: "Phase 21 lands as a new Master Checklist section
with the full backlog Alex and I synthesized this session. Include
MCP capability gaps, AI capability gaps, strategic Airtable assets
sitting empty, buried operational ideas."**

Living section. Each row is a candidate for a future sprint; ordering
is by impact + operator decision sequencing.

| # | Item | Severity | Notes |
|---|------|----------|-------|
| 21.1 | Operator fires `/api/admin/appraiser-backfill?apply=1&limit=10` against 37 active deals | CRITICAL | Phase 16.x backfill tool shipped; awaiting operator fire. BroCard v1.3 pricing reflects on every active record once this runs. Iterate with limit=10 until `remaining_eligible: 0`. STOP condition #3 — operator-fire only (touches active records via per-record API calls; benign but operator-authorized). |
| 21.2 | Operator processes 33-response cluster via /sentinel queue | CRITICAL | Sentinel approval queue shipped (Phase 13 N sprint). The aged 33-response cluster from the 5/18 briefing is the canonical first-batch test. Operator triages, classifies, drafts, approves — exercises the whole inbound-autonomy stack. |
| 21.3 | Operator provisions DocuSign JWT credentials (12.7) | HIGH | Top of revenue-unlocking external-account work. ~15-20 min in DocuSign Admin Console. Scribe goes live the moment env vars land in Vercel. 23 Fields gets tracked immediately. |
| 21.4 | Operator provisions Vercel API token (12.1) + GitHub PAT (12.2) | MEDIUM | Unblocks Maverick load-state briefing's deploy/git source visibility. Both are 5-min external-account operations. |
| 21.5 | Crawler 1.0 live integration — PropStream API subscription + key + adapter wire-up (13.5) | CRITICAL | The wife-retirement bet's external dependency. Framework + intake pipeline shipped Q.7; live integration is: subscribe ($299/mo API tier as of 5/19), generate key, set PROPSTREAM_API_KEY env, replace adapter scan() body with real fetch+map. Inline wire-up checklist in lib/crawler/sources/propstream.ts header. |
| 21.6 | Crawler 2.0 per-county adapters (probate / tax / code) (13.6) | HIGH | Gated on Crawler 1.0 + market-selection (13.4). Per-county scraper / public-records-API per source. PII handling discipline required for agent-contact enrichment. Sequenced after PropStream proves intake volume can drive deal flow. |
| 21.7 | Phase 13.4 Geographic expansion logic — needs market-signal data | HIGH | "Sentinel suggests Beaumont because signals are strong" requires market-signal data infrastructure that doesn't exist until Crawler 1.0 produces it (activity-by-zip, comp density, response rate per ZIP3, etc.). Builds on top of 21.5. |
| 21.8 | Phase 14.5 Joe Schmoe cross-context recall UI | HIGH | maverick_recall MCP tool exists; needs a `/maverick/recall` page with search-across-Spine + audit. Estimated 4-6 hours. Unblocks "all-seeing eye" capability. |
| 21.9 | Phase 5.3 Buyer-side contract auto-draft | HIGH | Gated on 12.7 (DocuSign JWT). Once Scribe is live, Scout-attribution contract drafting via DocuSign templates. |
| 21.10 | Phase 9.10 Auto-allow maverick_load_state | LOW | Claude.ai connector UI toggle — operator-side, not code. Friction reduction once trust established. |
| 21.11 | Phase 1.6 DOM discrepancy check (PropStream vs Redfin) | MEDIUM | Needs Redfin scrape adapter (Crawler 1.0 second source after PropStream lands). |
| 21.12 | Phase 1.10 bulk cleanup of 890 records | HIGH | Destructive — touches active records. STOP condition #3. Operator authorization required + ideally paired with the appraiser backfill run (21.1) so they're not two passes over the same records. |
| 21.13 | Phase 2.9 Cadence_Queue async dispatch — architectural fork | HIGH | Vercel has no native queue. Three options: (a) Inngest, (b) QStash, (c) Anthropic Routines (Map 2 substrate). STOP condition #5 — operator chooses substrate based on Map 2 trajectory. |
| 21.14 | Phase 4.7 Scenario G retirement (not hardening) | CRITICAL → DEFERRED | Retire G via Make MCP once Dispo Agent (Scout queue from 4.6) has shipped enough sends to validate parity. ~5-min Make-MCP operation. |
| 21.15 | Phase 12.5 / 12.9 secret rotations | MEDIUM | (12.5) Anthropic Console-level spend dashboard awareness — operator weekly check. (12.9) ScraperAPI + OpenPhone key rotation — operator's call when to fire. |
| 21.16 | jarvis-brief / jarvis-chat URL aliases | LOW | File names + URL paths retained for backwards-compat per Phase 9.11 deprecation tags. A future commit could mount /api/maverick/brief + /api/maverick/chat as aliases with redirects. Pure cleanup; no behavior change. |
| 21.17 | Phase 14 — UI surfaces for Ledger /ledger page + family-time signal consumption | MEDIUM | Ledger data layer shipped (Q.4); /ledger page rendering deal-by-deal P&L cards + retirement meter is small (mirror SentinelApprovalQueue / PulseDashboard pattern). Family-time consumption in briefing aggregator + Shepherd panel for non-critical-surface dimming. |
| 21.18 | Quo throttle caller-site wire-up | MEDIUM | `lib/quo-throttle.assertThrottleHeadroom` shipped Q.6. Integration is per-call-site judgment (throttle-fail = hard-error vs queue). Buyer fire-blast + cron/scan-comms + cadence routes are the candidates. |
| 21.19 | Sentry / process-intake wire-up of intake-quality-gates | MEDIUM | `lib/intake/quality-gates.runIntakeGates` shipped Q.5. Process-intake route should call runIntakeGates() and route candidates to Manual Review queue based on action. Wire-up landing when intake route is next touched. |

### Map 2 substrate items (Anthropic Managed Agents — case-by-case migration)

| # | Item | Notes |
|---|------|-------|
| 21.M1 | Migrate Pulse cron → Anthropic Routine | Touch when next modifying the scheduler. Vercel cron stays as fallback during migration. |
| 21.M2 | Migrate Spine audit writes → Memory Stores | Touch when next modifying the audit-log write path. Memory Stores give cross-session recall without manual Spine entries. |
| 21.M3 | Skills install for repeatable patterns (sprint brief template, Phase 19.8 ritual, vocabulary-drift check) | Install at first /sprint command, /spine-write command, or vocabulary-audit need. |
| 21.M4 | Credential vault for Jarvis Key | Adopt at next ANTHROPIC_API_KEY rotation. |

### Orphan / cleanup items

| # | Item | Notes |
|---|------|-------|
| 21.O1 | `_Orphan_Outreach_Offer_Price_5_18` (fldhl0njOHREJQ6Gd) Airtable field | Empty orphan field from Commit H field rename (Phase 20.2). No code reads from it; operator deletes via Airtable UI when convenient. |
| 21.O2 | Phase 12.8 Constitution.docx find/replace | `Buyer_Tx_Median` → `Buyer_Median`. Manual edit in Alex's Claude Project knowledge file (not in repo). |
| 21.O3 | Stale Maverick MCP server identifier rotation discovered 5/18 | The Maverick MCP server identifier has rotated between `mcp__f3b378e8-...` and `mcp__Maverick__*` across sessions. Worth adding a Pulse detector for "mcp_identifier_changed" so future drift is surfaced automatically. |
| 21.O4 | Per-call token usage in audit log | Anthropic Messages API returns usage{input_tokens, output_tokens} per call; synthesizer captures this in outputSummary (Phase 10 P.1). Operator could later replace the per-event-cost-estimate table with the real values from these counts × Anthropic's current pricing. Lighter footprint than maintaining the estimate table. |

---

## Map 1 closed — 2026-05-19

This document is the closing record for Map 1 (Inevitable 1.0 build).
Every row above is now DONE, OBSOLETE, DEFERRED (with reason), or STOP
(awaiting external operator action — surfaced via Phase 21).

### Operational state at close

- **Branch:** `claude/fix-token-burn-cost-JUDad`
- **Test count:** 958/958 passing
- **TypeScript:** clean
- **Next.js build:** clean
- **Pulse cron:** firing daily at noon UTC per `vercel.json`
- **Sentinel queue:** live at `/sentinel`, awaiting operator first-pass on 33-response cluster
- **Scribe:** code live, awaiting DocuSign JWT provisioning (Phase 21.3)
- **Appraiser backfill:** armed at `/api/admin/appraiser-backfill?apply=1`, awaiting operator fire (Phase 21.1)
- **Crawler framework:** scaffold live at `/api/agents/sentinel/crawler/scan`, awaiting PropStream credentials (Phase 21.5)
- **Ledger:** `/api/agents/ledger/summary` live; deal-by-deal P&L data ready for /ledger UI render (Phase 21.17)
- **Maverick load_state:** returns current reality on fresh session

### Stops surfaced to operator (Phase 21)

External account work (5 items): Phase 12.1, 12.2, 12.5, 12.7, 12.9.
Business strategy (5 items): Phase 17.2, 17.3, 17.7, 17.8, 17.9.
Operator-fire (2 items): Phase 21.1 backfill, 21.2 Sentinel cluster.
Architectural fork (1 item): Phase 21.13 Cadence_Queue substrate.

### What Map 2 picks up

Map 2 is the Anthropic Managed Agents substrate migration. Map 1 ships
the system on Vercel + Airtable + Quo + DocuSign. Map 2 migrates
piece-by-piece to native primitives (Memory Stores, Routines, Skills,
Credential Vaults) when each surface is next touched. Items 21.M1-M4
are the first targets.

---

## Phase 22 — Belt-Era Foundation (2026-05-20, post Map-1 closeout)

**INSERTED 2026-05-20** BECAUSE the day after Map 1 closeout produced a sustained burst of investigation + remediation + spec work that didn't fit any existing phase. Investigation-driven remediation cycle emerged as a new mode: audit → discover → operator-decide → remediate → spec next-step. Today's deliverables build on top of Map 1 but are forward-pointing toward the belt MVP.

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 22.1 | Firecrawl operational (validated end-to-end on Redfin) | **DONE** | HIGH | (CODE 5/20.) Egress validated against `api.firecrawl.dev/v1/health` (HTTP 404 no `x-deny-reason` header → connection clean). End-to-end scrape against 4838 Wisteria St Dallas TX 75211 via Firecrawl REST API: HTTP 200, 26,836 chars markdown, content markers present ($239,783 estimate, 1,825 sq ft, address, sale history, property details, climate), zero anti-bot signals (no captcha / Cloudflare interstitial / DataDome / "verify you are human"). Verdict: Firecrawl works end-to-end from the remote container against Redfin. Unblocks Belt v1 Verification station replacement (Scenario B retirement). |
| 22.2 | Belt v1 Spec — architectural design v1.0 → v1.2 | **DONE** | CRITICAL | (CODE 5/20 commits `bd91e93` → `688ace5` → `47537e2`.) `docs/specs/AKB_Belt_v1_Spec.md` — orchestration design. DESIGN-ONLY (no orchestrator code, no endpoint modifications). v1.0 → v1.1 (Firecrawl-native Verification + Make H2 retirement) → v1.2 (strategic-layer revision: state-machine refinements, Scenario A retirement, action-queue surface framing, source-of-truth communications principle, 3 new open questions). Resolves which-stations-need-wrapping question for belt MVP. 388 lines. |
| 22.3 | Attribution Layer v1 Spec — INV-007 Step 2 sprint design | **DONE** | CRITICAL | (CODE 5/20 commit `e1ef585`.) `docs/specs/Attribution_Layer_v1_Spec.md` — architectural design for unified attribution layer (one Comms_Attribution surface that resolves the 5-divergent-attribution-paths root cause from INV-007 audit). 8 sections, 2 mermaid diagrams (ingest flow + render flow), 4-sprint plan with cross-sprint dependencies, 9 open questions. **Resolves on build:** INV-007 Step 2, INV-014 (L3 winner-takes-all), INV-015 (scan-comms fan-out), INV-016 (price-match misalignment), plus the unnumbered `scorePropertyMatch` unit-test coverage gap. Pending operator decision on §8 #1 (storage substrate: Airtable Comms_Attribution table vs Vercel-native Postgres/KV) before Sprint 1 begins. |
| 22.4 | INV-001 Distress_Pass remediation (Option α) | **DONE** | CRITICAL | (CODE 5/20.) Audit `docs/investigations/Distress_Pass_Audit_v1.md` discovered that `Distress_Pass` is an Airtable FORMULA field (not written by Make or Vercel), consuming ONE signal (DOM ≥ 60d) while the system's own `Distress_Score` composite next to it uses 3 signals. 30-day rejection volume was 980/2280 records (43% gate-out on a single criterion); 83/100 sampled records scored High/Extreme on the system's own composite. Option α remediation: single Airtable formula update — `Distress_Pass` now consumes `Distress_Bucket` ∈ {High, Extreme, Moderate}. Post-change: rejection-on-no-distress 980 → 35 (96% reduction), gate-out rate 43.0% → 1.5%. All 35 remaining rejections carry `Distress_Bucket = "Low"` — formula behaves as designed. Spine `rece3J2f0TbHXOuE2`. Single formula edit, reversible. |
| 22.5 | INV-002 Listing_Condition persistence (Option A — dual-path write) | **DONE** | HIGH | (CODE 5/20.) Audit `docs/investigations/Listing_Condition_Audit_v1.md` confirmed CSV `Listing_Condition` is filtered at intake (both Make + Vercel paths) but never persisted — the system has TWO condition signals (CSV thrown away; vision-derived buried in `Rehab_Line_Items_JSON` blob, populated for 0/50 sampled records because Phase 4B not running). Option A remediation: (1) new singleLineText field `Listing_Condition` (`fldgWNINIBKmY6fM1`) on Listings_V1; (2) Vercel `/api/process-intake/route.ts` patched (FIELD_MAP entry + `normalizeCondition()` helper + parallel-write line); (3) **Make Scenario A `4256273` blueprint pushed via `scenarios_update` MCP** — first session-authorized Make blueprint API push, lean-blueprint pattern documented (strip UI-only metadata, push runtime essentials only, Make regenerates UI metadata on next editor open). Spine `recOB75kGmHzkPgKr`. Schema verified post-push. Forward-going only — no backfill of historical records per operator policy. |
| 22.6 | INV-007 Step 1 — `/api/conversations/[id]` attribution filter | **DONE** | HIGH | (CODE 5/20 commit `5252ec0`.) Audit `docs/investigations/Multi_Listing_Agent_Attribution_Audit_v1.md` identified 5 independent attribution behaviors across the system (deal-context / conversations / L3 / scan-comms / multi-listing-detect). Step 1 surgical interim: export `scorePropertyMatch` + `SiblingRecord` from `lib/timeline-merge.ts`; add sibling lookup + per-Quo-message attribution filter inside `/api/conversations/[id]` Quo loop. Same 0.6 confidence floor the AMBIGUOUS banner uses (single source of truth). Visible leak closed: 3273 Steele St outbound no longer renders in 23 Fields Ave deal-room conversation panel for Candice Hardaway (4-listing multi-listing agent). Spine `rec5UVLsXktD8M8Mi`. Step 2 (unified attribution layer at ingest) designed in 22.3, deferred to belt MVP sprint. **Untouched paths:** L3 winner-takes-all (INV-014), scan-comms fan-out (INV-015), `scorePropertyMatch` price-match misalignment (INV-016) — all queued, all resolve when Step 2 ships. |
| 22.7 | ECC plugin + hooks operational | **DONE** (per operator confirmation 2026-05-20) | MEDIUM | Operator-confirmed in 5/20 brief. Code has no repo-side evidence to validate at audit time — accepted on operator confirmation. Cross-link to ECC docs if/when they're added to the repo. (CODE 5/20: surfaced this provenance gap in chat per stall-condition discipline; awaiting future audit cycle to verify against actual code/config.) |
| 22.8 | INV-003 Stage_Calc collapse — V2 canonical with MLS-date gate, Execution_Path_Calc routes Data Issue → Manual Review | **DONE** (V1 field deletion pending operator UI action) | MEDIUM | (CODE 5/20.) Audit `docs/investigations/Stage_Calc_V1_V2_Audit_v1.md` (commit `38628cd`) established V1 had ZERO runtime consumers (vestigial post-V2-refactor artifact); V2 was de-facto canonical via `Execution_Path_Calc` but lacked V1's missing-MLS-date data-quality gate. Resolution shipped Option A + Path 2: (1) `Stage_Calc_V2` (`fldA8B9zOCneF0rjp`) gains `IF(NOT({Has_MLS_Date}), "Data Issue: Missing MLS Date", …)` as the first branch; (2) `Execution_Path_Calc` (`fldNRMrcxbiKHW1C9`) gains `IF(LEFT({Stage_Calc_V2}, 10) = "Data Issue", "Manual Review", …)` LEFT prefix match at the top — future-proofs against additional Data Issue variants without further edits. Verified live: 2 known SqFt-divergent records unchanged (Reject); 6 missing-MLS-date records (5 TEST + 1 real listing 934 Lehman St) now route to Manual Review. ~10 records/full active pipeline now correctly flagged at intake instead of silently passing V1. **V1 field (`fldDCGwYvt7b8fNCj`) deletion pending operator UI action** — no Airtable MCP `delete_field` tool exists; pre-deletion grep checks all PASS (only docs references remain, no runtime consumers). Spine `recPKEhirWUWthpXS` (`principle_amendment`, sentry attribution). |
| 22.9 | INV-004 Crier stale-contact false-positive on fresh contracts | **DONE** (preventive — zero records affected today; activates when DocuSign provisioning lands) | MEDIUM-HIGH | (CODE 5/20.) Audit `docs/investigations/Crier_Stale_Contract_Audit_v1.md` (commit `3b63efe`) established Q1 false-positive count today = 0 (no record has `Envelope_ID` populated; Phase 12.7 DocuSign JWT provisioning is operator-external STOP) — but architecture is structurally vulnerable; ~33 of 43 active Negotiating/Response Received records are currently Crier-silence-flagged with no contract-state signal to distinguish them. Resolution shipped Option D (Hybrid): (1) Option A guard — `lib/maverick/deal-commentary.ts` gains exported pure `isUnderContract(listing)` helper + Crier silence branch wrapped in `!isUnderContract(listing)` guard + `DealCommentaryListing` interface gains `envelopeId`; (2) Pulse `stale-data-drift.ts` inherits the same guard via cross-module import + closes a parallel Phase 11.2 regression (`mostRecentTouchMs` extended from 2-field to 4-field parity with Crier's `latestContactIso`); (3) `app/pipeline/[id]/page.tsx` projection updated to pass `envelopeId`; (4) Option B phase-aware ladder documented in `AKB_Belt_v1_Spec.md §6` with 4 prerequisites for Phase 14 implementation (DocuSign JWT live, Listings_V1↔Deals join hardened, Deals workflow consistent, EMD signals populated for prior 5 contracted deals). 9 new unit tests (3 `isUnderContract` + 4 contract-state guard + 2 Pulse parity), full suite 990/990 passing, `npx tsc --noEmit` clean. Spine `rec0A9ZWSMMT5Nk9a` (`principle_amendment`, crier attribution, related to `recOry0VQVddhEWFb` Phase 11.2 max() staleness math — INV-004 is the next layer of the same fix family). Behavioral impact today: zero records affected; preventive infrastructure activates the moment DocuSign provisioning lands. |
| 22.10 | INV-006 Outreach_Status auto-transition on signed contracts | **DONE** (preventive — zero records affected today; activates when DocuSign provisioning lands) | MEDIUM-HIGH | (CODE 5/20.) Audit `docs/investigations/Outreach_Status_Transition_Audit_v1.md` (commit `a2ba60f`) established INV-006 as the **cure** to INV-004's **patch**: INV-004 added a runtime guard so Crier silence is suppressed when Envelope_ID is set; INV-006 makes the status field itself transition automatically so consumers see correct state without depending on the guard. Resolution shipped Option D (Hybrid): (1) **Cron reconciler** `/api/cron/outreach-status-reconcile` (daily 14:00 UTC slot, no collision with existing crons) — scans Listings_V1 for `Envelope_ID populated AND Outreach_Status ∈ {Negotiating, Response Received} AND Notes does NOT contain idempotency marker`; transitions matches to `Offer Accepted` with appended Notes line + per-record audit + per-record Spine entry; (2) Pure helper `lib/maverick/outreach-status-reconcile.ts` (RECONCILE_IDEMPOTENCY_MARKER constant + `shouldAutoTransition(listing)` returns `{action, reason}` with 4 stable reason codes + `notesContainMarker` + `buildAuditNoteLine`); (3) Belt v1 Spec §6 — new "Phase 12.7 — In-process Outreach_Status transition (INV-006 sequel)" subsection documents the envelope-create-handler wiring that will land alongside Phase 12.7 DocuSign provisioning. Cron stays as belt-and-suspenders for direct-DocuSign-UI envelopes; reverse-transition handling deferred to Phase 13+ DocuSign-webhook scope. **Option A (Airtable native automation) explicitly rejected** by Master Checklist Rule 10 + Belt v1 §6 audit-trail principle (opaque to Spine, no audit log). 10 new unit tests covering all transition + skip paths + idempotency roundtrip, full suite 1000/1000 passing, `npx tsc --noEmit` clean. Spine `recmiFJLja4cHrZ3O` (`principle_amendment`, crier attribution, related to `rec0A9ZWSMMT5Nk9a` INV-004 patch). Behavioral impact today: zero records affected (no Envelope_ID populated); preventive infrastructure activates the moment DocuSign provisioning lands. Operator-override discipline preserved via Notes-marker idempotency. |

---

## Phase 23 — Investigation Queue Discipline

**INSERTED 2026-05-20** BECAUSE the parallel-track investigation queue emerged as a new mode of work this session and needs explicit codification in the master narrative. The queue itself lives at `docs/investigations/Active_Queue.md`; this phase records the discipline that governs it.

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 23.1 | `docs/investigations/Active_Queue.md` created — live tracking surface for forensic investigations | **DONE** | HIGH | (CODE 5/20.) Created commit `8dd15fe`. Single-file living artifact with sections: Active / Queued / Discovered during prior investigations / Discipline notes. Co-exists with master checklist per rule 10 above. Current state: 16 numbered queue items, 1 unnumbered "Discovered" entry (`scorePropertyMatch` unit-test coverage gap). |
| 23.2 | Naming discipline established (temporary local numbering → operator promotion to formal INV-NNN) | **DONE** | HIGH | (CODE 5/20.) Footer rule in `Active_Queue.md`: "When an investigation surfaces new findings during its work, Code adds them to 'Discovered during prior investigations' using temporary local numbering. Operator promotes them to formal queue items with assigned INV-NNN numbers in the same commit as the next queue update. Prevents numbering collisions; queue file is single source of truth for INV identifiers." Exercised three times this session (INV-004/5/6 candidates → INV-011/12/13 promoted; INV-014/15/16 candidates → INV-014/15/16 promoted). |
| 23.3 | Backfill policy codified (pre-Inevitable Crawler 1.0 records NOT backfilled by remediations) | **DONE** | HIGH | (CODE 5/20.) Footer rule in `Active_Queue.md`: "Pre-Inevitable Crawler 1.0 records (created by prior system iterations) are NOT backfilled by remediations. Forward-going behavior changes apply to records created after the remediation date. Historical records age out via normal deal lifecycle. Purging/archiving pre-Inevitable records is a separate operator decision flagged for future consideration." Applied this session to INV-002 (Listing_Condition forward-only writes) and is the operating principle for any future Comms_Attribution rollout per Attribution Layer v1 Spec §6. |
| 23.4 | Three remediations shipped this session via audit-discover-decide-remediate cycle | **DONE** | HIGH | (CODE 5/20.) Distress_Pass Option α (Spine `rece3J2f0TbHXOuE2`), Listing_Condition Option A (Spine `recOB75kGmHzkPgKr`), INV-007 Step 1 (Spine `rec5UVLsXktD8M8Mi`). Each: audit report → operator decision → remediation commit with `§N Remediation outcome` appendix added to the audit report. Cycle is repeatable; future investigations follow the same pattern. |
| 23.5 | Sixteen-item queue with thirteen active investigations queued for future briefs | **STATUS** | — | (CODE 5/20.) Queue state at session close: INV-001 ✅ shipped, INV-002 ✅ shipped, INV-007 ✅ Step 1 shipped + Step 2 designed. Remaining 13 items queued (INV-003, INV-004 through INV-016 per current numbering): 3 from prior session brief, 7 from operator dashboard review 5/20, 3 from Code-discovered + promoted. Each is a forensic-investigation candidate; sequencing is operator-decided in future briefs. |

---

## Phase 24 — Sentinel Autonomy: ZIP Registry & Market-Expansion Approval Gate (D1)

**INSERTED 2026-05-27** BECAUSE Workstream D1 makes ZIP targeting data-driven (replacing the `CRAWLER_TARGET_ZIPS` env) and adds an operator approval gate before a new market goes live. **Naming drift logged:** the operator's D1 brief used "Phase 14" sub-numbering (14.1–14.5) for these Sentinel-autonomy deliverables, but Phase 14 is the Pulse build and rows 14.1–14.12 are already occupied. Per the same convention applied to Phase 13's 13.7–13.9 (Commit N) and Phase 14's own 14.7–14.12 (Commit O), these land as a new phase with next-available numbering. Brief→doc mapping: **14.1→24.1, 14.2→24.2, 14.3→24.3, 14.4→24.4, 14.5→24.5.** Refs Spine `recGtpPH4YxvUL2V8` (approval-gate model) + `recTOr9pk1oQ1zfhB` (D scoping). D2/D3/D4 remain out of scope (see brief §8).

| # | Item | Status | Severity | Notes |
|---|------|--------|----------|-------|
| 24.1 | `ZIP_Registry` Airtable table created + seeded | **DONE** | HIGH | (CODE 5/27.) Table `tbljGMYeY3GcOZza1` created in base `appp8inLAGTg4qpEZ` via Airtable MCP (17 fields). Seeded 24 ZIPs at `Market_Tier=active`: San Antonio TX (78201/78202/78207/78210/78211/78225/78237), Dallas TX (75216/75217/75228), Houston TX (77021/77028/77048/77087/77033/77051/77053/77045), Memphis TN (38109/38114/38116/38118/38127/38128, all `Memphis_Assignment_Required=true`). **Spec deviation (operator-approved):** `Wholesale_Restricted` + `Memphis_Assignment_Required` ship as CODE-MAINTAINED boolean checkboxes, not Airtable formulas — the Meta API/MCP can't reliably create formula-type fields. `lib/zip-registry.ts` `deriveWholesaleRestricted`/`deriveMemphisRequired` (single source of truth, set by seed + intake cron from State). `Accept_Rate_30d`/`Saturation_Threshold` are percent fields stored as fractions (0.01 = 1%); threshold seeded 0.01. |
| 24.2 | `listings-intake` reads ZIP_Registry (replaces `CRAWLER_TARGET_ZIPS`) | **DONE** | HIGH | (CODE 5/27.) `/api/cron/listings-intake` reads `getActiveIntakeRows()` (Market_Tier ∈ {launch, active} AND NOT Wholesale_Restricted) instead of the env. Manual `?zips=`/`?zip_override=` still bypasses the registry for per-ZIP runs. RentCast fetch now batched 3-concurrent (`Promise.all`) — note the expensive step (Firecrawl verify) was already 20-way pooled, so the batch is on the cheap fetch loop; dedup Sets stay race-free (sync processing after each batch resolves). Per-ZIP stats write-back on live runs: `Last_Ingested_At`, `Accept_Rate_30d`, `Avg_DOM`, `Avg_List_Price`, `Records_Ingested_30d`. **Snapshot semantics:** v1 writes the latest run's figures into the `_30d` fields; true 30-day rolling aggregation is owned by 24.5 (the sole consumer). |
| 24.3 | Approval-gate dashboard panel `<ZIPApprovalQueue />` | **DONE** | HIGH | (CODE 5/27.) `components/sentinel/ZIPApprovalQueue.tsx` + `app/sentinel/zip-approvals/page.tsx` read `approval_pending` via `GET /api/zip-registry/queue`. Approve → `active` (one click); Reject → `paused` (requires a note). `POST /api/zip-registry/decision` stamps `Approved_By` + `Approval_Method="dashboard"` and appends a Spine row (refs `recGtpPH4YxvUL2V8`). Mirrors the `SentinelApprovalQueue` shape. |
| 24.4 | SMS approval path (poll-cron option) | **DONE** | HIGH | (CODE 5/27.) **Option chosen: frequent poll cron** (not the brief's Option A daily / Option B Airtable-webhook). Rationale: inbound OpenPhone is polled (no inbound webhook exists), and Vercel **Pro** now permits sub-daily crons — so a 15-min cron gives a near-real-time round-trip fully in-repo with no external Airtable automation. `/api/cron/zip-approval-reply-scan` (`*/15 * * * *`): (1) notify-sweep SMS's pending ZIPs not yet notified (idempotent via `Approval_Notified_Channels`); (2) reply-sweep polls the operator cell (`MAVERICK_STAGE4_SMS_TARGET`, default `+16302505865`), parses `YES/NO [ZIP]` via the strict `parseZipApprovalReply` parser (12 tests), flips state with `Approval_Method="sms"`, logs Spine. Outbound also fires in-code at any state flip via `notifyZipApprovalPending`. |
| 24.5 | Saturation auto-detection | **DEFERRED (follow-up PR)** | MEDIUM | (Operator-confirmed follow-up.) Daily cron: active ZIPs where `Accept_Rate_30d < Saturation_Threshold` for 14 consecutive days flip to `saturated` + surface a Pulse expansion suggestion. Not in this PR. Requires a true 30-day rolling accept-rate store (24.2 writes per-run snapshots until then) and a 14-day-streak persistence mechanism. Pulse surfacing is D3 (out of scope). |

---

## Insertion log

| Date | Item # | Reason |
|------|--------|--------|
| 2026-05-31 | **A1 — System Facts vault** (Spine `recpLB1yC1SaDTqff` ship + go-live build_event paired this cycle) | **DONE + LIVE on `main` 2026-05-31** (merge commit `d36f26c`, reconcile merge `7b115a3`, ship commits `0d58e10` + `485d292`). Closes the "no canonical System Facts record" gap Maverick has flagged repeatedly at session-open (the Vercel-plan question alone re-derived incorrectly twice; immediately context-relevant after yesterday's D1 row above, where Pro was finally settled inline). **New vault:** `akb-dashboard/docs/system/SYSTEM_FACTS.md` — Vercel plan (**Pro**, team `team_zwFAlAQ8CyjGYcxyk7Sn6ww0`, project `prj_X1pCuqzRml74iOKfNhTo4ZMG9K87`), repo identity (`Golf4Life27/coach-pulse`, default branch **`main`** — codifies the deploy-truth + git-source-truth fixes), Continuity Layer spec version (v1.1 + 6.4 attribution), Airtable table IDs (base / Listings_V1 / Spine / Property_Intel / Buyers / D3), external-service identity (Quo numbers, RentCast cap, Firecrawl tier, DocuSign JWT path), named-agent roster, autonomy lanes (Type 1/2A/2B/2C), mission constants ($10K floor, 65% MAO, market cap rates, Crawler 2.0 + Dream Phase unlock thresholds). **Authority rule:** when code / AGENTS.md / Spine recall contradicts a fact in the vault, the vault wins. **Maverick wiring:** new `lib/maverick/sources/system-facts.ts` async loader (candidate-path pattern mirroring `codebase-metadata.ts` — works from both repo root and `akb-dashboard/` cwd; never throws); new `SystemFactsSection` + `EMPTY_SYSTEM_FACTS` on `StructuredBriefing`, placed FIRST in the structured payload; `lib/maverick/aggregator.ts` fires the loader in parallel with the SourceResult fetchers and resolves before composition; read failure → `staleness_warnings.unshift("system_facts: ...")`, never blocks the briefing; `lib/maverick/template.ts` renders `## SYSTEM FACTS (authoritative — docs/system/SYSTEM_FACTS.md)` immediately after the welcome line and before `## CURRENT BUILD STATE`; `lib/maverick/synthesize.ts` SYSTEM_PROMPT amended with a new **Rule 0** making vault facts authoritative over source-fetched data, recent_key_decisions, and prior training, with explicit guidance to lead the briefing on any vault fact that resolves an ambient session ambiguity. **Hygiene:** new top-level `.gitattributes` (`*.md text eol=lf` — no prior EOL policy or `.editorconfig` to reconcile against on this branch); `.obsidian/` + `**/.obsidian/` appended to outer `.gitignore` (Obsidian workspace metadata, per-user, never source of truth). **Stale-info flagged for next legitimate edit, not changed in this ship:** `akb-dashboard/AGENTS.md` still describes "Vercel Hobby plan — once per day maximum"; the vault supersedes it but the file should be amended on next touch (yesterday's D1 row already flagged this). **Tests:** +6 — `sources/system-facts.test.ts` (both cwd layouts, missing-file → error-not-throw, smoke against the committed vault anchoring the "Pro" fact); `template.test.ts` (System Facts renders FIRST, omitted entirely when markdown is null). All test fixtures that construct full `StructuredBriefing` (template / synthesize / handlers) now seed `system_facts: EMPTY_SYSTEM_FACTS`. 1436/1436; tsc clean. **Active_Queue.md:** new top-level "Build discipline (A-series)" entry recording A1 → DONE this cycle. Own branch (`claude/bold-meitner-l1VGm`, off the D1 ZIP_Registry ship). |
| 2026-05-28 | Re-verify H2 Auto-Proceed queue before first live fire (Spine recwkHvBMTjeMLECp sibling) | **DONE — re-verify applied (operator reviewed the prod dry run, directed the apply). 36 demoted to Review; eligible 57→20; first-touch 17→13. H2_OUTREACH_LIVE stays false (operator's live call).** (Sibling to renovation-hard-veto commit 66d82a3.) **Queue audit (live Listings_V1 via Airtable MCP, base appp8inLAGTg4qpEZ):** eligible = **57**, exactly matching today's dry-run (581 Auto-Proceed+Active → 412 already-statused, 111 Do-Not-Text, 1 no-phone → 57). **Dedup DONE (1 write):** the real within-eligible duplicate is **1803 Mardell** (`recYvNnYb76jCaquj` + `recuDwRvc0CYfpsbY` — both v2, both eligible, same agent Coy Tackitt / same $91K MAO, DIFFERENT phones → planQueue's phone-key would NOT stall them → same listing textable twice). Retired `recuDwRvc0CYfpsbY` (Outreach_Status→Dead + provenance note); kept `recYvNnYb76jCaquj` canonical (fuller address; phone 2103259807 matches the agent's 942 W Lynwood listing; carries RentCast intake note). Eligible 57→**56**. **Brief premise corrected:** the brief's 1402 Mardell pair (`recNRBSkiifK7G0RP` v2 + `recKQRgb5Flidk6bi`) IS a real same-property/same-phone dup, but `recKQRgb5Flidk6bi` is **v1_legacy** — already excluded from H2 by the Source_Version backstop, so NOT a live-fire risk; left untouched per operator (scope = 1803 only). **RE-VERIFY BLOCKED (core safety step):** Firecrawl verify + `classifyVerifiedListing` can't run in this container — no `FIRECRAWL_API_KEY`, and the classifier consumes only live-scrape signals (`stillActive`/`isNewConstruction`/`hasRenovatedLanguage`/`hasConditionSignal`) NOT persisted on records (`Listing_Condition` empty on all 57); running it here would false-reject all 57 as `firecrawl_not_configured`. **The chosen fallback (re-run deployed `/api/cron/listings-intake` dry-run) does NOT cover this** — that cron verifies NEW RentCast intake and dedups against known addresses ("never scrape a known address"), so it skips the existing 57. No current endpoint re-verifies an in-Airtable queue. Renovated/turnkey/new-construction demotions (incl. whether 1402 Mardell → Review) are therefore NOT done in-container. **Route shipped:** `POST /api/admin/reverify-queue` (GET = dry; POST `{dry_run:false}` = apply, dry_run defaults TRUE) loops `selectH2Eligible` → `verifyListing(address)` (existing bounded-concurrency pool + rate gate + 270s wall-clock guard) → `classifyVerifiedListing` → pure `planRequalification` (`lib/admin/reverify-queue.ts`): renovated/turnkey/new-construction/condition-missing → **Review**; inactive → **Off Market**; clean distress → **keep**; **Firecrawl INFRA failure → skip, NEVER demote** (safety invariant, unit-tested). Batched ≤10, typecast via patchAndVerify, formula fields untouched, demotions carry a Verification_Notes provenance line and are reversible. +9 pure tests; tsc clean; full suite 1423/1423. **APPLIED 2026-05-28** (operator ran the prod DRY run, reviewed it, and supplied the per-record demotion list; writes executed via Airtable MCP — prod auth not reachable from the build container): **36 of 56 eligible demoted to Review** (25 `firecrawl_renovated` + 11 `condition_signal_missing_flagged`), each with a reversible Verification_Notes provenance line (Outreach_Status→Review; intake notes preserved; formula fields untouched; batched ≤10, typecast). The **1 `skip_unverified`** (`recN6e8Is7nmIkQDK`, 1932 E Highland — Firecrawl infra failure, NOT a quality signal) and the **19 `keep`** were left untouched. **Post-apply H2 dry-run** (re-derived via the repo's real `selectH2Eligible`+`planQueue`+`normalizePhone` over all 3,644 records): **eligible 20** (19 keep + 1 skip), **first-touch 13**, prior_contact_stall 7. **False-positive spot-check resolved externally vs live MLS** — 1023 Sacramento (rehabbed 2020/21 turnkey, 182 DOM) + 1026 W Lynwood (updated, move-in-ready) are **true-positive renovations; scoping fix holds, no leak.** **H2_OUTREACH_LIVE NOT flipped** — operator makes the live call. **Observation (not actioned, out of scope):** two first-touch rows look like a same-property dup — `recZcEOdSzGeBv0k8` "1610 22nd" + `recpLIHAMpnTvEq3K` "1610 Nw 22nd St" (both MAO $61,750, distinct phones so planQueue won't stall them); flag for a dedup pass. |
| 2026-05-28 | Maverick deploy-truth (Spine recwkHvBMTjeMLECp) | **DONE** per Rule 9 — opened + closed in the same commit. **Bug:** Maverick reported a PR *preview* deploy as if it were production (cost ~40 min on launch morning). Confirmed in source on `main` @ 468d5f3, not inferred. `lib/maverick/sources/external-vercel.ts` queried `/v6/deployments` with `target=""` (any target) `limit=1` and took `[0]` = the single newest deploy of ANY branch/target; when the newest push was a preview, that preview's SHA rendered under `template.ts`'s literal "Production deploy:" label. A comment claimed a client-side branch filter that **was never implemented**, and `ACTIVE_BRANCH` defaulted to a dead branch. **Fix (root cause, not selector bandaid):** query `target=production&state=READY&limit=1` — Vercel now returns only the live production deploy server-side, so `[0]` can never be a preview. Extracted pure `buildProductionDeploymentsUrl()` (exported, unit-tested) as the anti-regression guard. Renamed the surfaced semantics `latest_deploy_*` → `production_deploy_*` across the interface + `summarizeDeployment` + `aggregator.ts` mapping + `briefing.ts` EMPTY/interface-ref + both `template.ts` render sites + 6 test fixtures, so the data name matches every consumer's "what's live" treatment. `behind_head` is now correct-and-useful for free (HEAD-of-main vs the production SHA). `ACTIVE_BRANCH` default `claude/build-akb-inevitable-week1-uG6xD` → `main` (note: the field it feeds, `active_branch_observed`, has **zero readers** — vestigial, flagged for follow-up removal). New tests: query-contract (target/state/limit) + incident regression fixture using the real SHAs (684fba4 preview must NOT win; 468d5f3 prod must). tsc clean; full suite 1411/1411. Shipped on its own branch off `main` (independent of the #13–#16 stack per operator). **Out of scope / flagged:** `lib/maverick/sources/git.ts` carries the **same** dead-branch default (`claude/build-akb-inevitable-week1-uG6xD`) and actively uses it to query commits (`sha=ACTIVE_BRANCH`) — a likely second truth bug (git source querying a dead branch), left untouched under this brief and surfaced for operator direction. H2 cron 401 also out of scope (separate auth finding). |
| 2026-05-28 | Maverick git-source-truth (Spine recwkHvBMTjeMLECp) | **DONE** per Rule 9 — opened + closed in the same commit. Closes the sibling bug **flagged** in the deploy-truth row above. `lib/maverick/sources/git.ts` `ACTIVE_BRANCH` defaulted to `claude/build-akb-inevitable-week1-uG6xD`, **confirmed deleted from origin** (`git ls-remote` empty) — and unlike external-vercel's vestigial field, git.ts **actively uses it**: `commitsUrl.searchParams.set("sha", ACTIVE_BRANCH)`. Absent the env override, `/commits?sha=<dead-branch>` 404s → blank git data + `branch_resolved:false` → `behind_head` silently `null` (Maverick reports no commits / can't compute deploy-lag). `MAVERICK_ACTIVE_BRANCH=main` was set in prod as interim mitigation; **this fix removes the dependency on that env** by defaulting to `main`. Mirrored the deploy-truth pattern: extracted pure exported `buildCommitsUrl(sinceIso, branch=ACTIVE_BRANCH, …)` and wired `fetchGitState` through it. New tests in `git.test.ts`: query-contract (sha defaults to `main` when env unset; explicit override honored; repo path + since/per_page). tsc clean; full suite 1414/1414 (+3). Own branch off `main`, independent of the #13–#16 stack. **Still open (separate):** H2 cron 401 prod auth finding. |
| 2026-05-27 | 24 (entire phase) | Workstream D1 (Sentinel autonomy — ZIP Registry + market-expansion approval gate). New phase per Rule 3. **Naming drift:** operator's D1 brief reused "Phase 14" sub-numbering (14.1–14.5); Phase 14 (Pulse) rows 14.1–14.12 are occupied, so D1 lands as Phase 24 with 14.x→24.x mapping (same convention as 13.7–13.9 Commit N + 14.7–14.12 Commit O). Ships 24.1–24.4 (table+seed, intake-registry-read, dashboard gate, SMS poll-cron); 24.5 (saturation) deferred to a follow-up per operator. **Infra note:** operator confirmed Vercel **Pro** this session — the `AGENTS.md` Hobby daily-cron cap (and the 5/17 "Hobby blocks sub-daily" note) no longer binds; the 15-min `zip-approval-reply-scan` cron relies on Pro. **Spec deviation:** two derived booleans are code-maintained, not Airtable formulas (Meta API limitation). Refs Spine `recGtpPH4YxvUL2V8` + `recTOr9pk1oQ1zfhB`. |
| 2026-05-20 | 22.10 | INV-006 (Outreach_Status auto-transition on signed contracts) resolution shipped per Rule 9 — Option D (Hybrid). Cron reconciler `/api/cron/outreach-status-reconcile` daily 14:00 UTC + pure helper `lib/maverick/outreach-status-reconcile.ts` + Belt v1 Spec §6 Phase 12.7 in-process sequel documented. Option A (Airtable native automation) rejected by Master Checklist Rule 10. 10 new unit tests, full suite 1000/1000 passing. Spine `recmiFJLja4cHrZ3O` (related to `rec0A9ZWSMMT5Nk9a` INV-004 patch — INV-006 is the cure). Zero records affected today; activates when DocuSign provisioning lands. Audit + §9 remediation outcome in `docs/investigations/Outreach_Status_Transition_Audit_v1.md`. |
| 2026-05-20 | 22.9 | INV-004 (Crier stale-contact false-positive) resolution shipped per Rule 9 — Option D (Hybrid). `isUnderContract(listing)` exported from deal-commentary.ts gates Crier silence branch on `Envelope_ID`. Pulse `stale-data-drift` inherits guard + Phase 11.2 4-field parity. Belt v1 Spec §6 documents Phase 14 Crier phase-aware ladder. 9 new unit tests, full suite 990/990 passing. Zero records affected today (preventive); activates when DocuSign provisioning lands. Spine `rec0A9ZWSMMT5Nk9a` (related to `recOry0VQVddhEWFb` Phase 11.2). Audit + §9 remediation outcome in `docs/investigations/Crier_Stale_Contract_Audit_v1.md`. |
| 2026-05-20 | 22.8 | INV-003 (Stage_Calc V1 vs V2) resolution shipped per Rule 9 — V2 formula updated (MLS-date gate added at top), Execution_Path_Calc updated (LEFT prefix match routes Data Issue → Manual Review). V1 field deletion pending operator UI action (no MCP `delete_field` tool). Spine `recPKEhirWUWthpXS`. Audit + remediation outcome in `docs/investigations/Stage_Calc_V1_V2_Audit_v1.md` §9. |
| 2026-05-20 | 22, 23, rules 9-10, 1.2-1.7, 2.2, 2.10 | Post Map-1 reconciliation. Two new phases land (22 — Belt-Era Foundation; 23 — Investigation Queue Discipline). Rules of engagement gain rule 9 (Checklist update required directive) + rule 10 (queue/checklist co-existence); existing rule 9 → 11. Today's session work absorbed: 3 remediations shipped (Distress_Pass α, Listing_Condition A, INV-007 Step 1), 2 architectural specs committed (Belt v1, Attribution Layer v1), Firecrawl validated end-to-end. Belt v1 Spec design decisions cascade to Phase 1 / Phase 2: Scenario A (1.2), Scenario B + fixes (1.3-1.7), H2 (2.2) all marked SKIPPED-REPLACED with cross-references to the spec. Phase 2.10 Make blueprint API gotcha updated — operator approved API-push discipline; first push exercised on Scenario A this session (Listing_Condition mapping add). v1.2 doc version. |
| 2026-05-19 | 21 | Strategic Backlog section added per Map 1 closing directive. Captures revenue-unlocking external operator-fire items, scaffold-live-credential-gated items, Map 2 substrate migration roadmap, and orphan cleanup items. |
| 2026-05-15 | 8.4 | Alex's "what fills the funnel" question revealed Sentinel-vocabulary-vs-reality gap. Code must inventory intake state in audit |
| 2026-05-15 | 8.5 | This checklist itself needs Code's repo-grounded audit to fill in real status per item. Inventory doc is the vehicle |
| 2026-05-15 | 13 (entire phase) | Sentinel build separated from generic "agent build" because funnel autonomy is foundational to wife retirement |
| 2026-05-15 | 19.8 | Doc generation discipline rule. The "7 billion documents" failure mode gets blocked at the gate |
| 2026-05-15 | 20.1 | Make.com migration vs retention question raised but no clear prior decision found. Captured as open question blocking Phase 8 audit until resolved |
| 2026-05-16 | 3.11a | Phase 4D — Unified Deal Math endpoint distinct from per-record pricing route (Phase 4 partial-build correction) |
| 2026-05-16 | 3.11b | Phase 4E — Two-Track BroCard rendering exists in components but not anchored to Daily UX Spec priority surface (Phase 4 partial-build correction) |
| 2026-05-16 | 9.3 | Named-agent vocabulary in code does not exist. Specs canonical, attribution mismatched. Must precede Phase 9.4 + Phase 13 to maintain coherence |
| 2026-05-16 | 9.11 | Jarvis→Maverick rename of legacy components (JarvisChat/JarvisFeed/JarvisGreeting + 4 jarvis-* API routes). Pre-Maverick naming per Code Briefing §9 |
| 2026-05-16 | 12.6 | Airtable concurrent-source contention causing live Gate 5 sample over-target (Day 1's pattern reproducing under load) |
| 2026-05-16 | 20.2 | Stored_Offer_Price semantics ambiguity surfaced during Phase 11.4 implementation. Logged as v1.3 spec sub-question. |
| 2026-05-16 | 20.3 | Dashboard browser fetch is a fourth auth surface beyond OAuth/CRON/bearer. Surfaced during Commit B live deploy when Shepherd panel hit load-state 401. RESOLVED-IN-PATCH (Commit B.1) via same-origin cookie path; v1.3 spec to fold into canonical auth model. |
| 2026-05-17 | 11.6 | 48hr unattended Sat-Sun window burned ~4.8M tokens via paired aggregator+synthesizer calls every ~6 min. Cron inventory confirmed `vercel.json` is clean (5 daily crons, none hit load-state; Hobby plan blocks sub-daily). Source of cadence unidentified; surviving hypotheses logged. `MAVERICK_CRON_ENABLED` gate (default false) + Phase 14 breadcrumb shipped Commit B.2. |
| 2026-05-17 | 20.5 | Quota-burn anomaly detection raised as architectural requirement for Pulse. The 5/17 burn was the exact failure mode Phase 14 exists to prevent and proves Pulse's scope must extend to internal-synthesis cost (not just external-API quotas). Owner: Phase 14 Pulse build. |
| 2026-05-17 | 11.7 | Browser polling visibility-state convention. Phase 11.6 cron gate did not cover backgrounded dashboard tabs (auth as `dashboard_session`, not `cron`). Convention spec'd in `Maverick_Daily_UX_Spec_v1.md §10.5`; canonical helper `lib/maverick/visibility-polling.ts`. ShepherdPanel migrated as the reference implementation. Mandatory for Phase 9.4+ client polling components. |
| 2026-05-18 | 12.3 | Alex provisioned `+16302505865` in the Quo workspace as Maverick's personal escalation number. A2P 10DLC registration pending at carrier (1-7 day window). Unblocks Phase 9.7 SMS escalation build — Commit E lands the code today against this target. |
| 2026-05-18 | 11.2 | Commit F lands the `Last_Email_Outreach_Date` schema + max() staleness math. Airtable field created via MCP (`fld4Jzjs8etKact6g` on Listings_V1). Attribution gap documented: only programmatic `sendEmail` calls with `listingRecordId` set populate the field; mailto: manual sends from the deal-detail page cannot write back (Phase 13/Sentinel-adjacent — out of scope). 23 Fields regression test locked in. |
| 2026-05-18 | 5.9 / 5.10 / 5.11 / 12.7 | Commit G lands Phase 5 Scribe. Three sub-commits: G.1 foundation (`lib/docusign.ts` JWT client + new 10th briefing source + 22 tests), G.2 ScribeRoom on factory floor, G.3 Envelope_ID field + deal-detail panel + send-reminder/track-envelope routes. Architectural decision logged: production uses Path A (JWT via DocuSign REST API directly) rather than MCP, because MCP is a Claude-side protocol unreachable from Vercel functions. Live data gated on Phase 12.7 credential provisioning (Alex's task in DocuSign Admin Console). Old "MCP server UUID" string `ab943441-...` corrected — that was Alex's account_id all along. |
| 2026-05-18 | 20.7 | Continuity-layer drift surfaced when an Alex Claude session opened with maverick_load_state referencing a 2-week-stale branch and maverick_recall returning zero for tonight's work. H1 (deployment): Vercel production target is `dpl_4peQ2dTrnWM34bJrE3XqJY3Sd69Y` (sha 33341a3e, main, May 12); every Phase 9-11-12-5 commit shipped as preview only. H2 (discipline): no maverick_write_state calls had been made since 5/16; Spine_Decision_Log had zero entries for ~14 commits of work. H2 fixed this session via 10 backfill build_event writes. H1 fix path: Alex repoints connector to `coach-pulse-git-claude-fix-token-b-efd89f-golf4life27s-projects.vercel.app` OR merges branch to main. Future Code commits include a write_state call as ritual. |
| 2026-05-18 | 20.2 / 3.2 / 3.11a / 12.8 | Commit H — Phase 20.2 closed via two-field split (Outreach_Offer_Price sticky 65%-of-List at door-opener time; Contract_Offer_Price operative offer at negotiation/DD, can be above or below outreach). Airtable: rename `Stored_Offer_Price` → `Outreach_Offer_Price` (field id `fldBFnL0HQJWahRov` preserved, data carried); new `Contract_Offer_Price` (`fldfJWuEIHqaRuWq3`) + `Seller_Motivation_Score` (`fldfEVJijfPOBulpc`). Phase 4D endpoint amendment to return a range `[V2.1 floor, motivation-adjusted target]`. Buyer_Tx_Median → Buyer_Median doc-only correction (zero live code references; Make scenarios + Constitution.docx flagged as Phase 12.8 manual follow-up). 523/523 tests. Orphan `_orphan_outreach_offer_price_5_18` (fldhl0njOHREJQ6Gd) left in Airtable for manual UI deletion. |
| 2026-05-18 | 12.4 / 12.8 / 12.9 / 4A.1 | Cleanup sprint + Commit I (Phase 4A.1 ARV endpoint). **Cleanup:** H2 (4724197) module 3 patched via Make MCP to snapshot Outreach_Offer_Price = {{1.MAO_V1}} at SMS send (Path A "Snapshot-at-send" per Alex's 5/18 decision; verified post-push isinvalid:false). Anthropic key rotated by Alex (Console + Make I); Vercel uses separate Jarvis key (no env update needed). J (4724499) + L2 (4812267) deleted via Make MCP. New Phase 12.9 row added for the OTHER Make blueprint secrets surfaced during the audit (Scenario I ScraperAPI key + H2 OpenPhone key hardcoded). **Commit I:** standalone Appraiser ARV endpoint `/api/agents/appraiser/arv/[recordId]` shipped across three sub-commits — I.1 (`9471dc7`) endpoint + `lib/appraiser/mao-range.ts` pure helpers + 18 tests including the 1219 E Highland $90K validation anchor; I.2+I.3 (`e4e281b`) briefing aggregator wire (ListingsActiveDeal gains 5 ARV fields) + Appraiser room ARV coverage rollup + deal-detail ARV panel with comps table. Phase 4A.1 → DONE; v1.3 range envelope `{floor, target, list_price, soft_ceiling, exceeds_soft_ceiling, modifier_inputs}` lands here. 541/541 tests passing. Phase 19.8 Spine wrap via direct Airtable fallback (Maverick MCP intermittently disconnected): Spine rows `recoZqd3zTklediUO` (cleanup) + `recCqTItoptQ3L8dL` (Phase 4A.1). |
| 2026-05-18 | 4C.1 | Commit K — Phase 4C.1 Buyer Intelligence Dual-Track. Four sub-commits: K.1 (`956cb25`) endpoint + `lib/appraiser/buyer-intelligence.ts` pure helpers (per-market cap rates TX 8% / TN 10% / MI 9% / Default 9%, env-overridable via BUYER_CAP_RATE_*; flipper + landlord MAO formulas; tie threshold $1000) + 26 tests with per-market fixtures + 1219 E Highland cross-check ($165K ARV + $1400 rent → landlord $135K beats flipper $90K) + new Airtable field `Estimated_Monthly_Rent` (`fldrFB0owY6BnQewr`); K.2 (`f416814`) briefing wire (ListingsActiveDeal +5 dual-track fields computed inline via summarizeListings → computeDualTrack) + Appraiser room Track Mix section (landlord_dominant count highlighted as "money on the table" signal; tier 1 override when any landlord-dominant) + AppraiserBuyerIntelligencePanel deal-detail UI showing both tracks side-by-side with dominant ★-marked; K.3 (`a84e93c`) MAO-range integration — `computeMaoRange` gains optional monthlyRent + state inputs; when both present, runs dual-track internally and uses dominant_value as floor (backward compat preserved for callers without rent/state); output gains `dual_track` sub-payload + `modifier_inputs.monthly_rent` + `state`; K.4 (THIS) Checklist wrap. 608/608 tests passing. Phase 19.8 Spine wrap via direct Airtable fallback (Maverick MCP disconnected). |
| 2026-05-18 | 4B.1 | Commit J — Phase 4B.1 Appraiser Rehab Calibration endpoint. Four sub-commits: J.1 (`a3243f6`) endpoint + `lib/appraiser/rehab-calibration.ts` pure helpers (BBC 5-tier × market multiplier per Bible v3 §4.2: Cosmetic $15 / Light $22 / Medium $30 / Heavy $50 / Gut $70 per sqft × TX-Metro 1.00 / TN-Distressed 0.90 / MI-Distressed 0.85 / Conservative-Default 1.10) + 28 tests with per-market validation fixtures (SA Medium $45K, Memphis Heavy $67.5K, Detroit Gut $89.25K, anywhere-else Medium $49.5K); J.2 (`a492e39`) briefing wire (ListingsActiveDeal +6 rehab fields with freshness + BBC tier inferred from rate) + Appraiser room rehab coverage section + AppraiserRehabPanel deal-detail UI with line-items table + red-flag chips + market-metadata strip; J.3 (`9f5f479`) MAO-range integration via new `pickCalibratedRehab` helper (prefers Phase 4B.1 estRehabMid over legacy estRehab; surfaces rehab_source in audit so Pulse can detect MAO floors still on legacy data); J.4 (THIS) Checklist wrap. Multipliers env-overridable via REHAB_MULT_TX_METRO / REHAB_MULT_TN_DISTRESSED / REHAB_MULT_MI_DISTRESSED / REHAB_MULT_DEFAULT. 576/576 tests passing. Phase 19.8 Spine wrap pending (Maverick MCP status check). |
| 2026-05-18 | 4D / 3.11b | Commit L — Phase 4D BroCard Two-Track Rendering. Four sub-commits: L.1 (`6638120`) `BroCardPricing` discriminated union (phase4 / legacy / no_math) on the BroCard interface + pure `classifyBroCardPricing` helper in `lib/brocard/pricing.ts` + jarvis-brief route preserves raw Listing on RankedDeal and attaches pricing post-LLM (fallback path included) + three-mode `components/brocard/PricingBlock.tsx` + wired into `CardBlock` in `JarvisGreeting.tsx` (the Daily UX Spec priority surface, Phase 9.x); L.2 (`0b229e0`) visual polish — three-stop range bar (floor → soft-ceiling at 75% of list → list, with target stacked on floor until Phase 13 motivation scoring separates them) with soft-ceiling marker flipping to caution amber + ⚠ when target exceeds it + expand-on-click modifier-inputs panel surfacing ARV / Rehab (+ "calibrated" emerald or "legacy" amber source badge per rehab_source) / Wholesale fee / Buyer profit / Rent / Cap rate + tier / State / Motivation + `cap_rate` and `cap_rate_tier` bubbled up onto `BroCardPricingPhase4.range.dual_track` so the tooltip surfaces them without re-running computeDualTrack client-side + `BroCardMarketTier` string-union inlined per the existing types/jarvis convention; L.3 (`3f78bbf`) 25 pure-function tests across the four render-mode states with the 1219 E Highland canonical anchor ($165K ARV + $60K rehab + $1400/mo rent + TX 8% cap → flipper $90K, landlord $135K dominant, soft ceiling $122,250, exceeds_soft_ceiling: true) + K.1/J.1 fixtures (Memphis TN landlord $87K, Detroit MI landlord-strongly-dominant with legacy rehab badge, SA flipper-only); L.4 (THIS) Checklist wrap. **Frontend-only sprint:** no new endpoints, no new Airtable fields, no math-layer changes — the v1.3 envelope was already being produced by Phase 4A.1 + 4B.1 + 4C.1; L closes the visual gap. **Mode gate:** phase4 when computeMaoRange returns a non-null floor (covers landlord-only edge case via dual_track.dominant_value fallback); legacy when math can't produce a floor but outreachOfferPrice OR contractOfferPrice is set; no_math otherwise. 633/633 tests passing (608 → 633, +25). Phase 19.8 Spine wrap via Maverick MCP (live this session, no direct-Airtable fallback needed): Spine rows `recsFfdxK9S20nojl` (L.1) + `recVyvvadi6czoD87` (L.2) + `rechqmNUDrcPHcDKc` (L.3) + (this row's wrap will be the fourth). |
| 2026-05-18 | 16.10 | Commit M — Phase 16.x Live Exercise Backfill. Three sub-commits: M.1 (`bc16be3`) `lib/admin/appraiser-backfill.ts` pure helpers (classifyBackfillEligibility with three skip reasons: manual_review_low_arv per Phase 4A.1 gate, already_complete per ARV+Rehab+Rent timestamp trio, missing_zip; estimateBackfillCost + totalBackfillCost in API-call counts) + `app/api/admin/appraiser-backfill/route.ts` GET dry-run route reading getActiveListingsForBrief (matches briefing aggregator view) + `agent: appraiser, event: backfill_dry_run` audit entry per run so Maverick load-state surfaces backfill activity; M.2 (`ba33f12`) apply mode behind `?apply=1` with sequential per-record ARV → Rehab → Buyer Intelligence internal-fetch calls + idempotency-on-write gate inherited from classifyBackfillEligibility (overridable via `?force=1`) + env-driven pacing via `BACKFILL_PACE_MS` (default 2000ms, clamped 0-30000ms via readBackfillPaceMs) + lambda budget guard with 70s per-record budget + 10s safety buffer setting `truncated_by_budget` flag so final audit always lands + per-record audit `event: backfill_record_applied` with per-endpoint http/ms/error breakdowns + final run audit `event: backfill_apply_run` + aggregateBackfillStatus helper rolling three per-endpoint statuses into ok/partial/error + 24 pure-function tests locking the destructive-path gates (eligibility ordering, force/include_manual_review overrides, cost estimation, pace clamping, status aggregation, plus a 37-active-deal anchor); M.3 (THIS) Checklist wrap. **Auth posture:** no app-level auth on the route — same convention as every other /api/admin/* endpoint in this codebase (d3-backfill-offer-fields, bulk-dead-stale-texted, etc.). Access control lives at the Vercel deployment layer (branch preview alias is private to Alex's team). **Operator runbook:** (1) dry-run first to see eligible count + cost estimate; (2) `?apply=1&limit=10` processes ~6-10 records per lambda at default pacing; (3) iterate until `remaining_eligible: 0`. 657/657 tests passing (633 → 657, +24). Phase 19.8 Spine wrap via Maverick MCP (live): Spine rows `recPzuGocEfJN3cxZ` (M.1) + `recry8F9NjJE8wcoY` (M.2) + (this row's wrap will be the third). |
| 2026-05-19 | Map 1 closeout | Commit Q — autonomous-run closure of remaining Checklist rows. Eight sub-commits: Q.1 (`635de58`) Phase 10.4/10.5/10.6 — synthesis budget 20s → 30s, active_deals trim cap 15, cache_system: true extended to Maverick BroCard synthesizer + Sentinel classifier + Sentinel drafter. Q.2 (`9769cc0`) Phase 14.1/14.2/14.3/14.6 — `PulseDetection.confidence` field added (defaults 1.0); two new detectors (outreach-volume-drop pattern detector + quo-quota-burn provider-side); `lib/maverick/family-time.ts` Character Spec §4.6 classifier. Q.3 (`f02d813`) Phase 13.1/13.2 + 3.11a — intake-signal detector (Multi-Listing Queued backlog + active-inventory-low) + `/api/deal-math/[recordId]` canonical-namespace read endpoint returning BroCardPricing + raw range envelope + listing snapshot. Q.4 (`073721d`) Phase 15.1-15.5 Ledger MVP — `lib/ledger/economics.ts` (per-deal P&L + truck fund + retirement progress meter, env-overridable config) + `lib/ledger/cost-attribution.ts` (per-agent LLM spend from synthesizer audit events) + `/api/agents/ledger/summary` endpoint. Q.5 (`66bce17`) Phase 4.5/4.6 + 1.4/1.5/1.7 — Scout dispo-readiness queue (`lib/scout/queue.ts` + `/api/agents/scout/queue`) + Scenario B intake quality gates (off-market regex, flip-keyword scoring, agent-phone validation). Q.6 (`9f5eb30`) Phase 2.8 + 5.5 + 7.5 — Quo throttle helper (`lib/quo-throttle.ts`, 15/hour env-overridable) + EMD wire procedure doc + MAVERICK_OPS spec cross-link extended. Q.7 (`d9ac90a`) Phase 13.3/13.5/13.6 Crawler 1.0/2.0 — adapter pattern (lib/crawler/types + pipeline) + PropStream stub + 3 off-market adapter stubs (probate/tax_delinquency/code_violations) + intake-quality-gates wire-up + `/api/agents/sentinel/crawler/scan` endpoint; live integration unlocks per-credential-paste per source. Q.8 (THIS) Master Checklist closeout: 25 rows DONE-flipped, 11 rows DEFERRED with reasons, 10 rows STOP-marked (5 external + 5 strategy), Phase 21 Strategic Backlog populated with 19 items + 4 Map 2 substrate items + 4 orphan/cleanup items, Map 1 close section added. 958/958 tests passing (872 → 958, +86 across Q.1-Q.7). Phase 19.8 Spine writes via Maverick MCP per sub-commit. **Naming drift logged in Q.3:** intake-signal detector uses `detector_id: "stale_data_drift"` (closest existing PulseDetectorId) for its two fires; future migration extends the union. **Refactor charter held across all sub-commits:** no prompt changes, no behavior changes outside additive surfaces. |
| 2026-05-19 | 10.1 / 10.2 / 10.3 / 10.6 / 10.7 | Commit P — Phase 10 synthesizer refactor. Four sub-commits: P.1 (`928f719`) `lib/maverick/voice-registry.ts` with 13 entries (maverick / briefing / shepherd / crier / appraiser / scribe / sentinel / scout / forge / sentry / pulse / ledger / agent_context) — model + max_tokens + temperature + Character Spec voice_fragment per agent + MODEL_SONNET_4_6 / MODEL_SONNET_4_5 / MODEL_SONNET_4_LEGACY / MODEL_HAIKU_4_5 constants; `lib/maverick/synthesizer.ts` single entry point with audit-tagged-by-agent + model-drift detection in outputSummary (model_matches_registry boolean); 16 synthesizer smoke tests. P.2 (`14774bd`) migration pass — every direct anthropic.com fetch (10 call sites: agent-context, sentinel/{classifier,drafter}, rehab-calibration, lib/maverick/synthesize, jarvis-brief, jarvis-chat, cron/scan-comms, buyers/{warmup-sequence,draft-outreach}) now routes through synthesize(). Synthesizer extensions made to accommodate existing call-site features (refactor charter — match what's there): timeoutMs + AbortController (briefing pattern), cache_system flag (briefing prompt-caching), SynthesizeMessage[] multi-turn (jarvis-chat pattern), user-vs-messages mutual-exclusivity, caller's max_tokens wins (registry is default, not floor — preserves agent_context's intentional 16-token cap). Pre-migration drift surface: 3 different Sonnet versions in production. Zero direct anthropic.com fetches remain outside synthesizer.ts. P.3 (`aa92b25`) `lib/maverick/voice-registry.test.ts` with 29 snapshot tests (per-agent model lockdown + inline-string voice_fragment snapshots + completeness invariants + max_tokens budget invariants) — duplicate-fragment invariant catches the "Crier sounding like Appraiser" drift class directly. P.4 (THIS) `lib/pulse/detectors/voice-drift.ts` with three drift classes: model_fallback (Anthropic served non-registry model; warning ≥1, critical ≥5 in 24h, env-overridable), disabled_agent_invoked (migration mistake hit a disabled registry entry; critical no-threshold), missing_registry_entry (audit event for unknown agent name; critical defensive); 17 detector tests; `voice_drift` added to PulseDetectorId union; runner wired with detectVoiceDrift in the runAllDetectors dispatch. **vercel.json:** Pulse cron added at `0 12 * * *` (noon UTC, no collision with existing slots 8/9/10/11/13 — within Hobby plan daily cap). **Naming policy clarified** in commit context per Alex's question: file names jarvis-brief / jarvis-chat retained for URL backwards-compat per Phase 9.11 deprecation tags; agent attribution in code is consistently "maverick"; migrations preserved legacy event_label values (jarvis_brief_synthesized) where they exist in audit history for continuity, new ones (maverick_chat_synthesized, crier_reply_drafted, scout_warmup_drafted, scout_outreach_drafted) use canonical names. 872/872 tests passing (826 → 872, +46 across P.3 voice + P.4 voice-drift). Phase 19.8 Spine wrap via Maverick MCP (live end-to-end this session): `reczd5ufnlLdTAERC` (P.1) + `recuDuCnUEpbysgcw` (P.2) + `recJQ5hDf2c2Gz1Xi` (P.3) + (this row's wrap will be the fourth). |
| 2026-05-18 | 14.7-14.12 | Commit O — Phase 14 Pulse self-monitoring. Four sub-commits: O.1 (`d98d35e`) types + 6 pure detectors (token-burn / cron-cycle-silent / spine-write-rate / test-count-regression / endpoint-error-rate / stale-data-drift) + KV-backed active-detection store (`pulse:state` key) + runner with active-set diff (Spine writes ONLY on transitions; steady-state silent — keeps the decision log clean) + `app/api/agents/pulse/scan` GET endpoint + 10 runner tests locking diff logic and Spine fan-out. **Closes Phase 20.5** (token-burn detection) and structurally locks the Phase 20.7-class silent-discipline-failure detector. O.2 (`2bcbb56`) briefing aggregator wire — `StructuredBriefing.pulse: PulseSection` always present; aggregator reads `readPulseState()` in parallel with the SourceResult fetchers and sorts active detections critical-first then newest-first; `lib/maverick/severity.inferPrioritySignals` extended with Pulse branch (severity → tier mapping info=1/warning=2/critical=3; truncated 160-char description; href: "/pulse"); active-store refactored to store full `PulseDetection` objects with `first_seen_at` so render-path doesn't re-run detectors; 6 severity tests + 3 pre-existing test-fixture patches (EMPTY_PULSE export). O.3 (`c79fe50`) `components/factory-floor/PulseRoom.tsx` with 3-column severity strip + top-3 detection headlines + tierOverride driving room border to highest active severity + `app/pulse/page.tsx` → `components/pulse/PulseDashboard.tsx` (auto-scans on mount + expand-on-click detection cards with description + suggested-action callout + source-data JSON + manual "Scan now" button; mirrors SentinelApprovalQueue shape). FactoryFloor swaps `StandingByRoom` placeholder for `PulseRoom`. O.4 (THIS) 54 per-detector tests across 6 detectors (known-good + known-bad + env-override + edge-case fixtures) + Master Checklist wrap (this entry + rows 14.7-14.12 above). **Phase 14 charter holds end-to-end:** Pulse surfaces only — no auto-remediation. Operator decides every action. Naming drift logged: Alex's brief reused "Phase 14" sub-numbering for self-monitoring sub-items distinct from the existing 14.1-14.6 (confidence thresholds / capability monitoring / family-time signal); new rows are 14.7-14.12. 810/810 tests passing (756 → 810, +54). Phase 19.8 Spine wrap via Maverick MCP (live end-to-end this session): `reckJOLmEp02LpaR4` (O.1) + `recouykaEEaWGX6Js` (O.2) + `recYrpTHJiCXpqZ4M` (O.3) + (this row's wrap will be the fourth). |
| 2026-05-26 | INV-005 / INV-022 / INV-023 / INV-029 / INV-031 / Crawler 1.0 | **Crawler 1.0 + Pre-EMD gate build-out** (post-main-merge, shipped direct to `main`). **INV-005** (`2dfa57e`) Phase 4B rehab manual-fallback affordance + `Rehab_Source` provenance + nightly vision-retry/drift cron. **INV-022 v1** (`ee0791a`+`d6cf0d4`+`303d942`) Data Federation: `Property_Intel` table (`tbllf0GNjYepvnUuv`, 45 fields incl. 7 INV-028-reserved) + RentCast/ScraperAPI/FEMA hydrators + `/api/cron/data-federation-pull` (16:00 UTC) with partial-failure isolation. **INV-023** mortgage payoff viability check added to Underwriter brief (doc). **INV-029** (`bfe9995`) Pre-EMD DD Gate (`pre_emd`, 7 blocking checks, `/api/orchestrator/run-gate`) + `Memphis_Assignment_Verified` (`fldZx12OwNJEXWbpQ`) + `EMD_Operator_Signoff` (`fldl8GIxjBS5KiMq4`) checkboxes; `property_intel` DataSource added to gate framework. **Crawler 1.0** (`65c4e8d`→`579e9bb`→`828cf60`→`f22661e`→`8a5a7ff`→`8beb057`→this): RentCast `/listings/sale` intake source (replaced ATTOM, retained for INV-023) → vendor-agnostic intake-filter (SFR, beds≥2, $20K-$400K flat, DOM≥14 long-DOM-is-distress) → Firecrawl `/v2/search` portal verify (renovation/turnkey + wholesaler-exclusion + condition-signal filters; INV-028 closed-by-merger) → dedup → `/api/cron/listings-intake` (03:00 UTC, dry-run default, `?zips=` override, `CRAWLER_TARGET_ZIPS`/`CRAWLER_INTAKE_LIVE` env). **INV-031** (`8beb057`) parallelized Firecrawl verify via bounded-concurrency pool (`lib/crawler/async-pool.ts`, `FIRECRAWL_MAX_CONCURRENT` default 20) + global rate gate + 270s wall-clock guard (fixes Vercel-Hobby-300s coverage cap). **This ship:** inverted DOM filter (too_old→too_new bug — long-DOM was being discarded) + softened condition_signal_missing to a write-with-`Outreach_Status="Review"` flag (terse-copy markets) via pure `classifyVerifiedListing`. Env added: `FIRECRAWL_API_KEY`, `FIRECRAWL_MAX_CONCURRENT`, `FIRECRAWL_MAX_SCRAPES_PER_RUN`, `FIRECRAWL_RATE_LIMIT_PER_MINUTE`, `DISTRESS_DOM_CAP` (optional). Spine: `rec84OQ8UCqLKElCU` / `rec8xYpE1Mw46JvjP` / `recCHkJP6z5zFwWa7` / `recqAAiiU0YEojxJK` / `recPEyzvAsmsCWnSJ` / `reckcIGI0ikt7Cc3Z` / `recQ0hA8Bs6R05kik` (+ this ship). 1284/1284 tests; tsc clean. **Operator-gated for live:** per-ZIP dry-run validation across 15 launch ZIPs → flip `CRAWLER_INTAKE_LIVE=true`. |
| 2026-05-26 | Crawler 1.0 / INV-031 (regression fix) | **e08a81d ship-blocker fix — active distress listings were being dropped.** Two root causes found by verifying against the actual code (NOT the commit's own diff — `e08a81d` was purely additive, the regression was a side effect of two pre-existing gates the new volume exposed): **(1) DOM lower floor.** `intake-filter.ts` `minListedAgeDays:14` rejected fresh-but-distress listings as `listed_date_too_new` BEFORE Firecrawl ever ran — this is what actually dropped 3410 El Paso (3 DOM) + 2810 W Salinas (10 DOM), NOT `firecrawl_inactive` as first diagnosed. **Removed the lower floor entirely** (reverses the prior-brief `≥14` mandate — operator's #4/#5 "fire on EVERY active band listing" + the long-DOM-is-distress principle `recX6jnNN3GzRrG2z` justify removal; a 3-day-old as-is listing is exactly a first-low-offer target). Optional `DISTRESS_DOM_CAP` upper bound retained; when unset (default) listed-date is no longer evaluated and a missing date no longer blocks intake. **(2) `detectStillActive` full-page false-positive.** `INACTIVE_MARKERS` substring-scanned the whole scraped page, so Zillow/Redfin nearby-homes / recently-sold / pending-comps boilerplate ("off market", "sold on", "this home sold", "sale pending") false-flagged active subject listings as inactive — this dropped 3719 W Houston (42 DOM). **Tightened `INACTIVE_MARKERS` to 3 unambiguous removal phrasings** ("no longer available", "listing removed", "no longer on the market"); RentCast remains the authoritative Active source, Firecrawl is staleness double-check only. Tests: `firecrawl-fixtures.test.ts` pins all 3 zpids (26222373 / 26124490 / 26138273) through the pure verify→classify pipeline → ACCEPT; updated `intake-filter.test.ts` (no-floor) + `firecrawl.test.ts` (boilerplate guard). 1288/1288; tsc clean. **No hand-coded address/zpid exceptions** — both fixes are general (operator requirement #4). **Could NOT re-scrape the 3 live zpids** (no Firecrawl key in container; fixtures are representative reconstructions of the active-listing-plus-comp-boilerplate failure shape) — operator runs the live re-scrape + 78207 re-run (target accepted ≥11) out of band. Spine `rec0StKyGRs8z37SQ` (diagnostic, supersedes the inactive-mechanism hypothesis) related to `reczlBG2pMMOVTtsf`. |
| 2026-05-26 | Phase 1 / Phase 9 — cleanup-stale-outreach + INV-A | **Pre-H2-live stale-outreach cleanup mechanism shipped.** `POST /api/admin/cleanup-stale-outreach` (auth waterfall + dashboard cookie; `dry_run` DEFAULTS TRUE — writes only on explicit `{"dry_run":false}`) resets the Auto-Proceed backlog (~807 expected) via **Do_Not_Text=true + a provenance note** (NON-destructive, reversible, preserves outreach history). Pure selector `lib/admin/cleanup-stale-outreach.ts` (`selectStaleOutreach`/`matchesCleanupCriteria`/`buildCleanupNote`, 14 tests): Outreach_Status empty + Execution_Path "Auto Proceed" + Live_Status "Active" + Agent_Phone present + not already Do_Not_Text; **never-resurface addresses EXCLUDED** (new shared `lib/never-resurface.ts`), **restricted-state matches flagged P1** (expected 0). Batches of 10 (Airtable's real PATCH cap, NOT 50 as briefed) with per-batch isolation + `sentry` audit. **DELETE intentionally NOT implemented** — irreversible, gated on explicit operator confirmation (brief recommended Do_Not_Text over the operator's stated DELETE preference). **Field-ID discrepancy flagged:** brief listed `Execution_Path = fldNRMrcxbiKHW1C9`; production codebase (airtable.ts + verify-listing) uses `fldOrWvqKcc1g6Lka` — trusted the codebase mapping. **INV-A (Candice dedupe fix) is NOT in this commit:** "H2 Module 20" is a Make.com scenario module, not codebase — no agent-phone dedup helper exists in the repo, so "same commit cycle" can't apply. INV-A needs a Make-scenario edit (match on Agent_Name OR Agent_Email when Agent_Phone mismatches) before H2 isActive=true; surfaced for operator direction rather than autonomously editing a live automation. Spine `recThGRtrWQjfCoM5` (agent dedupe principle). 1302/1302 tests; tsc clean. |
| 2026-05-18 | 13.7 / 13.8 / 13.9 | Commit N — Phase 13 Sentinel inbound autonomy. Four sub-commits: N.1 (`93eb356`) `lib/sentinel/types.ts` (7-intent taxonomy: motivated / lukewarm / rejection / question / wire_fraud_red_flag / off_topic / spam — richer than scan-replies' 4-bucket regex; 7 red-flag categories listed individually for downstream Pulse baselining) + `lib/sentinel/classifier.ts` (Anthropic Sonnet via injectable fetcher; pure coercion helpers exported individually with safe `off_topic` default; SENTINEL_SYSTEM_PROMPT specifies JSON schema + v1.3 motivation 1-5 scale; conservative bias on wire-fraud false-positives) + `app/api/sentinel/classify/[recordId]/route.ts` (GET classifies latest inbound from `lib/notes.lastInboundLine`; POST { body } for explicit-body classification; per-call audit `agent: sentinel, event: sentinel_classified`); 38 pure + injected-fetcher tests. N.2 (`42705e9`) `lib/sentinel/drafter.ts` (7 stable option IDs + `optionsForIntent` dispatch + SentinelDraftPackage shape + alert-only LLM-call short-circuit saves Anthropic budget on wire-fraud/off-topic/spam intents + SENTINEL_DRAFTER_SYSTEM_PROMPT: direct/agent-respectful voice, no cold-script reintros, SMS default with email for multi-paragraph, no inventing numbers) + `app/api/sentinel/draft/[recordId]/route.ts` GET classifies-then-drafts in one round-trip; POST { body?, classification? } skips re-classification for the approval queue path + extended classify route with `?apply_motivation=1` writing `Seller_Motivation_Score` per v1.3 spec (closes the Phase 20.2 loop) with never-stomp gate; 32 drafter tests. N.3 (`1cead30`) `app/api/sentinel/queue/route.ts` lightweight metadata feed (lastInboundAt > lastOutboundAt gate, sorted newest-first, NO LLM calls) + `components/sentinel/SentinelApprovalQueue.tsx` lazy per-row classify+draft on operator click (no auto-fire) + classification chips + draft cards with editable bodies + Send button routes through existing `/api/deal-action/[id]` action_type=send_reply (identical writeback path to JarvisGreeting CardBlock) + per-row dismiss + alert-only amber banner + `app/sentinel/page.tsx` shell + `components/factory-floor/SentinelRoom.tsx` extended with inbound-queue teaser (count + deep-link, role updated "Intake → Intake + Inbound triage"). N.4 (THIS) `lib/sentinel/motivation-gate.ts` extracted pure `decideMotivationApply` gate from the classify route (gate ordering: not_requested → intent_not_motivated_or_lukewarm → no_hint → existing_score_set → apply) + 13 unit tests locking the destructive boundary + Checklist wrap (this entry + rows 13.7 / 13.8 / 13.9 above). **Approval-gated per Phase 13 charter:** No auto-classify on queue load (saves Anthropic spend), no auto-send (every send is an explicit operator click), motivation-write IS NOT outbound but still gated never-stomp on operator-set values. Naming drift logged: Alex's brief reused "Phase 13" for inbound autonomy; existing 13.1-13.6 (Sentinel intake / Crawler) remain a separate NOT-STARTED track; new rows are 13.7 / 13.8 / 13.9. 740/740 tests passing (657 → 740, +83). Phase 19.8 Spine wrap via Maverick MCP (live end-to-end this session): `rec7jv3uKohXGAgIF` (N.1) + `rec3uNPSW0iRShdyS` (N.2) + `recl3tKt08Pw72h2C` (N.3) + (this row's wrap will be the fourth). |

---

## Skip log

| Date | Item # | Status | Reason |
|------|--------|--------|--------|
| 2026-05-15 | 17 (subset) | SKIPPED-REPLACED | Coaching / Cohorts / Discord / SaaS / DFY permanently out of scope. Alex is a loner |

---

## The bottom line

This document is the operating instruction set for Inevitable. **If we're not working on an item from this checklist, we're either (a) closing a deal in Phase 16, (b) handling a real-world fire that needs documentation as a new insertion, or (c) wasting time.**

After Code's 5/16 audit pass, the high-leverage next actions are:

1. **Phase 11.4 fix** — wire `Stored_Offer_Price` into the pricing-agent + H2 outreach-fire path (~30 line change, unblocks V2.1 pricing discipline visibility)
2. **Phase 12.1 + 12.2** — provision `VERCEL_API_TOKEN` + `GITHUB_PAT` env vars (no code change, immediate briefing improvement)
3. **Phase 12.6** — Airtable concurrent-source contention mitigation (directly relieves Gate 5 P95 overshoot)
4. **Phase 11.1 fix** — Quo quiet-vs-down false-negative (Maverick currently lies about Crier health)
5. **Phase 9.3** — Named-agent vocabulary in code attribution (foundation for Phase 9.4 + Phase 13)
6. **Phase 9.1 + 9.2** — Shepherd panel + priority surface BroCards (Daily UX Spec's foundational UX shift)

Items 1-4 are low-effort high-leverage fixes that should land before any Phase 9 dashboard rework begins. Items 5-6 begin the dashboard rework proper.

---

*Document v1.1 — Code-audited May 16, 2026. Living Artifact. The next version is written when a Claude session refreshes status against repo state, when new items get inserted via the insertion log, or when phases complete and procedural focus shifts.*